# Debt Ledger

The only place accepted technical debt lives. It is public on purpose: an honest debt register is
a trust feature, and several entries here make excellent `good-first-issue`s.

**Rules** (from `CLAUDE.md`):

- Adding an entry **requires a repayment trigger**. "Someday" is not a trigger — an entry without
  one is a rejected review finding, not accepted debt.
- Reviewed at every minor release. Each entry is repaid, re-dated with a written reason, or
  promoted to "won't fix" with the rationale moved into an ADR. Silent carryover is not an option.
  `CLAUDE.md` specifies that CI fails a release tag when a trigger has lapsed without a disposition,
  and that check is now built: `scripts/ledger-check.py`, run by `make ledger-check` from
  `.github/workflows/release.yml`. It is deliberately narrow — it asks only whether an entry whose
  trigger **names the release being cut** carries a disposition, because a checker that guessed at
  "when a second Tuner lands" would be wrong often enough to get switched off. Everything it cannot
  ask is still asked by hand, at every minor release, one row at a time.

**Last full audit:** 2026-09-01, before v0.2. Every open entry carries a dated disposition from it.
Two things that audit found are worth keeping in mind when writing a new row. A re-date announced in
the *What* column but never written into the *Repayment trigger* cell is invisible to the gate,
which reads only the trigger cell — five rows had drifted that way. And a trigger conditioned
solely on somebody else moving (a vendor shipping a feature, a user filing a report, data nobody
counts) cannot lapse, so it needs a date leg beside it.
- `TODO`/`FIXME`/`HACK` comments in code must reference an entry here (`// DEBT(docs/debt.md#42): …`)
  or an issue. The lint bundle rejects naked ones.

Reference an entry by its number: `docs/debt.md#3`. Each row carries an explicit HTML anchor, so
those links actually resolve on GitHub — a table row is not a Markdown heading and would otherwise
have nothing to point at.

The numbering jumps from 154 to 165. Entries 155–164 were accepted by the redundancy-detection
work and live on its open PR ([#178](https://github.com/uknoAI/kno/pull/178)), which does not merge
— see [ADR-0008](adr/0008-measured-redundancy-is-cut-from-v0-2.md). Only #165 is here, because the
decision it produced landed without the feature; the rest land only if that branch ever does, and
holding their numbers is what keeps every citation of them — in the plan, in the PR, and in that
branch's own code comments — resolving to the entry it names.

| # | What was deferred | Why | Repayment trigger | Owner |
|---|---|---|---|---|
| <a id="1"></a>1 | **REPAID in V-4c** (2026-08-27). `valuationFor` gates BOTH delta numbers on their intervals: a nil interval — fewer than two pairs, or ragged per-Case attrition — sets `not_measured=UNDERPOWERED` and ships no number, and the proto godocs now say a delta is omitted exactly when `not_measured` is set. `TestValuationOmitsDeltaWithoutInterval` and `TestRaggedAttritionReportsUnderpoweredNotAShrunkenDelta` pin both shapes. Original entry follows. ~~Prime directive 5 ("no reported delta without its CI") is **representable but not enforced**. There are **five** delta/interval pairs (AssetValued joined them in M2-V0), all unguarded at construction time: `Valuation.delta_goal`/`delta_interval`, `Valuation.delta_control`/`control_interval`, `Portfolio.dev_estimated_gain`/`dev_estimated_interval`, and `Report.holdout_gain`/`holdout_interval`. Separately, "no holdout access before Validate" is a runtime property no schema can express. | Phase-1 finding A3, broadened by Phase-3 review of the schema — the original entry named only the first pair, which would have left the M1 implementer to rediscover the other three mid-work. `TestEveryDeltaHasAnIntervalField` now enumerates all four, so the checklist is executable rather than prose. The schema guarantees the *field* exists; only construction-time code can guarantee it is populated. | Construction-time invariant when the Value stage lands (M1); holdout-canary test lands with the Baseline stage (M1). | @devarispbrown |
| <a id="2"></a>2 | ~~No typed `Run` resource.~~ **REPAID** in M1-1: `Run` is a first-class message with stage, status, fingerprint, and the three Case counts. Original entry follows for the record. Run IDs are bare ULID strings; `Event`, `Report`, and the future API all reference them untyped. | [Plan](plans/2026-08-18-repo-foundation.md), finding A6. Adding a typed `Run` message later is purely additive, so deciding now bought nothing. | Before `serve` / the event stream lands (v0.3). | @devarispbrown |
| <a id="3"></a>3 | **CARRIED, trigger cell corrected** (2026-09-01, v0.2 ledger audit) — unfired: the tree still holds no benchmark at all (no `func Benchmark` outside `bin/`), so `make bench-diff`'s tripwire has nothing to fire on, and no PR has claimed a performance change. The correction matters more than the carry: the 2026-08-29 re-date announced a new trigger in this column and never wrote it into the trigger cell, which is the cell the release gate reads. Moved. Original entry follows. **RE-DATED** (2026-08-29, v0.1 ledger review): the trigger (first PR touching the scoring loop, routing, or NDJSON framing) fired with M1 and was never disposed — recorded as the lapse it is, not silently carried. Re-dated to **"before 1.0, or the first PR that claims a performance change to the scoring loop"** — whichever is first: the tripwire stays armed and CI-enforced (`make bench-diff` hard-fails the moment a benchmark appears unbaselined), and the v0.2 performance work is the natural home for the first baselines. Original entry follows. `make bench-diff` does not compare benchmarks. It is a **tripwire**: it passes while no benchmark exists and hard-fails the moment one appears. | [Plan](plans/2026-08-18-repo-foundation.md), finding A9, hardened after Phase-3. A target that silently passes forever would drift into "we never got around to it"; failing loudly on the first benchmark makes the trigger self-enforcing rather than a note. Documented in CONTRIBUTING.md so the red build is not a surprise. | **Before 1.0, or the first PR that claims a performance change to the scoring loop, routing, or NDJSON framing — whichever is first.** (Re-dated 2026-08-29; moved into this cell 2026-09-01, where the release gate can read it. The original trigger, which fired at M1, was: before the first PR touching the scoring loop, routing, or NDJSON framing merges.) `make bench-diff` stays armed: it passes while no benchmark exists and hard-fails the moment one appears unbaselined. | @devarispbrown |
| <a id="4"></a>4 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired since the 2026-08-31 re-date: `cli/config.go`'s last two commits are 603053b (2026-08-28) and 360cabc (2026-08-29), both before it, and the tree still holds exactly one fuzz target, `adapters/agent/agentref/fuzz_test.go`. The stale Makefile claim this entry names is still there and has drifted line number — it now sits at `Makefile:418-419`, not `:371`. Original entry follows. **RE-DATED** (2026-08-31, v0.2 ledger audit) — the trigger fired and nothing caught it. The kno.yaml parser landed with #102 (`yaml.Unmarshal` in `cli/config.go`), and this entry requires a fuzz target **in the same PR**. There is none: the tree has exactly one, `adapters/agent/agentref/fuzz_test.go`. Worse, `Makefile:371` still asserts the kno.yaml loader 'does not exist yet', so the gate's own text disagrees with the code — which is why nobody noticed. New trigger below can lapse and is checkable by grep. Original entry follows. **Partly repaid in M2-4** (2026-08-21): the agent-ref parser landed with `FuzzParse` in the same PR, as the trigger requires, and `make fuzz-short` now runs rather than reporting PEND. It found five real defects — one in the parser's design (a base URL carrying a fragment) and four in the canonicalization added during review. The plugin handshake, NDJSON frame, and `kno.yaml` parsers still have none. Original: **there are no fuzz targets at all.** `make fuzz-short` discovers targets rather than hardcoding them, so it currently finds none and reports PEND. | Phase-1 finding G11, corrected by Phase-3: an earlier draft of this entry claimed the target fuzzed proto unmarshal. It did not — describing behavior the code does not have is exactly the failure this ledger exists to prevent. The parsers `CLAUDE.md` names (plugin handshake, NDJSON frames, agent-ref, `kno.yaml`) do not exist yet. | **Before 1.0, or the next PR touching `cli/config.go`'s parse path, whichever is first.** That PR adds `FuzzConfig` over the loader and corrects `Makefile:371`'s stale claim in the same change. The plugin-handshake and NDJSON legs stay future (both packages are `doc.go` only). | @devarispbrown |
| <a id="5"></a>5 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `google.golang.org/protobuf` is still v1.36.12, `tools/go.mod` has a single commit in its whole history, and `default_api_level=API_OPEN` is still pinned at `buf.gen.yaml:20-25`. No major bump has happened. Original entry follows. `protoc-gen-go`'s Opaque API is not adopted; `api_level = API_OPEN` is pinned. A flip would break every struct literal and test fixture at once. | [ADR-0001](adr/0001-proto-as-domain-types.md), finding G3. The Opaque API would insulate call sites from generated-struct changes, but migrating now costs more than pinning. | Before any `protoc-gen-go` **major** version bump. | @devarispbrown |
| <a id="6"></a>6 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no `kno.v1` field-naming policy has been written, and 1.0 has not been cut. Original entry follows. Generated field spellings diverge from Go convention and from `DESIGN.md`'s own sketch: `Asset.Id`, not `Asset.ID`; `Url`, not `URL`. Because `gen/` is lint-excluded, `ST1003` never fires, so the divergence is permanent **and invisible**. | [ADR-0001](adr/0001-proto-as-domain-types.md), finding G9. A direct consequence of proto messages being the domain types; the alternative was a converter layer (rejected, see the plan's Alternative F). | Set an explicit `kno.v1` field-naming policy before 1.0. | @devarispbrown |
| <a id="7"></a>7 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: Ring-2 plugin work has not begun — `plugin/` is `doc.go` only. Note for the next milestone: this trigger names v0.3, and the release gate now reads a minor series rather than only a patch-complete version, so a 0.3.0 tag will refuse this row without a disposition. Original entry follows. The `core` import-boundary test enforces only the **Go import graph**. It cannot detect schema contamination — the platform adding fields to shared `kno.v1` messages. | [Plan](plans/2026-08-18-repo-foundation.md), finding G10. No Go-level check can see this; it is a proto-review problem. [ADR-0003](adr/0003-platform-schema-boundary.md) records the intended split. | When Ring-2 plugin work begins (v0.3), which adds new import surfaces to reason about. | @devarispbrown |
| <a id="8"></a>8 | ~~Borrow-for-one-iteration is a godoc convention, not enforced.~~ **REPAID** in M1-4: the executor clones in the producer before dispatch, and `TestWorkersDoNotShareCaseMemory` drives it with a buffer-reusing source under `-race`, verified failing without the clone. Original entry follows. "Iterator output is borrowed for one loop iteration; clone before retaining or mutating" is a **godoc convention**, not a compiler-enforced rule. | [Plan](plans/2026-08-18-repo-foundation.md), finding G12. Concurrent workers mutating a shared `*Asset` is an ordinary data race that `-race` catches only if a test happens to exercise it. | Before the first pipeline stage that fans out workers over one iterator (M1). That PR must add the race test. | @devarispbrown |
| <a id="9"></a>9 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no platform proto package exists, no platform proto work has begun, and 1.0 has not been cut. Original entry follows. No mechanism decided for platform-only proto fields. With shared `kno.v1` types, "just add a field to `Case`" is the path of least resistance for platform work. | [ADR-0003](adr/0003-platform-schema-boundary.md), finding G14. Recorded intent: a separate platform proto package referencing OSS IDs; `kno.v1` never carries platform-only fields. Not yet enforced by anything. | **Before 1.0, or when platform proto work begins, whichever is first.** The date leg is new (2026-08-31): 'before platform proto work begins' names a milestone nobody has scheduled, so it could never lapse and CI could never fire on it. | @devarispbrown |
| <a id="10"></a>10 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `depguard` and `forbidigo` are both still enabled (`.golangci.yml:35-36`) with their rule sets intact, and no `//nolint` in the tree targets either. The gates have not been bypassed or disabled. Original entry follows. The proto-alias design was chosen over generated idiomatic types + converters. Four review findings (G1, G2, G3, G9) are **handled by lint gates rather than dissolved by design**. | [Plan](plans/2026-08-18-repo-foundation.md), Alternative F, finding G13. Converters are a second representation that can drift; `DESIGN.md`'s "defined once, in protobuf" reads most literally as aliases. | Revisit if the depguard/forbidigo gates are bypassed, disabled, or prove insufficient in review — or at 1.0, whichever comes first. | @devarispbrown |
| <a id="11"></a>11 | The `KNO_MAX_COST_USD` budget cap is read by **no code**. `stats/budget` is a stub. | Phase-3 finding F3. The nightly live-API job was armed with real credentials and a cap comment asserting a protection that did not exist — a silent-overspend trap set ahead of the guard. Mitigated by a `live_spend_guard` make define that refuses to run unless the cap is set *and* some Go file actually references it, so the trap fails closed instead of spending. (Correction, 2026-08-21: this previously said the mitigation was `make test-live`. It was — and only that. `make record-fixtures` set `KNO_LIVE_TESTS=1` itself and passed neither check, so the mitigation had a hole the same shape as the finding it mitigated. Phase-1 review of the M2 plan caught it. The guard is now a shared define both targets call, and both were verified to fail closed on both conditions.) | ~~M0c~~ **lapsed without disposition; re-dated 2026-08-21.** `stats/budget` landed in M0c but nothing reads `KNO_MAX_COST_USD`: the guard takes its `Limits` from the caller, and the only caller is the CLI, which reads flags. Nothing was at risk in the interval — the interlock refuses to run live tests while no Go file references the variable, so it fails closed rather than spending. Re-dated to **M2, with the first real adapter**, which is the first time a live test can spend anything. That PR must wire the env cap into the live-test path and delete the grep interlock. **REPAID in M2-7** (2026-08-22). `adapters/agent/openaicompat/live_test.go` reads `KNO_MAX_COST_USD`, parses it into `budget.Limits`, and authorizes every live call through a real `budget.Guard` — Estimate, Authorize, Invoke, Settle, the same four steps in the same order as `core.invokeOnce`. A missing variable is `t.Fatal`, not a skip: a skip lets a misconfigured nightly job report green having tested nothing. The Makefile's grep interlock is deleted in the same change, as this entry required — it matched a comment as readily as an enforcement, so once real enforcement existed it could only mislead. | @devarispbrown |
| <a id="12"></a>12 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `make vuln` still runs `govulncheck ./...` over the shipping module only (`Makefile:401-403`) while three modules exist (`./go.mod`, `tools/go.mod`, `tools/goreleaser/go.mod`); no tool dependency has appeared in an advisory, and 1.0 has not been cut. Original entry follows. `make vuln` scans only the shipping module. The `tools/` module's dependency graph — roughly 300 packages whose binaries execute in CI — is unscanned. | Phase-3 finding F12/LOW. `CLAUDE.md` describes this gate as "govulncheck + dependency audit"; only the first half exists. `govulncheck ./...` in a module with no Go packages errors rather than scanning its dependencies. | Before 1.0, or the first time a tool dependency appears in a security advisory. | @devarispbrown |
| <a id="13"></a>13 | **REPAID** (2026-08-27). v0.0.2 shipped through the full pipeline — six archives, six SBOMs, a cosign bundle over `checksums.txt` — and the verification chain ran for real: `cosign verify-blob` accepted the bundle against the pinned identity and OIDC issuer, `gh attestation verify` accepted the SLSA provenance, and `install.sh` installed the binary end to end. It took three attempts, each failing on a distinct, real defect the pipeline had never executed far enough to expose (cosign's legacy flag refusal, then the new-format validation, then a case-sensitive identity regexp that never matched the repo owner's casing), and the first published-but-empty release page pattern is recorded as [#81](#81). Original entry follows. **PARTLY REPAID in M2-V4-release** (2026-08-26) — the machinery exists and has never run. goreleaser builds six platforms, cosign signs the checksum file keylessly, syft attaches an SPDX SBOM per archive, `actions/attest-build-provenance` records SLSA provenance over the checksummed set, and `install.sh` verifies the checksum unconditionally and the signature whenever cosign is present. `make release` refuses outside CI, the job carries an `environment:` and refuses a commit that is not an ancestor of `main`, and `make release-check` validates the config on every PR. Phase-3 review caught two defects that would each have made the FIRST tag produce nothing: `make release` had no goreleaser prerequisite and exited 127 (reproduced), and `"draft": true` in release-please-config.json meant no git tag was ever created — GitHub creates the ref when a release is published — so the trigger could not fire, `workflow_dispatch` had no tag to select, and RELEASE_PLEASE_TOKEN would not have helped. Both fixed. It stays PARTLY because signing, provenance and publishing have still never executed, and a pipeline verified only by reading is not a repaid one. Original entry follows. ~~No release pipeline: no goreleaser, no signed artifacts, no SLSA provenance, no SBOM, no checksums.~~ | [Plan](plans/2026-08-18-repo-foundation.md) defers it deliberately — there is no binary worth releasing yet. Phase-3 finding F8 caught `SECURITY.md` asserting it in the present tense to security researchers; that text now says plainly that it does not exist. | **The first tag must produce a set of artifacts that `cosign verify-blob` and `gh attestation verify` both accept, and that `install.sh` installs end to end.** Until then this stays open. A trigger of "before the first tagged release" cannot lapse for an entry whose whole content is that the first tagged release has not happened. Before the first tagged release. No binary ships without it. **REPAID 2026-08-26** ([plan](plans/2026-08-26-release-pipeline.md)). `.goreleaser.yaml` builds six archives (darwin/linux/windows × amd64/arm64, `CGO_ENABLED=0` — `modernc.org/sqlite` is pure Go, which is what makes one runner enough), emits `checksums.txt`, signs **that file** with keyless cosign, and attaches an SPDX SBOM per archive; `.github/workflows/release.yml` attests SLSA provenance over every artifact the checksum file names, with `contents: write`, `id-token: write`, `attestations: write` and nothing else. Verified: `goreleaser check` passes, and `make release-stamp` builds a binary and **reads its `--version` back** — the only way to catch an `-X` path that names the wrong symbol, a failure that is otherwise silent and ships a binary reporting `dev` forever. Both run in CI on every PR, so neither is a target someone has to remember. Not verified, and stated rather than implied: signing, provenance and publishing have never executed, because they cannot be exercised without tagging (the general form of [#16](#16)). Every action in the new workflow is SHA-pinned, so it adds nothing to [#14](#14). Phase-1 review found that the workflow would never have fired at all — see [#74](#74), which is the reason the release is now created as a draft and only published after attestation succeeds. `SECURITY.md`'s "there is no release pipeline" paragraph is replaced by what a researcher should actually run to verify a download, and `README.md` gained an Install section that installs a checksum-verified binary. Four new entries record what this change accepted rather than solved: [#71](#71), [#72](#72), [#73](#73), [#74](#74). | @devarispbrown |
| <a id="14"></a>14 | **Partly repaid, and the entry's own text is now overstated** (2026-09-01, v0.2 ledger audit) — 11 of 23 `uses:` lines are SHA-pinned today (all of `release.yml`, `release-please.yml` and `pricing-check.yml`, plus two in `ci.yml`), and `scripts/actions-pin-check.sh` enforces the pinning (`Makefile:412-414`), deliberately outside `make check`. The remaining 12 are enumerated in issue [#152](https://github.com/uknoAI/kno/issues/152). "GitHub Actions are pinned to mutable tags" is no longer true of the repository; it is true of 12 named lines. Carried on its date leg: 1.0 has not been cut and no action we use has been compromised. Original entry follows. GitHub Actions are pinned to mutable tags (`@v4`, `@v5`) rather than commit SHAs, while every Go tool is byte-pinned by `go.sum`. | Phase-3 finding F12. Inconsistent supply-chain posture: Dependabot bumps the tags, but a tag can be moved. | Before 1.0, or immediately if any action we use is compromised. | @devarispbrown |
| <a id="15"></a>15 | **CARRIED, with one of its own suggestions now built** (2026-09-01, v0.2 ledger audit) — the ledger-lapse check this entry offers as a `good-first-issue` exists: `scripts/ledger-check.py`, run by `make ledger-check` from `.github/workflows/release.yml:124`. The remaining candidate is the docs-diff check alone. Everything else is unchanged and accurate: the plan link and the behavior-change/docs-diff pairing are human-only, and `.github/pull_request_template.md` says so in its own text rather than claiming CI does it. Unfired: 1.0 has not been cut. Original entry follows. Nothing verifies the Definition of Done in the PR template, the CHANGELOG entry, the plan link, or the behavior-change/docs-diff pairing `CLAUDE.md` describes as gated. | Phase-3 finding F8. These are enforced by human review only. The template previously claimed CI verified them, which was false; it now says a human does. | **Partly repaid** (2026-08-26): the CHANGELOG-entry half is now mechanical — `.github/workflows/changelog.yml` fails a PR whose title's Conventional Commit type is not `refactor:`/`chore:`/`test:`/`build:` unless `CHANGELOG.md` is in the diff, with a visible `no-changelog` label as the escape hatch. See [#49](#49). The plan link, the docs-diff pairing, and the behavior-change label are still human-only, so this stays open on its original trigger: **before 1.0. A ledger-lapse check and a docs-diff check are both good `good-first-issue` candidates.** | @devarispbrown |
| <a id="16"></a>16 | **Partly repaid** (2026-09-01, v0.2 ledger audit) — `make selftest` exists (`Makefile:464-466`, `scripts/selftest.sh`) and covers `docs`, `ledger-check` and `judge-calibrate-check`, so this entry's "a `make selftest` target ... is a well-scoped `good-first-issue`" is stale prose describing work already done. What remains: the target is not in `make check`, and the uncovered gates are tracked as issue [#151](https://github.com/uknoAI/kno/issues/151). Carried on its date leg: 1.0 has not been cut. Original entry follows. Nothing verifies that a gate actually *fails* when it should. Every gate is currently trusted on the strength of a one-off manual check. | The `.SHELLFLAGS` defect proved the risk concretely: eleven recipes reported success while the commands inside them failed, and `make check` was green throughout. A gate is only real once it has been seen to fail. | Before 1.0. A `make selftest` target that deliberately breaks each invariant and asserts a non-zero exit is a well-scoped `good-first-issue`. | @devarispbrown |
| <a id="18"></a>18 | `coretest`'s goroutine-leak check is opt-in via `LeakCheck()` and unusable from a `t.Parallel()` test, so most adapters will not run it. | goleak takes a process-global census: a parallel sibling's goroutines are indistinguishable from a leak, which both masks real leaks and flakes on scheduling order. A flaky gate gets disabled rather than fixed. The alternative — `goleak.VerifyTestMain` per adapter package — is goleak's own recommendation but cannot be enforced from a shared harness. | **Partly repaid in M2-3** (2026-08-21): `adapters/agent/internal/transport` installs `goleak.VerifyTestMain`. The entry names the adapter packages, and this is not one — but it is where the goroutines actually live (pooled connections, limiter and timeout timers), so it serves the purpose as well as the letter. The CONTRIBUTING sentence the entry requires landed with it. The adapter packages themselves land with M2-7/M2-8. Original trigger: when the first adapter lands, its package must install `goleak.VerifyTestMain`, and CONTRIBUTING must say so. **Further repaid in M2-7**: `adapters/agent/openaicompat` installs `goleak.VerifyTestMain`. Closes when `adapters/agent/anthropic` (M2-8) does the same. **REPAID in M2-8** (2026-08-22): `adapters/agent/anthropic` installs `goleak.VerifyTestMain` too, so both M2 adapters run the check. Closed. | @devarispbrown |
| <a id="17"></a>17 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired since the 2026-08-31 re-date: the platform fork is still the single one this entry names (`adapters/agent/exec/proc_unix.go` and `proc_windows.go` — no other non-test file carries a GOOS constraint), and `internal/cmd/covercheck` still has neither a GOOS matrix leg nor a named exemption list; it decides exemption from `build.Default`, which is the host runner's own platform. Original entry follows. **RE-DATED** (2026-08-31, v0.2 ledger audit) — the trigger fired with #99 on 2026-08-28 and nothing caught it. `adapters/agent/exec/proc_windows.go` and `proc_unix.go` are a real `//go:build` platform fork in shipping code, and neither is measured by `covercheck` on CI's GOOS — precisely the hole this entry describes. Original entry follows. `covercheck` measures only the packages `go test ./...` compiles on the current platform. A package whose only code sits behind a build tag for another GOOS is exempt here and unmeasured there. | Deliberate: `go test` only instruments what it compiles, so counting tag-excluded files would demand coverage for statements that never existed on this runner. The residual gap is real but small — the repo is not currently platform-forked. | **Before 1.0, or when a second platform fork lands, whichever is first.** The repayment is a GOOS matrix leg for `covercheck` or an explicit exemption naming the forked files; either is checkable, and the fork count is greppable for `//go:build` so this cannot lapse unnoticed again. | @devarispbrown |
| <a id="19"></a>19 | **REPAID** (2026-08-29) at the v0.1 release, by decision: keep the patch-bump policy. The decision is recorded in the CHANGELOG version-policy paragraph ("the recorded decision, not a default") and as a comment in `release-please.yml` (the config JSON cannot carry comments — the comment names both keys so changing either without changing the comment is the silent-reversal this entry exists to prevent). Original entry follows. ~~`release-please` is configured so `feat:` bumps only the patch version, which is not standard SemVer behavior for a 0.x series.~~ | Chosen after release-please's first successful run proposed **1.0.0** for a repository containing a schema and a build system, no engine. CLAUDE.md makes 1.0 a covenant over the proto schema, plugin protocol, exit codes, `kno.yaml` schema, and public Go API — none of which are stable. Bumping patch keeps the version far from 1.0 until someone chooses it deliberately. | At v0.1, revisit whether `feat:` should bump the minor instead. Before 1.0, set `bump-minor-pre-major` off and pick the version by hand. | @devarispbrown |
| <a id="20"></a>20 | **CARRIED, trigger backstopped** (2026-09-01, v0.2 ledger audit) — unfired: no adapter persists a per-attempt request id and no provider idempotency key is in use; `adapters/agent/internal/transport/client.go:50,336` still only discuss the header as a proposal. The trigger was conditioned entirely on a vendor shipping something, which means it could never lapse and CI could never fire on it — the shape [#46](#46)'s review rejected — so a date leg is added below. Original entry follows. The dark-spend window: a SIGKILL landing between a provider accepting a request and anything being persisted locally is unobservable. Bounded by `concurrency` in-flight calls' worth of estimated cost. | [M1 plan](plans/2026-08-19-m1-baseline.md) §0, Phase-1 review. No architecture available here closes it — the money is committed remotely before we can record intent durably. Named rather than left silent, because prime directive 4 deserves an honest boundary. **Investigated in M2-7** (2026-08-22): the repayment condition is **not met** for OpenAI. Chat Completions has no `Idempotency-Key`. The only client-supplied, pre-call identifier is `X-Client-Request-Id`, documented purely as a support-lookup handle — the same value twice produces two billed completions. (`Idempotency-Key` exists in OpenAI's Agentic Commerce Protocol, which is merchant checkout, not inference.) So M3 cannot close this window; it can make it **auditable** — persist a per-attempt `X-Client-Request-Id` before the call and a post-SIGKILL reconciliation becomes a support ticket rather than a guess. Because it is not a real idempotency key it does **not** flip `net/http`'s `isReplayable()`, so `transport`'s `req.GetBody = nil` remains what keeps "does not retry" true. | Revisit if a provider offers an idempotency key that can be persisted *before* the call — M2, with the first real adapter. **Widened by M2-10d** and tracked separately as [#50](#50): a graceful budget refusal or Ctrl-C now discards a charge that was observed, priced, and settled, which is a different window from the SIGKILL this entry describes and has a different bound. | @devarispbrown |
| <a id="21"></a>21 | **REPAID in V-4c** (2026-08-27). The second stage landed and its trigger fired: Value accepts `*SealedEvals` only, `validate` refuses without it, and the canary is a real test — `TestValueNeverTouchesTheHoldout` plants a holdout Case in the source and asserts it reaches neither routing, measurements, nor events. Original entry follows. ~~The holdout seal is enforced by type at the `Baseline` boundary. Nothing stops a future stage from accepting a raw `core.Evals` and never sealing it. | [M1 plan](plans/2026-08-19-m1-baseline.md) §3. A distinct `SealedEvals` type makes bypass a compile error at every call site that requires one — but only at call sites that require one. | When the second stage lands (M2): consider making the raw adapter constructor unexported outside its package so a sealed wrapper is the only way to obtain Cases. | @devarispbrown |
| <a id="22"></a>22 | **CARRIED, trigger backstopped** (2026-09-01, v0.2 ledger audit) — unfired: both loads are still full-set, `CompletedCases` at `store/sqlite.go:767-793` and `CompletedMeasurements` at `store/measurement.go:294-320`, and neither the ~1e6-measurement nor the 10M-Case threshold has been observed. Every leg of the trigger was conditioned on data nothing in the repo counts, so none of them could lapse on their own; a date leg is added below. Original entry follows. Resume loads every completed Case ID into memory (~26 MB at 1M Cases), a stated exception to the streaming-memory promise. | [M1 plan](plans/2026-08-19-m1-baseline.md) §2. The alternative is a point query per Case through the single writer that is already the throughput bottleneck. | **Re-dated 2026-08-26 (M2-V4a)**, which is a disposition rather than a carryover: the entry accepted the full-set load at Baseline scale, and `CompletedMeasurements` — the reader a Value resume consults — is bounded by assets x cases x arms x trials rather than by cases. That is two orders of magnitude past what was accepted here, so the acceptance does not transfer silently. Still accepted for now: the alternative is a query per measurement inside the measurement loop, which is worse on the same axis, and the set holds four small fields per row rather than a Case. Revisit when a real pool makes a Value run exceed ~1e6 measurements, or when the API streams a resume rather than loading it. If an eval set exceeds 10M Cases, or if resume memory appears in a profile. | @devarispbrown |
| <a id="23"></a>23 | The fake agent cannot exercise partial reads, connection reuse, or mid-stream deadlines, so the executor is proven only against the failure modes the fake can produce. | [M1 plan](plans/2026-08-19-m1-baseline.md) §5. It covers cancellation-during-call, scheduled failures, and scheduled rate limits; the rest need a real transport. (Correction: this entry previously claimed rate-limit *backoff* was covered. It was not — backoff did not exist until M1-5, and Phase-3 review caught the claim being false while it sat on main.) | M2, with the first real adapter — that PR must re-run the executor's conformance and concurrency tests against it. **REPAID in M2-7** (2026-08-22). `adapters/agent/openaicompat/misbehaving_test.go` re-runs the executor's conformance and concurrency guarantees against a real HTTP transport: 60 Cases at concurrency 4 through a server that deterministically 429s, 500s, truncates mid-body, and refuses, asserting exactly-once execution, the succeeded/failed partition, and the concurrency bound. The three failure modes the fake could not produce each have their own test — partial read and close-after-headers, connection reuse, and a mid-stream deadline. | @devarispbrown |
| <a id="24"></a>24 | **CARRIED, trigger backstopped** (2026-09-01, v0.2 ledger audit) — unfired: `store.SQLite` is still the only backend, and `rows.Close`'s error is still discarded at `store/sqlite.go:779` behind its `// DEBT` comment. "When a second `Store` backend lands" names a milestone nobody has scheduled, so it could never lapse; a date leg is added below. Original entry follows. `rows.Close` errors are discarded in `store.CompletedCases`. | Phase-3 review of M1-2 flagged the original `//nolint:errcheck` as lacking a linked entry, which CLAUDE.md forbids. The error is genuinely not actionable — a read-only cursor's close reports only failures `rows.Err` already surfaced, and that is checked — but the discard should be visible rather than silenced. | If a future backend's cursor close can fail independently of `rows.Err`, handle it. Revisit when a second `Store` backend lands. | @devarispbrown |
| <a id="25"></a>25 | `kno purge` and the resume done-marker share a row. Trace content lives in `outcomes.response_proto`/`score_proto`, and that same row is what tells resume a Case is finished. | Phase-3 review of M1-2. A purge that DELETES rows would silently reopen the double-spend hole this store exists to close; a purge that NULLs only the blob columns is safe and needs no migration, but nothing enforces that today. | **REPAID in M2-1** (2026-08-21). `kno purge` NULLs `response_proto` and `score_proto` and never deletes a row. `TestPurgeLeavesTheRunResumable` asserts `CompletedCases` is unchanged, as this entry required, and goes further: `SettledSpend` and the aggregate too. Verified failing against a `DELETE`-based purge — completed 10→0, spend 10000→0, aggregate 45→0. The concern that made this debt is also structurally gone: the score now lives in a `score_value` column rather than inside the blob a purge nulls. | @devarispbrown |
| <a id="26"></a>26 | `Run`'s five Case counters (`dev_case_count`, `holdout_case_count`, `attempted_case_count`, `scored_case_count`, `errored_case_count`) do not track presence. A stage that does not execute Cases — Value works over Assets, Select attempts none — writes a Run reporting a hard zero, indistinguishable from genuinely attempting nothing. | Phase-3 review of M1-1 raised this and the fix was claimed in that PR, but the edit silently did not apply and shipped unfixed. Adding `optional` now is a cardinality change that `buf breaking` correctly refuses, so it cannot be slipped in quietly. | **Partly repaid in M2-0** (2026-08-21): `CaseExecution` carries the counts with message presence — see [ADR-0004](adr/0004-per-run-observations.md), which records why it is not named `BaselineDetail` as prescribed below. The flat counters stay until their writer (`closeRun`) and reader (`cli/jsonreport.go`) migrate in M2-10, so this stays *partly* — a `DEBT()` comment pointing at a row marked repaid is worse than a live marker. Original trigger: before the first non-Case-executing stage writes a `Run` (M2, Value). Fix additively with a `BaselineDetail` submessage carrying the counts, which has message presence and needs no cardinality change — not by forcing the flat fields to optional. **REPAID** (2026-08-25). `closeRun` writes `CaseExecution` for every Baseline run — presence set by the STAGE, not derived from whether the query found rows, because a run whose Cases were all refused after being charged returns none and would then report "this stage executes no Cases" about a run that executed Cases and spent money. Counts come from `Store.CaseObservations`, aggregated from what is durable; dev and holdout come from the options, which is ADR-0004's ingestion rule. `cli/jsonreport.go` and `cli/render.go` both read it. The flat counters stay on the wire and stay written — retiring them is a separate change with a deprecation note and no reader ready. The `--json` counts stay non-pointer deliberately: the absent case is unreachable while Baseline is the only front end, so making them nullable would cost a `jq` user something and buy nothing. | @devarispbrown |
| <a id="27"></a>27 | A resumed run's `AggregateScore` is the mean over the Cases *that process* scored, not over the whole run. The case COUNTS span the whole run (fixed in M1-5), but the score sum does not: prior Scores live in the store and are not re-read. | Phase-3 review of M1-5 found the counts bug; this is the half not fixed with it. Re-reading and unmarshaling every prior Score to recompute the mean is real work for a number no stage consumes yet — Baseline's aggregate is informational, and the number that matters is `Report.holdout_gain`, which Validate computes fresh. | Before any stage reads `BaselineResult.AggregateScore` as a reference — M2 at the latest. **Mechanism landed in M2-1**: `Store.ScoreSum` sums the new `score_value` column and reports how many Cases can no longer contribute a number. **REPAID in M2-9** (2026-08-22). The aggregator seeds `priorSum` from `Store.ScoreSum` alongside the counts it already seeded, so a resumed run's mean spans the same population its counts do. `TestResumedRunReportsTheWholeRunsMean` verified failing against the tail-only mean — 0.48 reported for a whole-run mean of 0.5 over 40 Cases. The `unrecoverable` count `ScoreSum` returns is now load-bearing rather than advisory: a run holding purged Cases reports **no** aggregate and an `IncompleteReason` naming the purge, instead of a mean over the survivors that looks like a mean over the run. That distinction is what [#31](#31) is about, and it stays open. | @devarispbrown |
| <a id="28"></a>28 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `core.Baseline` still has exactly one non-test caller, `cli/baseline.go:422`, and `api/` is `doc.go` only — the second caller this entry re-dated onto does not exist. Original entry follows. `BaselineOptions.DevCases` / `HoldoutCases` / `HoldoutUnderpowered` are trusted from the caller with no cross-check against what the sealed source actually yields. | Phase-3 review of M1-5. It cannot leak the holdout — the seal still bounds what is iterated — but a bug in whatever computes the split silently produces a `Run` and an event stream misreporting their own denominator, with no defense in the stage. | **Trigger met 2026-08-21** (PR #22) by the second option: `BaselineOptions` now states plainly in godoc that the caller owns their accuracy, and the stage refuses a zero on either side so the one unrecoverable case — a run that can never be validated — is caught. Verification against the source is still not done and would cost a second pass over the iterator. Re-dated: revisit when a second caller of `core.Baseline` exists (the API, M3), since one caller owning a contract is materially different from two. | @devarispbrown |
| <a id="29"></a>29 | A resumed run emits a second `RunStarted` carrying the ORIGINAL total Case count, and the schema has no `RunResumed` variant. | Phase-3 review of M1-5. A consumer replaying full history reconstructs correctly, but a live view that resets progress on `RunStarted` visibly jumps backward on every resume. | **Partly repaid in M2-0** (2026-08-21): the `RunResumed` payload exists, carrying already-completed, remaining, total, and the spend restored from disk. The emitter still sends an unconditional `RunStarted` with the original total; that two-line change lands in M2-10, so this stays *partly*. Re-dating it to a milestone was rejected — the payload is additive to a file M2-0 already opens, which is the same argument that repaid entry 26 there. **REPAID in M2-10a** (2026-08-23). A resumed run emits `RunResumed` carrying already-completed, remaining, total, and the spend restored from disk; the unconditional second `RunStarted` is gone. `TestAResumedRunOpensWithRunResumedNotASecondRunStarted` asserts the **absence** as well as the presence — an early draft emitted both, and a test that only looked for `RunResumed` passed against it. `RunStarted`'s "always sequence 1" comment holds again: it was false for every resumed run, which emitted a second one at `maxSeq+1`. The opening event is chosen by the STREAM's state rather than by `--resume`, so a run whose first process died before emitting anything still opens with `RunStarted` — gating on the flag would have left such a stream carrying no stage, agent, or goal direction at all, which is a different hole in the same place. | @devarispbrown |
| <a id="30"></a>30 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `backfillScoreValues` still buffers every scored row before writing (`store/sqlite.go:560-605`, accumulating into `pending` and updating in a second loop), no user has reported a slow first run after upgrade, and 1.0 has not been cut. Original entry follows. `backfillScoreValues` buffers every scored row of the database into memory before writing. Measured 215 MB `TotalAlloc` and ~1.4s for 200k rows; extrapolates to ~105 MB peak and ~7s at 1M, blocking the first `kno` invocation after an upgrade with no output. | Phase-3 review of M2-1. `CLAUDE.md` calls `iter.Seq` load-bearing and forbids loading a 1M-Case set into RAM; `docs/debt.md#22` ledgers the 26 MB resume set as a stated exception, and this is roughly 4x that. Accepted for now because it runs **once per database**, on a one-time version bump, and paging it is real work for a path most users take with a few thousand rows. | Before 1.0, or the first time a user reports a slow first-run after upgrade. Fix by keyset-paging on `(run_id, case_id)` inside the same transaction. | @devarispbrown |
| <a id="31"></a>31 | **REPAID in M2-V4a** (2026-08-26), on the second attempt — the first was wrong and the correction is the useful part of this row. `ScoreSum` returns a `ScoreSummary` splitting the old single count into `Purged` and `UnknownProvenance`, discriminated by whether the **Score blob** survives. The first attempt added a `writer_schema_version` column and keyed the split on it; Phase-3 review proved that unreachable. `Purge` clears `score_proto` and never touches `score_value`, and every build since the column existed writes `score_value` on every scored row — so "scored, no number, written by a recent build" cannot occur, the `Purged` term was provably always zero, and a genuine pre-column purge (which carries no marker) was reported as the bucket meaning *no evidence of a purge*. Exactly backwards, and the test passed only because it fabricated by hand a row no binary can write. Reproduced against this repository's own M1 fixture. The blob is what actually separates the two: `score_proto IS NULL` means a purge took it before the number had a column to survive in and the migration's backfill could not reach it; `score_proto IS NOT NULL` with a NULL `score_value` is what a binary predating the column leaves in an already-migrated database, since nothing re-runs the backfill. The column was dropped rather than kept for a future use nothing has. `TestScoreSumSeparatesAPurgeFromAnOlderBinary` now builds both states for real and was verified failing against the inverted predicate. Original entry follows. ~~**REPAID in M2-V4a** (2026-08-26). `writer_schema_version` is stamped on every `outcomes` and `measurements` row this build writes, and `ScoreSum` returns a `ScoreSummary` splitting the old single count into `Purged` and `UnknownProvenance`. A scored row with no readable number is attributed to a purge only when its writer knew the score column; a row carrying version 0 predates the marker and is reported as exactly that. Older rows are NOT backfilled to the current version — doing so would assert a provenance this build cannot know, which is the same false confidence the entry describes, written by us instead of inherited. `TestScoreSumSeparatesAPurgeFromAnOlderBinary` drives all three states and was verified failing against a predicate that conflates them. `Unrecoverable()` sums the two for callers that only need to suppress an aggregate, which is what Baseline does — the split exists so the REASON can be reported, and that reporting lands with the reader that renders it. Original entry follows. ~~`ScoreSum`'s `unrecoverable` count conflates two different things: a Case whose score was purged before the column existed, and a Case written by an **older binary** that did not know the column.~~~~ | Phase-3 review of M2-1. `migrate` refuses to open a database from a newer build, but an older binary has no such check and writes rows with a NULL `score_value`. In a mixed-binary team the aggregate quietly drops those Cases and attributes it to a purge nobody ran. Pre-1.0 with a Homebrew tap, that mix is realistic. | When a second `kno` version is in circulation — i.e. at the first tagged release. Fix by recording a per-row schema marker, or by splitting the count. **Re-dated 2026-08-25**: `schemaVersion` is now 2, so a build that understands only version 1 refuses to open any database this one has written. The mixed-binary window this entry describes is no longer hypothetical — it is one `brew upgrade` and one `--resume` apart — and every future migration widens it. Trigger stands at the first tagged release, which is when a second version is genuinely in circulation. | @devarispbrown |
| <a id="32"></a>32 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: the bound is unchanged and is now surfaced from three stages rather than one (`core/baseline_record.go:604`, `core/value_loop.go:763`, `core/validate_loop.go:347`), and no nightly live run has reported settled cost exceeding its reservation. 1.0 has not been cut. Original entry follows. A cost cap is a **soft bound**: `C + N x d_max`, where `d` is per-call under-estimation and `N` is concurrency. Settlement cannot enforce it — by then the money is spent. | Phase-1 pass three, H-4. `fitsLocked` already refuses everything once spend reaches the cap, so a check inside `Settle` would add no enforcement; making `Settle` fail would turn a successful, paid, scored call into an errored Case and lose paid work. `Guard.Overshoot()` (M2-2) makes the excess **computable**, but nothing SURFACES it until M2-10 emits `SettlementOvershoot`. | Before 1.0, or when nightly live tests show settled cost exceeding its reservation. Checkable without writing code once M2-10 lands. **Surfaced in M2-10d** (2026-08-24): `SettlementOvershoot` is emitted at settle time, gated on the delta `Reservation.Settle` now returns. Once the cap binds, `fitsLocked` refuses every further authorization, so only reservations already in flight can overshoot — the event count is bounded by concurrency rather than by Case count, which is the `C + N x delta_max` bound this entry writes down. The bound itself is unchanged; it is now visible rather than merely computable. | @devarispbrown |
| <a id="33"></a>33 | **CARRIED, trigger cell corrected** (2026-09-01, v0.2 ledger audit) — the 2026-08-29 re-date added a "before 1.0" leg and announced it in this column only; the trigger cell still held the inbound-report-only text the re-date was written to replace, and the trigger cell is the one the release gate reads. Moved. Unfired on either reading: `feasibleHeadroomFraction = 0.25` still stands (`core/baseline_budget.go:308`), no user has reported a run stopping below its cap, no live data contradicts `f`, and 1.0 has not been cut. Original entry follows. **RE-DATED** (2026-08-29, v0.1 ledger review): the trigger was inbound-report-shaped — it cannot lapse and CI can never fire on it, the shape #46's review explicitly ruled out. A date leg is added: **"before 1.0, or the first user report, whichever is first"**. Original entry follows. **Headroom forfeiture**: a pessimistic reservation holds `N x pessimistic` of the cap un-spendable at the boundary. Measured on a $5.00 cap at 4.7% (concurrency 8) and 19.5% (concurrency 32). | Phase-1 pass three. Mitigated by the feasibility check, **which landed in M2-6** at `f = 0.25` — and note it plans against `Estimator.WorstCase` where an adapter supplies one, since planning against a scalar the run will not reserve answers a different question: it reduces concurrency rather than letting the guard deny its way to a halt, and refuses outright when not one Case fits. Measured before it: a 32k output ceiling against a $1.00 cap at concurrency 8 denied the FOURTH Case with $0.00 spent. It caps the forfeiture rather than removing it. Pinned by `stats/budget/throughput_test.go` as a bound, not a count. | **Before 1.0, or the first user report of a run stopping below its cap, or live data showing `f = 0.25` is wrong — whichever is first.** (Re-dated 2026-08-29; moved into this cell 2026-09-01, where the release gate can read it.) | @devarispbrown |
| <a id="34"></a>34 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `kno serve` has not landed — `api/` is `doc.go` only, so concurrent runs are still the exception. This trigger names v0.3 and the release gate now reads a minor series, so a 0.3.0 tag will refuse this row without a disposition. Original entry follows. Cross-process rate-limit coordination does not exist. Two concurrent `kno` runs against one provider can collectively exceed its limits. | The limiter is per-host per-process. Coordinating across processes needs shared state this tool deliberately does not have. | When `kno serve` lands (v0.3), which makes concurrent runs the normal case rather than the exception. | @devarispbrown |
| <a id="35"></a>35 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no SSE or stream parser exists in any adapter or in `internal/transport`, every adapter sets `Stream: false` explicitly rather than leaving the capability dangling, no stage needs incremental output, and 1.0 has not been cut. Original entry follows. Streaming is unimplemented. `Capabilities.stream` exists in the schema; M2 makes non-streaming requests only. | Phase-1 pass two, L5. An SSE frame parser is a new parser and would roughly double the transport PR — and would need its own fuzz target per entry 4. | **Before 1.0, or when any stage needs incremental output, whichever is first.** That PR adds the parser **and** its fuzz target. The date leg is new (2026-08-31): the condition is one we control and could avoid indefinitely, which is not a trigger. | @devarispbrown |
| <a id="36"></a>36 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `countTokens` is still bytes over a constant (`adapters/agent/pricing/estimate.go:212-238`, `bytesPerToken = 2.0`), and the nightly comparison the entry describes is in place and has reported no under-estimate (`adapters/agent/openaicompat/live_test.go:114,166`). Original entry follows. Token counting is approximate: **bytes divided by a constant**, not a BPE approximation and not a vendored tokenizer. Measured against tiktoken, the ratio ranges from 1.47 bytes/token (base64) to 3.6 (English prose), and the divisor is set to cover the low end. | Phase-1 pass one, Alternative C. Bounded by pessimistic reservation and reconciled at settlement, but a systematically mis-counting model skews every reservation until enough Cases settle. | When nightly live tests report **any** call whose input count we under-estimated, or a p95 over-estimate above 5x. A MEDIAN trigger — which this entry carried until Phase-3 of M2-5 — is structurally incapable of firing on this failure: the median over mixed traffic is safely high while the tail is what breaks a cap, and "a bound that can be too low is not a bound" is a statement about the tail. M2-7 adds the comparison to the nightly job. **Comparison added in M2-7** (2026-08-22): `spendOne` in `adapters/agent/openaicompat/live_test.go` compares the reservation's input term against the provider's reported `prompt_tokens` on every live call, failing on an **under**-estimate — the direction that breaks a cap — and logging the over-estimate ratio otherwise. Over-estimation is reported rather than failed: one Case is a sample, and a nightly job that reddens on a single long prompt gets muted. | @devarispbrown |
| <a id="37"></a>37 | **RE-DATED** (2026-08-29, v0.1 ledger review): the open export half's trigger field still names the satisfied M2-11 leg. The export half is re-dated to **"the v0.3 OTel export PR (DESIGN places export at v0.3)"**. Original entry follows. **PARTLY REPAID in M2-11c** (2026-08-25) — INSTRUMENTATION only; OTLP export is still owed and this entry stays open. Every stage, Case, and adapter call is now a span correlated by run ID, via the OTel API, which is a no-op until something registers a provider — so the instrumentation is unconditional and costs nothing on the default path. `--trace-spans` writes them to stderr for local debugging. `CLAUDE.md` and `DESIGN.md:399` turn out NOT to conflict once instrumentation and export are separated, which is how they are worded: "tracing is built in" and "OTel export at v0.3". The M2-11 plan claimed a conflict and proposed resolving it in `CLAUDE.md`'s favor by editing `DESIGN.md`; that was wrong. `CLAUDE.md` DID need one correction, caught in Phase-3 review: it named `--otel-endpoint` as the flag that turns tracing on, and this ships `--trace-spans` with different semantics — so it documented a flag that does not exist and omitted the one that does. **The plan's dependency argument was also wrong and was measured rather than repeated**: it claimed `otlptracehttp` "avoids grpc entirely and is the cheaper design". Both OTLP exporters cost 21 modules and both pull `google.golang.org/grpc` transitively via `go.opentelemetry.io/proto/otlp`. The API plus SDK plus a stdout exporter costs 11 and pulls no grpc, which is what shipped. Spans carry IDs, counts, and money only — never a prompt, an answer, or a system prompt — enforced by `observe`'s attribute constructors and by a test that drives a real run, with an agent whose ERRORS quote the Case, and scans every attribute, event, and status description on every span. Original entry follows. ~~Adapter calls emit no OpenTelemetry spans. `CLAUDE.md` says tracing is built in — "every stage, every adapter call ... is a span with run ID correlation" — and M2-3 introduces the first adapter call without one.~~ | Deferred deliberately, not dropped. OTel is a new dependency tree on a PR that is already the milestone's security boundary, and `DESIGN.md:399` places "OTel export" at v0.3, so the two documents disagree about when this is due. Splitting it keeps the transport diff reviewable as a security change. | **Re-dated to M2-11** (2026-08-22), the CLI-wiring PR. The original trigger was "before M2-7 merges", and M2-7 is the PR that trips it — so this is a disposition, not a silent carryover. The reason: there is no `--otel-endpoint` flag and no OTel dependency in `go.mod`, so spans added now would be permanently unreachable dead code with no way to turn them on, which is exactly the trap [#42](#42) records. M2-11 builds the flag surface, and that PR adds the spans together with the test asserting no Case or Response content reaches one. `DESIGN.md:399` placing OTel export at v0.3 still disagrees with `CLAUDE.md`; M2-11 resolves which document is right. | @devarispbrown |
| <a id="38"></a>38 | `transport.ErrTransient` is unreachable from `core`, so the remedy it exists for is inert. The M2 plan §2.1 specified `errs.ErrTransportTransient` in `core/errs` precisely because `core` retries only `ErrRateLimited` and a stale pooled connection would otherwise mark a healthy baseline unusable. The sentinel shipped inside `adapters/agent/internal/transport`, which `core` cannot import — nor should it, under prime directive 3. | Phase-3 review of M2-3. The transport correctly *classifies*; nothing yet *translates*. Landing the `core/errs` sentinel in the transport PR would have meant `core` changes in a diff that is the milestone's security boundary. | **REPAID in M2-6** (2026-08-22): `errs.ErrTransportTransient` exists and `core.retryable` retries it alongside `ErrRateLimited`. The adapter half — mapping `transport.ErrTransient` onto it — lands with the adapter in M2-7, which is the first PR that has a transport to map from. **Adapter half repaid in M2-7**: `openaicompat.classify` maps `transport.ErrTransient` onto `errs.ErrTransportTransient`, and `errorFor` maps 5xx and 408 onto it too. Context cancellation passes through unchanged, so a shutting-down run is not reclassified as a provider failure. | @devarispbrown |
| <a id="39"></a>39 | The transport returns a 429 as an ordinary `*Response` with status 429, where the plan §2.1 specified it return `errs.ErrRateLimited` carrying the parsed `Retry-After`. | Phase-3 review of M2-3. Same boundary as entry 38: the error type lives in `core/errs`. The limiter already closes the host, so the *pacing* works; what is missing is `core` seeing a retryable classification rather than an adapter interpreting a status code. | **Partly repaid in M2-6** (2026-08-22): `core.retryAfterOf` reads a provider-requested wait off the error and uses it in place of the doubling backoff, so the pacing is the provider's rather than ours. The mapping from a 429 `*Response` to `errs.ErrRateLimited` is the adapter's, and lands in M2-7. **REPAID in M2-7** (2026-08-22). `openaicompat.errorFor` maps a 429 onto `errs.ErrRateLimited` wrapped in a `rateLimitedError` carrying the transport's parsed, clamped `Retry-After` behind the `RetryAfter() time.Duration` method `core.retryAfterOf` reads. It **wraps** the `Actionable` rather than embedding it: with embedding, `Unwrap` is promoted and returns the Actionable's own cause, so `errors.As(err, **errs.Actionable)` skips the sentinel and `errs.ExitCodeOf` reports an unclassified failure. Verified empirically during Phase 3 by rewriting it to embed — the mutation fails the suite. | @devarispbrown |
| <a id="40"></a>40 | **REPAID** (2026-08-28). The drift detector landed ([plan](plans/2026-08-22-pricing-drift-detector.md)): `internal/cmd/pricingcheck` runs six checks — check 0 gates the table's **age** against the 90-day leg of this entry's trigger (calendar-day arithmetic, future-dated versions refused), checks 2–3 gate Anthropic table selection by a committed header literal and the providers' published cache ratios, checks 1 and 4 report cross-source disagreement and discovery using `pricing.Lookup` itself as the covered-predicate, and check 5 gates prefix-colliding variants except the 16 owed rows of [#46](#46), which it reports every run under a `pending` disposition — the detector is the promised input for that entry, and the pending list is discovered live, not guessed. `.github/workflows/pricing-check.yml` runs weekly (plus `workflow_dispatch` and a push trigger on the pricing surface) with `contents: read` and job-scoped `issues: write`, SHA-pinned actions per [#14](#14), and a set-based issue lifecycle: intersect updates, disjoint closes with a verification comment, at most one create per run — so no re-open storm, no orphaned issues, and the evergreen #46 issue closes itself the week the rows land. Verified: a live run on 2026-08-28 passed all gated checks and reported exactly the 16 pending variants; broken fixtures (batch table, shifted columns, duplicated table, wrong envelope) each drive the corresponding gate, and every check was verified failing before it was written. Fixtures are real captures with provenance (URL, date, sha256 of the untrimmed capture) in `note.txt`. Accepted risks the plan records rather than this ledger: OpenAI's page restructure fails soft where Anthropic's gates (nothing deterministic to pin), a fetch blip files an issue the next healthy run closes, and report values round to cents so the hand-verify step is the only trusted source. Original entry follows. ~~The pricing table is **static and dated**, so it goes stale between releases. Prices as of 2026-08-21 for `anthropic` and `openai` only; every other provider is unpriced. Within those two, `claude-mythos-5` is omitted deliberately (invitation-only). A **version** suffix resolves by longest prefix to its base row, so pinning a version works — corrected 2026-08-22: the fallback used to accept any suffix, which priced `claude-opus-5-fast` as base ([#46](#46)).~~ | Fetching prices at runtime is rejected outright (M2 plan, Alternative D): an endpoint that is down leaves the engine choosing between refusing to run and running with no ceiling, and one that is wrong is a spend path with no ceiling at all. Staleness is the price of that, and it is the cheaper failure — a stale price is visible, dated, and overridable, and settlement reconciles against the provider's own reported usage anyway. | **Re-dated 2026-08-27, at the 0.0.1 close-out, and this is a disposition rather than a carryover.** The entry is about a table going stale *between releases*, and 0.0.1 is the first — there is no interval yet for drift to accumulate across. The table is dated 2026-08-21, six days before the tag. Three shipped mitigations bound the exposure meanwhile, and all three are the safe direction: an unpriced model is **refused** under a cost cap rather than guessed at, `--price-input-per-mtok`/`--price-output-per-mtok` let a user state rates the table lacks, and `kno doctor` prints the table's date so staleness is visible rather than assumed. **New trigger: before 0.1.0, or when the committed table is 90 days old, whichever is first** — both can lapse, which is what the ledger rules require, and the second fires on its own without anyone deciding to look. **Before the first tagged release.** Scope settled by Phase-1 review (2026-08-22): a **detector**, not a generator — see [the plan](plans/2026-08-22-pricing-drift-detector.md). It fetches, compares against the committed table, and files an issue; a human edits thirteen rows by hand, which is what "reviewed like code" already means. The PR-opening generator was rejected: its proposed gate (fail on >50% movement) sat exactly on the amplitude of the modal parse error, since the batch-pricing table is precisely half base on every row, and generating `table.go` would destroy the hand-written rationale including the only in-code pointer to [#41](#41). Cross-source disagreement replaces the invented threshold. Deferred to after M2-11. OpenRouter exposes `.data[].pricing.{prompt,completion}` at `/api/v1/models` in USD per token; Anthropic and OpenAI are HTML pages and need scraping or manual entry. | @devarispbrown |
| <a id="41"></a>41 | **Clause (d) REPAID (2026-08-29)** — the adapters that first used it landed ([plan](plans/2026-08-29-bedrock-vertex-agents.md)): the Bedrock and Vertex agents price their regional endpoints, so the 10% is charged, not skipped. `pricing.RegionalMultiplierPct` returns 110 for partner-cloud models and 100 otherwise; Estimate, WorstCase, and Settle all apply it via `regional(costOf(p, u))` before the budget guard settles, and cross-region `us.`/`eu.`-prefixed inference profiles are refused until priced, so no unmodeled premium can slip through a guessed region class. The check half of the repayment is pricingcheck check 6: AWS's machine-readable Bedrock price list is fetched every run and the spread between us and eu region prices must be the committed 110% within a 1% rounding band, or the gate fails; vertex has no machine-readable source, which is reported on every run as the standing obligation. Original entry follows. **Pricing modifiers the estimate does not model.** (a) `Price` carries ONE cache-write rate; Anthropic publishes **two** — 5-minute at 1.25x base input and 1-hour at 2x — and the table records the 5-minute one. (b) Fast mode prices Opus 5 and Opus 4.8 at $10 in / $50 out, a **2x** under-reservation if an adapter sets `speed: "fast"`. (c) `inference_geo: "us"` applies 1.1x to every category on 4.6+ models. (d) Bedrock and Vertex regional endpoints add 10%. (e) Tool use adds 286-804 input tokens per request plus $10/1000 web searches, with no term in the estimate at all. | Phase-3 of M2-5. Harmless today because M2's adapters never set `cache_control`, so no cache write is ever billed. It becomes an under-charge — the direction prime directive 4 calls a P0 — the moment an adapter enables 1-hour caching, since settlement would apply the cheaper rate. | **When an adapter first uses any of them.** Each is inert until then: M2's adapters set no `cache_control`, no `speed`, no `inference_geo`, no tools, and reach no partner cloud. The PR that enables one prices it — for (a), either a second `Price` field (additive, so `buf breaking` permits it) or the 1-hour rate with 5-minute writes documented as over-charged. M2-8 makes clause (a)'s premise **asserted rather than assumed**: `TestNoCacheControlIsEverSent` drives a Case with a system prompt and multi-turn history and fails if the string `cache_control` appears anywhere in the request body — inert by construction, not by convention. A cache **read** can still happen unrequested (the provider caches on its own) and M2-8 settles `cache_read_input_tokens` at the cached rate, so only the **write**-rate ambiguity remains unreachable. Clauses (b)-(e) untouched. | @devarispbrown |
| <a id="42"></a>42 | **REPAID in M2-11a** (2026-08-25): the check moved from run-open to first-response time. `BaselineOptions.ResolvedModel` is REMOVED — it was caller-supplied and read at `openRun`, before any request, so the only value it could ever hold was one a previous run had recorded and `checkResumable` compared it to itself. It never fired once. `modelGate` now sees the model in a `Response`, under `sync.Once` because with concurrency there is no "first response" and the answer cannot differ between workers; state lives in `Baseline` rather than on `BaselineOptions`, whose value receivers would have copied a `sync.Once` per call. First-response placement also catches an alias re-pointing DURING a long run, which the old placement could not. Driven end to end by `TestAnAliasThatRepointsPartWayThroughAResumeIsCaught`: a resume that STARTS on the recorded model and is re-pointed several Cases in, which is the test this entry's trigger demanded and was not writable before. The first version of that test was a stub — it ran one baseline and asserted a model was recorded, while its name and docstring claimed a mid-run re-point — and it would have passed against a gate that never fired; Phase-3 review caught it. The gate is also STATELESS: memoizing the verdict under a `sync.Once` made it blind to exactly this case, because a first response that MATCHED spent the Once and every later re-point returned nil. The Case that trips the gate is SCORED and recorded, not discarded: it is paid for, and failing it would have filed a scoreable answer as an agent error. Removing an exported field from `core` is a public-Go-API break, permitted pre-1.0 with a CHANGELOG migration note. **[#55](#55) remains open**: up to `concurrency-1` further Cases land under the new model before the run ends, and nothing marks the Run unusable to a downstream stage. Original entry follows. ~~The resolved-model resume check is **dead in production**. `checkResumable` compares `Run.case_execution.resolved_models`, and nothing writes `CaseExecution` outside tests — M2-10 is the PR that populates it.~~ | Landed ahead of its data on purpose: M2-7 introduces the first adapter that can report a resolved model, and the check has to exist before the adapter does or the first run with a moving alias is the one that discovers the gap. Tested against a hand-populated `Run`, which is honest about what it proves — the wiring is untested because there is no wiring. | **M2-10**, which populates `CaseExecution`. That PR must add an end-to-end test driving the check through a real run rather than a synthesized `Run` record. **Recording half landed 2026-08-25**, and the entry stays open. `closeRun` writes `resolved_models`, so the RECORDED operand exists for the first time — and two defects that would have made the check unsafe are fixed with it: the set excludes the empty string every errored Case contributes, and the comparison is membership rather than `models[0]`, which would have refused a run that saw `{A, B}` and is now served by `B`. **The check still does not run.** Nothing sets `BaselineOptions.ResolvedModel`, and nothing can before the first response, which is this entry's actual content. The PR that landed the recording half claimed to arm the gate; that claim was wrong and is corrected here, in the CHANGELOG, and in the PR body. **Re-dated to M2-11** (2026-08-25) with the design changed, which is a disposition rather than a silent carryover: M2-10 is the PR that trips this trigger and it cannot repay it. The end-to-end test the old trigger demanded is not writable at all, because `BaselineOptions.ResolvedModel` is a *caller-supplied* field read at `openRun`, before any request is made — so the only value it can ever hold in-process is one a previous run recorded, and the caller reading it back and passing it in would be the check comparing a value to itself. The gate has to move from run-open to **first-response time**: the first Case whose response reports a model compares it against the recorded set and fails the run before the second Case is dispatched, which is also the only placement that catches an alias that re-points *during* a long run rather than only between two. That is a `core` change with an event and an exit code, so it goes with M2-11 alongside entry [#47](#47), which needs the same run-fatal escalation machinery. The end-to-end test lands with it and drives a real run through the fake adapter reporting a changed model mid-run. | @devarispbrown |
| <a id="43"></a>43 | **CARRIED — and checked hard, because this entry has already fired twice** (2026-09-01, v0.2 ledger audit). It did not fire a third time. The only commit touching `adapters/agent/` or `internal/transport/` since the 2026-08-31 re-date is e13b558 (#169, validate), which added `Capabilities.ContextSetInject` and a `context.go` to four adapters and touched no `errors.go` and no error path. The debt is intact and unrepaid: `BilledCostUSDMicros` still exists only in `adapters/agent/openaicompat/errors.go:362`, and bedrock, vertex and anthropic still settle a failed attempt at zero. Original entry follows. **RE-DATED AGAIN** (2026-08-31, v0.2 ledger audit) — **the second consecutive silent carryover on this entry, which is the failure the ledger exists to prevent.** The re-dated trigger ('the next `internal/transport` PR after v0.1 that changes request/response handling') fired hours later: #128 gave `transport/client.go` an `Options.Authorize` hook and RawPath request rewriting. It went unreported because this row said 'Re-dated', and the release gate counted that word as a disposition — fixed in the same PR as this audit. `BilledCostUSDMicros` still exists only in `openaicompat`; bedrock and vertex settle a failed attempt at zero. Original entry follows. **RE-DATED** (2026-08-29, v0.1 ledger review): the transport-half trigger ("the next `internal/transport` PR") fired with #50 and was not disposed — recorded as the lapse it is. The core half is repaid; the transport sentinel half is re-dated to **"the next `internal/transport` PR after v0.1 that changes request/response handling"**, which names the actual obligation instead of every file touch. Original entry follows. A failed attempt settles `Spend{Calls: 1}` with **zero cost**. A provider that bills a request which then errors is invisible to both the guard and the store. | Phase-3 of M2-6. Pre-existing, but the retry rewrite multiplies the exposure by `MaxAttempts`: a Case that exhausts three attempts against a provider billing failed requests spends three times what is recorded. The call cap still counts them, so the exposure is bounded by `--max-calls` rather than unbounded. | **Before 1.0, or the next PR that adds or changes an agent adapter's error path, whichever is first.** Named that way because 'the next transport PR' has now fired twice without being paid: the obligation is the sentinel, not the file. That PR adds `ErrPartialResponse` (or equivalent) and settles a billed-but-failed attempt in every adapter, not just the first one. | @devarispbrown |
| <a id="44"></a>44 | **FULLY REPAID** (2026-08-29): the prompt half landed with the init/consent PR (#102, plan `2026-08-29-init-config-consent.md`) — `budget.Estimate.Width` (additive, plain struct) is populated on the run-open path and the consent quote prints "width 32 → 5 (headroom)" when the engine narrows the width. The TUI-milestone re-trigger below is superseded by the repayment it was waiting for. **REPAID in M2-11b** (2026-08-25) — the REPORT half; the PROMPT half stays open and is re-triggered below. The human report prints a `width` line and `--json` carries `concurrency`, `concurrency_requested`, and `concurrency_reduced_reason` (a NAME, not the enum number, so a jq pipeline does not break when a value is inserted). Gated on the REASON rather than on `requested != effective`: `requested` is presence-carrying and absent means "no particular width", so the first version printed "width 8 (asked for 0; unspecified)" on every ordinary run. The `--json` fields are hand-written rather than an embedded `ConcurrencyDecision`, per ADR-0001. **The prompt half is NOT repaid**, and this entry's own text names it as the natural place: `budget.ConfirmFunc` is `func(ctx, Estimate, Remaining)` and has no channel for a `ConcurrencyDecision`, so putting it there means changing a signature in `stats/budget` — the prime-directive-4 package, with its own coverage floor and property tests — and breaking all three CLI closures and any API caller. **Re-triggered to the TUI milestone**, which owns the prompt and is already carrying [#59](#59) for the same reason. Original entry follows. ~~`checkFeasible` reduces concurrency **silently** — a user's `--concurrency 32` can become 5 with no event, no log line, and no field on the `Run`.~~ | Phase-3 of M2-6. `CLAUDE.md` says new user-visible state is a new event type, never a side channel, and a 6x slowdown the user did not ask for is user-visible. The check does not take a `ctx` and runs before the event stream is open, so emitting is not a one-line change. | **M2-10**, which is the event-emission PR. Either a `ConcurrencyReduced` payload or a field on the `Run`, and the CLI reports it. **Partly repaid in M2-10b** (2026-08-24): schema only. `ConcurrencyDecision` records the width and why, `Run.concurrency` carries it, and a `ConcurrencyReduced` event carries the same message rather than a second copy of its fields. It holds both terms of the arithmetic — the headroom and the per-Case estimate — because the engine divides a fraction of the first by the second, and a consumer given only one can solve for what they were not told rather than check the result. `requested` is optional so a defaulted width is distinguishable from an asked-for one; telling a user their request was overridden when they made no request is how a report earns distrust. **Nothing emits or writes it** — the emitter is M2-10c and the CLI report this entry requires is still outstanding. **Engine half repaid in M2-10c** (2026-08-24): `checkFeasible` records a `ConcurrencyDecision` for every Case-executing run, and emits `ConcurrencyReduced` when it actually narrowed the width. **The CLI report this entry requires is still outstanding** — nothing in `cli/`, `tui/`, or `api/` reads the field back, so a user whose `--concurrency 32` becomes 5 still sees nothing. The state moved from "no record" to "a record no surface reads". Note also that `checkFeasible` runs BEFORE `confirmRun`, so the consent prompt quotes a run whose width has already been cut and does not say so; the prompt is the natural place for the report. **M2-11.** | @devarispbrown |
| <a id="45"></a>45 | **REPAID** (2026-08-28) with the LangSmith Evals adapter ([plan](plans/2026-08-28-langsmith-evals-adapter.md)), on the trigger. The invariant the entry names — the Evals adapter dedups Case IDs at load — is now a **core property**, not a per-adapter convention: `coretest.CheckEvalsDuplicateIDs` fails any Evals source that yields a duplicate Case ID (verified failing against a deliberately-duplicating producer, per the #16 gate-honesty rule), and both adapters pass it. The langsmith adapter makes an in-run duplicate **fatal, naming the example id** — jsonl-identical semantics, because an in-run duplicate from LangSmith is the same example seen twice across a pagination seam of a concurrently-edited dataset, a source defect to fix rather than a count to paper over. The first draft's alternative — `RecordOutcome` reporting whether it inserted — was rejected in Phase-1 review of the plan: a dropped duplicate discards its settled spend from the durable record, so a resumed run's restored cap is inflated by exactly that amount (prime directive 4), the identical hole exists one table over in Value over `RecordMeasurement`, and the premise (distinct examples colliding on an id) cannot occur when the id IS the row's primary key. The rejection is recorded in the plan so the next reader sees why the `store.Store` interface was deliberately NOT changed. Original entry follows. ~~`agg.add` records a score even when `RecordOutcome`'s `INSERT OR IGNORE` dropped the row as a duplicate, so the in-memory count can exceed the persisted one.~~ | Phase-3 of M2-9. Previously the drift stayed inside one process; now the in-memory count and the store-derived prior are mixed into one mean, so the two disagree in the numerator and denominator of the same number. Unreachable today: the only Evals adapter (`adapters/evals/jsonl`) dedups Case IDs at load, and `core` has no other source of Cases. Fixing it means `RecordOutcome` reporting whether it inserted, which is a `store.Store` interface change for a path nothing can currently take. | **When a second Evals adapter lands**, or when any non-jsonl source of Cases reaches `core` — whichever is first. That adapter's PR must either dedup in `core` or make the insert report itself. | @devarispbrown |
| <a id="46"></a>46 | **REPAID** (2026-08-28), on the trigger, with a disposition the user chose: **fast rows land, everything else becomes a deliberate exclusion with a written reason.** The fast variants — `claude-opus-5-fast`, `claude-opus-4-8-fast` at $10/$50 per MTok, read off Anthropic's own page — are table rows now, `pricing.Version` bumped to 2026-08-28, so a capped run on fast mode is authorized at the published rate instead of refused. The batch variants (10 models, published at 50% of base) are deliberate exclusions: no adapter speaks the batch endpoint, so a row would authorize a run the Messages endpoint rejects per Case — strictly worse than the one-time refusal naming pricing. The three `gpt-5.6-*-pro` variants and `claude-opus-4-7-fast` are deliberate exclusions because NO price-of-record page publishes them (OpenRouter-only, and this entry's own standard refuses a non-price-of-record source for a spend-authorizing row). The detector keeps every exclusion honest: the dead-exclusion check fails the run the day any of these gains a row or a price-of-record, and its anthropic parser now selects the fast table by its own header literal, so the two fast rows are watched by the agreement check against OpenRouter rather than single-sourced. The pending disposition and its every-run reporting are removed — the evergreen issue the detector filed for this entry closes itself on the first post-merge run. The disposal split was an explicit user decision (fast only + exclusions), recorded here because the alternative — pricing batch and pro variants from unpublished or non-price-of-record rates — was the failure mode this entry exists to prevent. Original entry follows. ~~**PARTLY REPAID in M2-11a** (2026-08-25) — the REFUSAL SHAPE only; the price rows are still owed and this entry stays open on its original trigger. Both adapters mark a model-level pricing miss run-fatal, so a capped run against an unpriced variant is refused ONCE, naming pricing, instead of erroring every Case and ending as "too many cases errored" — a verdict naming nothing about pricing, after taking the user's consent for a figure that was never going to apply. Marked in the ADAPTER rather than keyed on `core`'s `errUnpriceable`, which covers three causes and cannot tell a model-level miss from an `Estimator` that refuses one Case and prices the rest; `TestEstimatorFailureRefusesWhenACostCapIsSet` exists to keep the second from aborting a run and was not weakened. The per-token override (`--price-input-per-mtok`/`--price-output-per-mtok` through `pricing.EstimateWithPrice`) and the price rows themselves land in M2-11b. Original entry follows. ~~Provider **variants** that carry their own price — fast mode, pro tiers — have no rows, so they are unpriced and refused under a cost cap rather than run.~~ | Phase-1 review of the pricing-worker plan ([plan](plans/2026-08-22-version-suffix-pricing.md)), which fetched the live model list and found `anthropic/claude-opus-5-fast` in it. Before that review `Lookup`'s longest-prefix fallback resolved it to `claude-opus-5` and authorized the run at the base rate; the provider's own page prices fast mode well above base. The fallback now accepts only a **version** suffix — versions are numbers, variants are words — which makes the variant unpriced — correct, and strictly better than a confidently wrong ceiling, but a user on fast mode with a cap now cannot run at all. Adding rows needs the published rates read from the provider's page by a human, which is deliberately not something a review summary is trusted for. A capped run on a variant also takes the worse of two paths today: it quotes a figure from the run-scoped scalar, takes the user's consent for it, then errors every Case and ends as "too many cases errored" — a verdict naming nothing about pricing. Refusing once, up front, needs a model-level sentinel an adapter sets and `core` reads; adding it before an adapter merges would be dead code, so it lands with **M2-11**. | **Re-dated 2026-08-27, at the 0.0.1 close-out.** The refusal shape shipped in M2-11a is what makes deferring the rows defensible: a capped run against an unpriced variant is refused **once**, before any Case, naming pricing — so the failure mode is a run that does not start, not a run billed at a rate nobody checked. Adding the rows needs published per-variant rates read off the provider's page by a person; a review summary is deliberately not trusted to supply numbers that authorize spending, and neither is a model reciting them from memory. **New trigger: before 0.1.0.** It stays on a date rather than an inbound report, for the reason the original entry gives — a report-conditioned trigger can never lapse, so CI's release check could never fire on it. **Before the first tagged release**, matching [#40](#40). A trigger conditioned on an inbound user report was rejected on review: it can never lapse, so CI's release check could never fire on it, which is the silent carryover the ledger rules forbid. The pricing detector ([#40](#40)) now reports the owed variants every run under a pending disposition — 16 as of 2026-08-28, discovered live, listed in `internal/cmd/pricingcheck/lists.go` — and is the natural input, but the date is what forces the disposition. | @devarispbrown |
| <a id="47"></a>47 | **REPAID in M2-11a** (2026-08-25): six conditions that cannot change within a run now end it at the first Case instead of once per Case — a rejected credential (401/403), the provider's own spend cap (`enforced_spend_limit_reached`), a user's self-set spend limit, an unpaid account (402), a model that does not exist (404), and a refused destination or key-binding mismatch. The adapter classifies and `core` escalates, through an anonymous `interface{ RunFatal() bool }` assertion beside `retryAfterOf` and `billedCostOf`: the adapter does not own the run and `core` never saw the status code. A sentinel in `core/errs` was designed first and rejected — `Actionable` has one `wrapped` slot and `Wrap` replaces it, so wrapping outward reproduces [#39](#39) verbatim and wrapping inward doubles the fix line with nowhere for the provider's cause. Marked PER INSTANCE, never on a sentinel, because `Actionable.Is` compares by `Code` and the qualifying errors share `ErrProvider`'s code with a dozen that do not; a mutation escalating the whole sentinel fails the over-escalation test. A plain 429, a 5xx, a truncation, and a response past the size ceiling are deliberately NOT escalated, and that direction has its own test because it is the more expensive one to get wrong. Original entry follows. ~~A **run-fatal** provider failure is reported once per Case rather than once per run.~~ | Phase-3 review of M2-8. `core.IsFatal` treats only `ErrBudgetExceeded` as fatal, so three conditions that cannot change within a run — a spend-cap 429 (`enforced_spend_limit_reached`), a 401/403 rejected credential, and a refused cross-host redirect or key-binding mismatch — are classified per-Case. A wrong `ANTHROPIC_API_KEY` on a 10,000-Case run makes 10,000 requests and settles 10,000 calls against `--max-calls` before the user is told anything, which is precisely what `anthropic.ErrAuthentication`'s own godoc claims it prevents. The adapter cannot fix it: the classification is correct and the escalation belongs to `core`. | **M2-11**, which owns the CLI flag surface and the `errs` fix lines and is therefore the PR that must make those messages true — or earlier if a nightly live run reports a full-run error sweep from a single credential fault. | @devarispbrown |
| <a id="48"></a>48 | `Guard.Settle` adds `Spend.CostUSDMicros` **unchecked**, so a saturated `MaxInt64` from any adapter drives `spent` negative. | Phase-3 review of M2-8, measured against the real guard: two such settlements against a $1.00 cap leave `spent = -2`, `Remaining` reporting more than the cap, and the guard authorizing again. `Guard.Spent()` — the number a report shows and the number `Guard.Restore` re-reads on resume — goes negative. Both M2 adapters now close their own path by refusing any usage dimension above 10M tokens before summing, so the value never reaches the guard from them; the guard itself is unhardened and every future adapter re-opens the hole. Fixed at the adapter rather than in `budget` because M2-8 may not edit `stats/`, and because refusing implausible input is right regardless of what the guard does. | **REPAID in M2-10d** (2026-08-24), in two places rather than one. `Reservation.Settle` clamps every dimension through `addSpend`: a negative reported charge is refused rather than subtracted — a settlement that CREDITS the run is not a settlement — and a saturating one pins at `MaxInt64` rather than wrapping. Fixed at the choke point rather than at each call site, because both M2 adapters bound their own usage blocks and every future one would have to remember to. `TestSettleClampsWhatAnAdapterReports` reproduces the entry's own figure against the unclamped code: `spent = -2` for two `MaxInt64` settlements against a $1.00 cap, with `Remaining` then reporting more than the cap. M2-10d also opened the third path into `Settle` — the error object rather than the response — which the adapters' own 10M-token refusal does not cover, so closing this here rather than at its "third adapter" trigger was the right order. Clamping `Settle` alone MOVED the overflow rather than removing it: with `spent` pinned at `MaxInt64`, `fitsLocked`'s three-term add wrapped to `MinInt64`, the comparison read as "well under the cap", and the guard authorized without limit — measured against a $1.00 cap, `Remaining()` reporting 0 while `Authorize()` returned nil. Phase 3 caught the false repayment. `fitsLocked` and `Restore` both saturate now; `Restore` matters because the store is not a trusted input, being written by another process or an older build. | @devarispbrown |
| <a id="49"></a>49 | A branch rebuilt onto `main` can drop a PR's documentation silently: M2-10a merged with **no CHANGELOG entry, no ledger repayment, and no plan file**, all of which existed and were reviewed before the rebuild. | Found while starting M2-10b. Recovering a rebase conflict by `git checkout -B branch main` and re-applying named source files drops everything not named — and the gates cannot catch it: `make docs` checks links, not presence, and the behaviour-change label is applied by a human. The ledger then asserted the opposite of the code, which is the failure the whole process exists to prevent. Restored in M2-10b. A second instance of the same class landed with M2-10b: the squash title says `ConcurrencyReduced and Run.scheduling`, but Phase 3 renamed those to `ConcurrencyDecision` and `Run.concurrency` and the PR title was never updated. Squash-merge takes the PR title, and release-please derives release notes from it, so the generated notes name a type that does not exist. The hand-maintained CHANGELOG is correct; `main` cannot be rewritten, so this one stands as a record. | **REPAID** (2026-08-26). `.github/workflows/changelog.yml` reads the Conventional Commit type out of the PR **title** — the string squash-merge makes the commit and release-please makes the notes, not the branch name — and fails the PR unless `CHANGELOG.md` is in the diff, exempting `refactor:`, `chore:`, `test:`, and `build:`. It fails closed twice: a title it cannot parse is an error rather than an exemption, and a file list the API will not return refuses to pass rather than looping over nothing and reporting green, which is the bug shape `dco.yml` already had to fix. The escape hatch is the **`no-changelog` label**, not a trailer or a phrase in the body, because an exemption has to be visible on the PR to be arguable — and `labeled`/`unlabeled` are in the trigger list so applying it re-runs the check rather than leaving the PR red until someone pushes an empty commit, which is how an escape hatch becomes a thing people route around. No action is used at all (the file list comes from `gh api` on the runner), so nothing here needs a SHA pin under [#14](#14). Verified with the gate's own script driven over fourteen cases: `feat:` without a CHANGELOG red and with it green, all four exempt types green, `ci:`/`docs:`/`fix!:` red, an unparseable title red, the label green, an unrelated label still red, and `docs/CHANGELOG.md.bak` not counting as a match. `actionlint` (which shellchecks the `run` block) is clean on it and on every existing workflow. The entry's second half lands where the entry says it belongs — the PR template's checklist, beside the CHANGELOG item, as a reviewer check that the title still describes the diff — not as CI. Original trigger: **before the first tagged release.** Either a CI check that a non-`refactor:`/`chore:` PR touches `CHANGELOG.md`, or a release-time assertion that every entry marked repaid in a merged PR names a commit that exists. The first is cheaper and catches it at the PR rather than at the tag. Extend the same check to assert the PR title still describes the diff when a type it names has been renamed — cheaper as a reviewer checklist item than as CI, and worth pairing with the CHANGELOG check rather than building separately. | @devarispbrown |
| <a id="50"></a>50 | A budget refusal on a retry **drops every charge the earlier attempts incurred**. `sinkFunc` skips persisting a Case whose error is `ErrBudgetExceeded`, which is right for one refused before any call and wrong for one whose first attempts were made, charged, and settled. Measured: guard $0.50, store $0.00. | Phase-3 review of M2-10d. **This PR created the exposure**: before it, a failed attempt settled `Spend{Calls: 1}` at zero cost, so the skip lost only a call count. Now it loses dollars, and `Guard.Restore` reads the store — so a resume gets them back as headroom. Not the SIGKILL window [#20](#20) describes: this is the graceful end of every capped run. Bounded by concurrency x (attempts-1) x per-call charge. Fixing it needs a spend-only durable record — writing the outcome row would mark the Case done and a resume would skip a Case that never got an answer, trading a money bug for a statistical one — which is a `store.Store` interface change and a design decision rather than a patch. Same shape for a Ctrl-C during backoff. | **REPAID** (2026-08-25). Orphan spend lands on `runs` columns via `Store.RecordOrphanSpend`, and `SettledSpend` sums both sources — so a resume restores what the first process actually settled. Both skip paths covered: the budget refusal this entry named, and the Ctrl-C-during-backoff path it also named and the first draft missed. The write predicate is "the guard settled something", not "the Case errored", so a Case refused before its first call records nothing rather than fabricating a call. Calls come from what was settled rather than from `Attempts`, which is incremented before `invokeOnce` and would have over-reported by one per refusal. Verified by kill/resume: guard 360000, store 320000 without the fix, equal with it. Phase 3 also found a **call**-accounting regression the fix introduced: making `settledSpend(nil)` return zero lost the guard's calls on the panic path, where the executor recovers and discards the outcome that carried them. Both `invokeWithRetry` and `workFunc` now preserve the accumulated spend across a panic, so a Case that made two charged attempts and then panicked reports them rather than zero. Attribution of the charge to a Case is a separate gap — [#52](#52). | @devarispbrown |
| <a id="51"></a>51 | `SettlementOvershoot` cannot report its own contribution, and `RETRY_REASON_TRANSPORT_TRANSIENT` is imprecise for a **billed** 5xx. | Phase-3 review of M2-10d. The payload carries reserved, settled, and the cumulative; the per-event delta `Reservation.Settle` now returns is used to GATE the emit and then discarded. Subtracting reserved from settled over-counts by the pre-cap headroom — 450k where the contribution is 300k — so a consumer summing across events inflates the overshoot. Separately, the proto defines TRANSPORT_TRANSIENT as "no evidence the provider processed the request" and `openaicompat` attaches a charge to a retryable 5xx, which is evidence it did: the one retry reason that costs money is reported as the one that means nothing happened. `PROVIDER_UNAVAILABLE` and `TIMEOUT` are defined and never emitted. | **REPAID** (2026-08-25). `SettlementOvershoot` carries `delta_usd_micros`, the figure `Reservation.Settle` already computes — reproduced against the derived form: 450k where the true contribution is 300k, because subtracting reserved from settled counts the headroom that was still under the cap. The retry half needed no new enum value: `RETRY_REASON_PROVIDER_UNAVAILABLE` already means "the provider returned a 5xx", and the discriminator core was missing is the **charge** — an adapter wraps both a reset connection and a billed 5xx as `ErrTransportTransient`, so the sentinel cannot separate them but a reported cost can. `TIMEOUT` remains unemitted: `core` sees a timeout as `ErrTransportTransient` too and has no signal that distinguishes it, which would need the adapter to report a reason rather than a sentinel. Split out as [#53](#53) — which the first version of this text promised and did not do, and Phase 3 caught. | @devarispbrown |
| <a id="52"></a>52 | Spend the engine cannot attribute to a Case is recorded against the **run**, and **no event says which Case it belonged to**. | Phase-3 review of the orphan-spend PR. The plan and four code comments claimed the event stream carried the attribution; it does not. `sinkFunc` returns before reaching `emit` on both skip paths, `emit` skips a budget refusal in any case, `RetryAttempted` names the Case but has no cost field, and `SettlementOvershoot` fires only when a settlement crosses the cap. Measured on the PR's own fixture: the Case carrying the orphan charge produced a `RetryAttempted` and nothing else. `CLAUDE.md`'s Observability section says new user-visible state is a new event type and **never a side channel**, and a column no event describes is a side channel. The money itself is durable and the cap is enforced — what is missing is being able to say which Case cost it. | **REPAID** (2026-08-25). An `OrphanSpend` payload carries the Case, the amount, the calls, and a reason distinguishing a budget refusal from a cancellation — a run stopped by a human is not a run that ran out of money. Emitted from the sink, after the durable write, so the stream never describes money the store does not hold; the reverse order would put a charge on the wire a resume cannot see. Landed with [#51](#51) rather than as a third schema PR, since both are additive changes to the same area. Verified by suppressing the emit (2 failures) and by reporting the wrong reason (1). | @devarispbrown |
| <a id="53"></a>53 | **REPAID in M2-11a** (2026-08-25): `openaicompat` splits 408 out of the 5xx arm and attaches `RETRY_REASON_TIMEOUT` through the adapter-side reason interface this entry asked for — the same anonymous-interface shape `retryAfterOf` uses for the wait. Both still carry `ErrTransportTransient` and both stay retryable: the reason is a label on a decision already made, never the decision. The status code is knowledge only the adapter has, so `core.retryReasonOf` now prefers an attached reason and falls back to its sentinel switch, rather than re-deriving what a sentinel cannot carry. Original entry follows. ~~`RETRY_REASON_TIMEOUT` is defined and never emitted, and a **billed 408** is reported as `PROVIDER_UNAVAILABLE`, whose schema definition is "the provider returned a 5xx".~~ | Phase-3 review of the overshoot/orphan PR — and this note is the one entry 51 promised to split out and did not, which is why it exists as its own row rather than inside a repaid entry. `openaicompat/errors.go` puts 408 in the same arm as 5xx and attaches a charge, and `core` sees only `ErrTransportTransient`, so it cannot distinguish a timeout from a server error. The old label was vague; the new one is specific and sometimes wrong. Separating them needs the adapter to report a reason rather than a sentinel — the same shape `retryAfterOf` uses for the wait. | **Before the first tagged release**, with the adapter-side reason interface, since `core` cannot fix this alone. | @devarispbrown |
| <a id="54"></a>54 | **REPAID in M2-11a-0** (2026-08-25): `recordCtx` now carries no deadline of its own, and the grace is armed BY the caller's cancellation via `context.AfterFunc` — which is what this field's godoc always described and no implementation had kept. A hung sink is bounded separately by the new exported `PerRecordTimeout`, per CALL, because the hazard is one write that never returns rather than a budget the run draws down. Scoping the grace to the results channel closing instead was considered and rejected in Phase-1 review of the [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §2.1: `results` is unbuffered and closed only after `workers.Wait()`, so the post-close window is at most one in-flight call and the grace would have bounded nothing while every test stayed green. Both halves are tested and both mutations kill their test. The shutdown test uses a sink that HONORS its context: `Run`'s godoc explicitly declines to bound one that does not, and the first version of that test asserted a promise this package does not make. Original entry follows. ~~The executor's `RecordGrace` deadline starts when the run STARTS, not when shutdown begins, so on any run longer than 30 seconds the sink receives an already-dead context.~~ | Phase-3 review of the overshoot/orphan PR, reproduced against the real executor: a healthy uncancelled run with a 250ms grace and six 100ms items failed with `recording result for c: context deadline exceeded`. `RecordGrace`'s own godoc says it "bounds how long result recording may continue AFTER the caller's context is cancelled". Every result after the deadline is dropped — no outcome row, absent from `CompletedCases`, re-paid on resume — and `sinkBroken` latches so the loss cascades. Invisible today only because every test uses a sub-second fake agent; every real LLM run exceeds 30s. Not introduced by that PR, but it is the live path by which a sink failure becomes a double-spend. | **Before M2-11**, which is the first PR that runs against a real provider from the CLI. Start the timeout when shutdown begins, or give the sink a per-call deadline rather than a whole-run one. | @devarispbrown |
| <a id="55"></a>55 | **REPAID in V-4c** (2026-08-27). The first stage consuming a Baseline as a reference reads the marker: `baselineCases` refuses a baseline whose `resolved_models` set holds more than one model, before any spend, with `--unsafe-baseline` as the explicit opt-out (`TestBlendedBaselineIsRefusedUnlessOptedIn`, `TestSingleModelBaselinePassesTheGate`). Prose was not sufficient — the numbers are recorded and the gate reads them. A trials MISMATCH is deliberately not gated: the recorded-baseline arm pairs against the baseline's aggregate, so a `Trials=3` baseline against a `Trials=1` Value run is a legitimate pair, and gating on equality would refuse the default configuration. Original entry follows. ~~A run whose resolved model changed mid-flight~~ is annotated in prose (`incomplete_reason`) but carries **no machine-readable marker**, so nothing refuses to treat it as a reference. Its `AggregateScore` blends two models. **CORRECTED 2026-08-25 (M2-11a Phase 3):** this entry said such a run "is annotated in prose (`incomplete_reason`)". It is NOT — `closeRun` writes that field only for `aggregateLost` and `ErrorRateExceeded`, and a gate stop has neither because the Cases are SCORED. The annotation does not exist, so the mitigation this entry recorded was never real. **And it is worse than stated:** the tripping Case is deliberately kept and recorded, so its new model joins `resolved_models` at close — a SECOND `--resume` arms the gate from the enlarged set, matches, and completes a run whose AggregateScore blends two models with nothing on the record saying so. Re-running the same command is the most natural thing a user does after a refusal. | [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §2.4, accepted risk R2. The first-response gate aborts the run, but `IsFatal` is consulted after the result is already on the channel and `cancel()` propagates asynchronously (`executor.go:305-315`), so up to `concurrency-1` further Cases are recorded under the new model. A marker field was deliberately NOT added: there is no consumer — the first stage that reads a Baseline as a reference is Valuation, which does not exist — and shipping a field ahead of its reader is the exact trap [#42](#42) records and M2-11 repays. | **The first stage that consumes a Baseline as a reference** (Valuation). That PR either reads a marker this one adds, or states why prose is sufficient for it. | @devarispbrown |
| <a id="56"></a>56 | **CARRIED, premise corrected** (2026-09-01, v0.2 ledger audit) — unfired: `plugin/` and `api/` are both `doc.go` only, so neither a plugin Agent nor the api error path exists. But one clause of the premise is wrong, and the code it cites is wrong in the same way: `errs.FromProto` sets `wrapped = errors.New(cause)` when the proto carries a cause (`core/errs/errs.go:259-274`), so `Unwrap()` returns nil only for a causeless error — while that function's own godoc at `:256-258` still states flatly that Unwrap returns nil, and `TestUnwrapIsNilWithoutACause` exercises only the causeless path, so nothing catches the contradiction. The half that matters is intact: only the outermost `Code` survives, so `RunFatal()` still cannot answer for an error that crossed a wire. Correcting that godoc is part of the repayment. Original entry follows. **Run-fatality does not survive serialization.** `errs.FromProto` rebuilds an `Actionable` with `Unwrap() == nil` and only the outermost `Code` (`core/errs/errs.go:249-265`), so the `RunFatal() bool` assertion M2-11 adds cannot answer for an error that crossed a wire. | [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §2.2 / Q1, accepted risk R3. Every in-process adapter is covered; a Ring-2 plugin Agent or an error arriving over the api SSE path is not, which re-opens [#47](#47)'s 10,000-call sweep for exactly those. Not fixed now because the fix is a field on the wire error — a proto change whose only consumer would be a plugin Agent that does not exist. | **The first plugin Agent, or the api error path, whichever lands first.** That PR carries run-fatality on the wire or accepts the sweep in writing. | @devarispbrown |
| <a id="57"></a>57 | **REPAID in M2-11b** (2026-08-25): `openaicompat.connect` refuses an absent credential for the DEFAULT host before any request, rather than omitting the `Authorization` header and letting every Case collect a 401. Only for the default host — a self-hosted OpenAI-compatible server (vLLM, Ollama, llama.cpp) legitimately needs no credential, and refusing there would make the local-model path unreachable, which is the same line `anthropic` draws for the same reason. The fix line distinguishes "no binding" from "bound to a variable that is empty", because those have different remedies. Original entry follows. ~~**`openaicompat` does not refuse an unset `--key-env` variable.** `connect` resolves an empty key and omits the `Authorization` header (`adapters/agent/openaicompat/config.go:229-234`), so the run proceeds unauthenticated and the provider 401s per Case.~~ | [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §5 / Q3, found in Phase-1 review. `anthropic` refuses at construction, but only for its default host (`anthropic.go:333-338`) — a non-Anthropic host may legitimately need no credential, and the same argument applies to a self-hosted OpenAI-compatible endpoint. So the correct behavior is genuinely open: refuse and break self-hosted, or proceed and let the 401 answer. M2-11 makes the 401 run-fatal, which bounds the damage to one Case either way. | **M2-11 PR 11b**, which either refuses at construction or records the decision in the cookbook's self-hosted recipe. Not deferrable past the PR that makes the flag reachable. | @devarispbrown |
| <a id="58"></a>58 | **REPAID** (2026-08-29, bookkeeping): #69's repayment text names itself the discharge path and `Capabilities()` is now load-bearing in scheduling — the row was never marked. No new code: this disposition records what the code already does. Original entry follows. **`Capabilities()` is never called.** Grep across every non-test file finds only the three adapter implementations and the interface declaration; `core/ring0.go:31-33`'s godoc ("Capabilities are checked BEFORE work is scheduled") describes a check that does not exist. | [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §2.9, accepted risk R5, found in Phase-1 review of a plan that had asserted the check was there. User-visible behavior is currently correct by accident: unsupported generation params are refused at **adapter construction** (`openaicompat/config.go:95-101`, `anthropic/anthropic.go:130-135`), which is before any spend. It stops being sufficient the moment an Agent is constructed without the CLI in front of it. | **When a second injection mode or a plugin Agent makes construction-time checking insufficient** — or M2-11 11b, if `kno doctor` turns out to need the ring-0 path anyway. Whichever is first. | @devarispbrown |
| <a id="59"></a>59 | **REPAID** (2026-08-29): the interactive spend-confirmation prompt exists — the init/consent PR (#102) ships the huh dialog (yes / no / adjust-cap), the non-TTY refusal, the `--yes` estimate, and the recorded consent decision. The TUI-milestone trigger below is satisfied by the prompt it names. Original entry follows. **The interactive spend-confirmation prompt does not exist.** `cli.confirmFunc` always declines and prints "Re-run with --yes to proceed" (`cli/render.go:104-116`), so the only usable real-provider invocation is `--yes`. | [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §2.5, accepted risk R4. Found in Phase-1 pass 2 checking a citation: draft 2 pointed this risk at [#31](#31), which is `ScoreSum`'s unrecoverable count and has nothing to do with consent — **no ledger entry covered the missing prompt at all**, so an accepted risk was carrying a phantom trigger. The huh prompt is a TUI dependency and the CLI declining is the honest interim; M2-11 11b makes `--yes` print the estimate it is consenting to, so the blanket flag is at least informed. | **The TUI milestone**, which owns the huh prompt. Not deferrable past it: a money path whose entire consent surface is a silent blanket flag is prime-directive-4 exposure, currently mitigated only by the printed estimate. | @devarispbrown |
| <a id="60"></a>60 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `checkBaseURL` still refuses fragment, query and userinfo (`adapters/agent/agentref/agentref.go:245-256`) and still never inspects `u.Path`, which is trimmed and reused verbatim; no provider has shipped a key format reliably distinguishable from a route prefix, and 1.0 has not been cut. Original entry follows. **A credential embedded in a `--base-url` PATH is accepted.** `https://gw.example/v1/sk-abc123` parses clean, is stored in `Run.agent.base_url`, emitted on `RunStarted`, rendered in `--json`, and survives `kno purge`. | [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §2.7, accepted risk R7. Query strings and fragments are already refused (`adapters/agent/agentref/agentref.go:234-238`, fuzzer-found) and userinfo is refused and redacted; the path is the remaining slot, and it is the four-copy exposure `agentref.go:133-139` records for the target and did not extend to the path. Accepted rather than fixed because a path segment is indistinguishable from a legitimate route prefix (`/v1`, `/openai/v1`, `/api/v3/deployments/gpt-4o`), and refusing segments matching known key prefixes (`sk-`, `sk-ant-`, `ghp_`, `AIza`) is a blocklist that fails **open** on every provider not in the list while false-positiving real routes. Mitigation is documentation: `SECURITY.md` says do not put keys in paths, use `--key-env`. | **Before 1.0, or when a provider ships a key format reliably distinguishable from a route prefix, whichever is first.** The date leg is new (2026-08-31) and the inbound-report leg is removed: a report-conditioned trigger can never lapse, which is the shape #46's review rejected and #33 was re-dated away from. Making the same mistake in three places is how it stops being a mistake and starts being a habit. | @devarispbrown |
| <a id="61"></a>61 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: the English prefix still decides classification (`adapters/agent/anthropic/errors.go:240`, with the constant at `:116`), no live-API canary exists — the only test naming the phrase is the mocked `runfatal_test.go:104-108`, whose own comment says the canary is the repayment — and the nightly has not reported the phrase. Original entry follows. **One run-fatal escalation rests on an English prose prefix.** `selfSetSpendLimit` is matched by `strings.HasPrefix(msg, "You have reached your specified API usage limits")` (`adapters/agent/anthropic/errors.go:119`), so a provider reword silently re-opens [#47](#47) for that condition. | [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §2.2b, accepted risk R8. Found in Phase-1 pass 2. It breaks the principle the neighboring `contextTooLong` comment states explicitly — *"the match only picks the FIX line, never whether the error is terminal, so a wording change degrades the advice rather than the classification"* — and M2-11 makes it decide classification. Accepted because it fails **open**: a reword drops back to per-Case handling, which is today's behavior, rather than killing a healthy run. The `spendLimitCode` and status-code conditions are structured and carry no such risk. | **A canary assertion against the live API**, or the nightly live run reporting the phrase — whichever lands first. The canary is the cheaper half and is a good `good-first-issue`. | @devarispbrown |
| <a id="62"></a>62 | **REPAID** (2026-08-31, v0.2 ledger audit) — **late, and the lateness is the finding.** The kno.yaml configuration layer shipped in #102 on 2026-08-28: `cli/config.go` carries 22 `KNO_*` specs with flag > env > file > default precedence, and its own header names this entry. The plan (`plans/2026-08-29-init-config-consent.md`) promised '#62 repaid'; #59 was marked and #62 was not. The 2026-08-29 ledger review then read this row two days later and missed it. Recorded here rather than backdated, because a ledger that hides its own misses is worse than one that carries them. Original entry follows. ~~**There is no env or `kno.yaml` configuration layer.** `os.Getenv`/`LookupEnv` appear outside tests in exactly two places, both for API keys (`adapters/agent/internal/transport/keybinding.go:131,134`). `DESIGN.md:294` specifies three layers — every field mirrored by a flag and a `KNO_*` var in a `kno.yaml` — and M2-11 11b ships **flags only**. | [M2-11 plan](plans/2026-08-25-m2-11-cli-wiring.md) §2.6, accepted risk R9. Draft 2 listed a `KNO_*` mirror column for thirteen flags, which is specifying a config system in a table cell; pass 2 caught that no mechanism exists to mirror into. Flagged rather than silently picked, per CLAUDE.md's rule on DESIGN.md conflicts. Two shipped fix lines already promise a `kno.yaml` that does not exist — `ErrBudgetExceeded.Fix` ("raise max_cost_usd in kno.yaml", `core/errs/errs.go:48`) and `ErrRateLimited.Fix` ("lower concurrency in kno.yaml", `errs.go:141`) — the same defect M2-11 fixes for `kno doctor`, now tracked rather than pretended away. | **The config PR.** `kno.yaml`'s schema is a post-1.0 covenant, so it is designed deliberately rather than accreted; the precedence rule is already decided (flag beats env beats file beats default) so that PR inherits a decision. Those two fix lines are corrected in the same PR or earlier. | @devarispbrown |
| <a id="63"></a>63 | **A test could spend the user's money.** `cli`'s tests drive the real `kno baseline` with real flags, and once provider adapters were wired, a subtest passing `--agent openai:gpt-4.1` resolved whatever `OPENAI_API_KEY` the machine exported and made live API calls. | Found while wiring M2-11b, on a subtest whose author believed it was refused before any network access — measured at 8.7 seconds of real calls. CLAUDE.md's rule that live-API tests are opt-in via `KNO_LIVE_TESTS=1` and never run in PR CI protected the adapter packages, which gate their live tests explicitly; it did not protect a package that had no live test and acquired the ability to make one by accident. **Mitigated, not closed:** `cli` now has a `TestMain` that unsets eleven provider credential variables unless `KNO_LIVE_TESTS=1`, and `CONTRIBUTING.md` states the rule. The list is a denylist, so a provider whose variable is not on it is still reachable. | **REPAID** (2026-08-26). `cli/main_test.go` now names the environment its tests may see — `PATH`, `HOME`, `TMPDIR`, each with a written reason — and clears everything else before the first test runs, `KNO_LIVE_TESTS=1` excepted. The allowlist is measured rather than guessed: the suite passes with an entirely EMPTY environment, so nothing on the list is load-bearing today and the three are there as process infrastructure, not configuration. Two tests hold it: `TestOnlyAllowlistedVariablesReachTheTests` asserts the surviving environment is a subset of the list, and `TestTheAllowlistCannotCarryACredential` refuses an entry whose name could hold a secret — without the second, "the test failed, so I allowlisted it" would be a two-line fix for this exact exposure. Set membership rather than a shape test on names, deliberately: a shape test has to describe what a credential variable looks like, which is the description that was one provider short to begin with, and it cannot express this list anyway — `--key-env host=VAR` takes any name, so `PATH` is as bindable as `OPENAI_API_KEY` and a shape test would need an exemption list, which is a denylist wearing a hat. Verified by mutation, all three fatal: with `EXAMPLE_API_KEY=sk-test` exported, a probe test could not see it; disabling the scrub made the probe fail and the membership guard fail on 20 surviving variables plus the canary TestMain plants before scrubbing; re-admitting `EXAMPLE_API_KEY` to the allowlist made `TestTheAllowlistCannotCarryACredential` fail on the name. The other packages were checked rather than assumed — `transport` and `agentref` cannot reach a real host, every non-live Agent in the two adapters is pinned to an `httptest` server, and the two that build against a real default host set the credential to `""` themselves — but the audit found `openaicompat`'s live gate testing `== ""` where its siblings test `!= "1"`, so `KNO_LIVE_TESTS=0` opted IN; fixed and verified both ways (with the old gate and no cap set, the live test ran and died at `liveGuard` rather than skipping). Original trigger: **when a fourth adapter lands, or at the first tagged release, whichever is first** — whichever PR adds an adapter must either add its variable or replace the denylist with an allowlist of what a test may read. A denylist that silently misses one is the shape of the bug it is guarding against. | @devarispbrown |
| <a id="64"></a>64 | **REPAID** (2026-08-27). `make tape` ran with vhs installed, `docs/quickstart.gif` is committed, and the README embeds it above the honest-1.000 note. Original entry follows. **PARTLY REPAID at the 0.0.1 close-out** (2026-08-27) — the tape exists; the GIF does not. `tapes/quickstart.tape` records `kno doctor`, a real nine-Case eval file, and one `kno baseline` run against `fake:`, pinned to a fixed theme and typing speed so a re-recording after a CLI change differs only where the CLI changed. `make tape` runs it and refuses with an actionable message when vhs or the `kno` binary is missing, so the Definition-of-Done clause has something to point at rather than being satisfied vacuously. The tape carries a comment saying the 1.000 score is honest rather than flattering — `fake:` answers every Case with what the Case expects — so nobody "improves" it later by choosing an eval file that makes the number look earned; the README says the same thing under the sample output. What is still owed: an actual recorded GIF. vhs is not installed on the machine this was written on, and a GIF is a binary artifact that has to be produced by a person on a terminal. README deliberately does NOT reference one yet, so nothing is broken meanwhile — a missing image would be worse than no image. Original entry follows. ~~**No vhs tape exists anywhere in the repo**, though `CLAUDE.md` describes a README quickstart GIF, lists tapes as a workstream, and the Definition of Done requires re-recording one whenever CLI output changes.~~ | Noticed in M2-11c while trying to honor that DoD line: there is nothing to re-record. Pre-existing rather than caused by any PR in this milestone — but M2-11b changed the CLI's output substantially (new flags, a `width` line, a `--yes` estimate, `kno doctor`), so the gap is now larger than it has ever been, and every PR since the DoD was written has satisfied that clause vacuously. | **Re-dated 2026-08-27 to before 0.1.0**, which is a disposition rather than a carryover: the gap the entry names — no tape anywhere in the repo — is closed, and what remains needs a human with vhs installed rather than another PR. That PR runs `make tape`, commits `docs/quickstart.gif`, and adds the image to README. **Before the first tagged release.** A README that shows the tool running is the single highest-leverage doc for the audience `CONTRIBUTING.md` describes, and the DoD clause is unenforceable until one exists — a checklist item nothing can fail is worse than no checklist item. | @devarispbrown |
| <a id="65"></a>65 | **REPAID** (2026-08-28, select/export plan). The Select stage ranks heterogeneous estimands, and the fix the trigger demanded now lives in it: `PortfolioEntry.n_routed_scale` records the scaling applied to each entry's tagged delta under the model this plan states (effect uniform on the routed slice, zero elsewhere), and the absent-`n_routed` case is flagged rather than silently divided — the scale field is present exactly when a delta was scaled. `what-the-numbers-mean.md` states the model in the same PR, per the plan's docs-pair-with-behavior gate. Original entry follows. ~~**The estimand varies per Asset in the Value stage.** A tagged Asset's `delta_goal` is the mean effect over its routed slice; an untagged one's is over the whole dev split. Select ranks them together on `delta_per_cost` as if commensurable. | [Value plan](plans/2026-08-26-value-stage.md) §2.5, accepted risk R1. Named rather than fixed: `n_routed` and `n_dev` are recorded so a reader can scale one to the other, and `what-the-numbers-mean.md` states that differently-routed deltas are not comparable. Ranking heterogeneous estimands is a Select design problem, not a Value one — Value's job is to make the incomparability visible. | **The Select stage**, which is where the ranking is acted on and where the fix has to live.~~ | @devarispbrown |
| <a id="66"></a>66 | **REPAID** (2026-08-28, select/export plan). `stats/portfolio.Correct` applies the Bonferroni correction (corrected level = 1 − (1−level)/n_screened, rescaled by the interval method's own quantile ratio) and every keep/reject decision in the Select stage decides on the corrected interval; the portfolio-level gain is one corrected claim combined under the shared-draw covariance discipline the entry's correlation note demands. The winner's-curse evidence the trigger required is TWO tests: a simulation of thousands of screens over synthetic ground truth asserting the false-discovery rate at the corrected level, and the `stats/interval` ranking-inflation characterization pinned as a bound. Original entry follows. ~~**`delta_interval` controls a per-comparison error rate, and nothing corrects for multiplicity.** With 200 Assets at 95%, roughly ten null ones have intervals excluding zero by construction — concentrated where `delta_per_cost` ranks highest, because small samples and small Assets are the same Assets. | [Value plan](plans/2026-08-26-value-stage.md) §2.6, accepted risk R2. `CaseExecution.valuation_count` records N so a consumer CAN correct, and the `Interval` godoc says `level` is per-comparison — but Value itself does not adjust. The correlation is also stronger than independent-comparison arithmetic suggests: where a recorded baseline is reused as the control, every Asset is paired against the same draw. | **The Select stage**, with the winner's-curse property test in `stats/interval` as the evidence of how bad it is.~~ | @devarispbrown |
| <a id="67"></a>67 | **REPAID** (2026-08-28, select/export plan). `stats/portfolio.NetLoss` combines treatment and control deltas weighted by their populations, with an interval that takes the shared recorded-baseline draw conservatively (perfectly-correlated bound; the independent bound for a fresh control arm). The durable Valuation gained the additive fields the combination needed (`n_control`, `fresh_control_arm`, `pairing_scheme`). The regression verdict is gated exactly as the plan's A4 requires: an underpowered control arm never produces a REGRESSION accusation. Original entry follows. ~~**No net-loss judgement.** `valuation.proto` promises `delta_control` answers whether an Asset's harm outweighs its help; combining treatment and control across two differently-sized Case populations, weighted by their sizes, is not computed. | [Value plan](plans/2026-08-26-value-stage.md) §2.4, accepted risk R4. The prose promise is on `delta_control`; the arithmetic has no home and no consumer until something acts on a net judgement. | **The Select stage**, which is the first consumer that would act on it.~~ | @devarispbrown |
| <a id="68"></a>68 | **CARRIED, trigger cell corrected** (2026-09-01, v0.2 ledger audit) — this entry fired on 2026-08-29 and was re-dated to "before 1.0" in the same breath, but both were announced in this column only: the trigger cell still held the Q9 and inbound-report text that had already fired and had already been rejected as unlappable. Moved. Unfired on the live leg: the divisor is still shared and still biased, every content-type pool still inherits it (see `adapters/pool/hf/doc.go`), and 1.0 has not been cut. Original entry follows. **FIRED** (2026-08-29, the Hugging Face adapters PR): the trigger was "the first PR that adds a second content-type pool", and this PR is that PR — the HF pool estimates `context_tokens` from bytes over the shared 3.6 divisor, exactly as the markdown and CSV pools do (see the plan's accepted risks and the `contextTokens` godoc in `adapters/pool/hf`). Firing means the bias is acknowledged and carried, not repaid: every content-type pool that ships the byte-based estimate inherits the same ~3x reservation on prose, and the ranking math's known bias is now the documented norm rather than a pending correction. The only repayment (a real BPE tokenizer) is re-dated to **before 1.0**. Original entry follows. **RE-DATED** (2026-08-29, v0.1 ledger review): the first leg (Q9's resolution in the V-1/V-4 slice) lapsed when V-4c shipped 2026-08-27 with the amendment keeping the trigger unchanged — recorded as the lapse it is. The second leg (first report of a pool ranked by content type) is the inbound-report shape #46's review rejected. Re-dated to **"before 1.0, or the first PR that adds a second content-type pool"** — a condition that can lapse and be checked. Original entry follows. **AMENDED in V-4c** (2026-08-27): Amendment A folded its Q9 sibling into this entry rather than splitting the same bias. The trigger stands unchanged; the bias is now live, since `Valuation.n_dev` and `delta_per_cost` are what this denominator feeds. **`CostVector.context_tokens` carries the tokenizer's bias, and it is the ranking denominator.** `pricing.countTokens` reserves ~3x what English prose uses — correct for reserving money, wrong for ranking — so two Assets of equal real token cost can differ ~2.4x in `delta_per_cost` by content type alone. | [Value plan](plans/2026-08-26-value-stage.md) §2.7, accepted risk R5 and open question Q9. The only unbiased alternative is a real BPE tokenizer, which `pricing`'s own godoc rejects as "a large dependency plus a per-model vocabulary that goes stale silently". The reservation path keeps the pessimistic bound, because under-reserving is the failure that costs money. | **Before 1.0.** A real BPE tokenizer is the only repayment; until then the bias is carried and documented at every content-type pool that inherits it. (Re-dated 2026-08-29 when the second content-type pool shipped, which fired the previous trigger; moved into this cell 2026-09-01, where the release gate can read it. The superseded trigger was: Q9's resolution in the V-1/V-4 slice, or the first report of a pool ranked by content type rather than by value.) | @devarispbrown |
| <a id="69"></a>69 | **REPAID in V-4c** (2026-08-27). The refusal landed: `measureAsset` asserts the WRAPPED agent declares `context_inject` before any spend on the Asset (`TestValueRefusesAWrapperThatLiesAboutInjection`), and `Capabilities()` is now load-bearing on the spend path. Original entry follows. ~~**`Capabilities()` is still never called, and the Value stage is the first place it must be.**~~ An adapter that ignores `pricing.Prompt.Context` would produce a full-price Value run of pure noise — with intervals — indistinguishable from a real result. | [Value plan](plans/2026-08-26-value-stage.md) §2.7, accepted risk R5's sibling and the discharge path for [#58](#58). V-3's contract requires the Agent returned by `WithContext` to forward `Estimator` and `Capable`; V-4c refuses an Asset before any spend if the adapter does not declare `ContextInject`. Neither exists yet. | **V-3 and V-4c of the Value plan.** Not deferrable past them: the refusal is what makes an unimplemented injector fail loudly rather than expensively. | @devarispbrown |
| <a id="70"></a>70 | **CARRIED, trigger cell completed** (2026-09-01, v0.2 ledger audit) — unfired: `adapters/evals/jsonl/` has no commit after 2026-08-28 (#103), and `TestEarlyBreakClosesTheFile` is still the 200-reopen version (`adapters/evals/jsonl/jsonl_test.go:88-117`) while the Pool adapter's descriptor probe sits one directory over (`adapters/pool/jsonl/jsonl_test.go:132-177`). The "before 1.0" leg the 2026-08-29 re-date added was announced in this column only and is now in the trigger cell as well. Original entry follows. **RE-DATED** (2026-08-29, v0.1 ledger review): the trigger (the next PR touching `adapters/evals/jsonl`) fired with #98 and the probe was not ported — recorded as the lapse it is. The test's failure is real: the descriptor-limit claim only fails on a system whose limit is exhausted, which CI never is. Re-dated to **"before 1.0, or the next PR that touches `adapters/evals/jsonl`"**, and the port is the whole repayment — no design remains to be made. Original entry follows. **`adapters/evals/jsonl`'s early-break close test cannot fail.** `TestEarlyBreakClosesTheFile` reopens the file 200 times and treats a successful reopen as proof the descriptor was released — but the descriptor limit on a developer machine and on CI is 1,048,576, so 200 reopens exhaust nothing whether or not the file is ever closed. | Found while building the Pool adapter (V-2 of the Value stage), which was modelled on this test and inherited the defect. Verified directly: deleting `defer f.Close()` from `adapters/evals/jsonl/jsonl.go` leaves the test **green**. It has been providing false assurance since M1 that early-break cleanup works — and early break is the common path, since `checkFeasible` and every consumer that stops reading take it. The Pool adapter's replacement probes the lowest free descriptor and keeps the abandoned iterators reachable until after the probe, because the runtime's finalizer otherwise reclaims most of the leak before it can be observed. | **Before 1.0, or the next PR that touches `adapters/evals/jsonl` — whichever is first.** The repayment is the port of `adapters/pool/jsonl`'s descriptor probe and nothing else; no design remains to be made. Not urgent — the cleanup itself is correct today and only the test is worthless — but a test that cannot fail is worse than no test, because it occupies the slot where a real one would go. (Re-dated 2026-08-29; the date leg moved into this cell 2026-09-01.) | @devarispbrown |
| <a id="71"></a>71 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired, and it still moves with [#12](#12): the tool modules remain unscanned, and with them the ~500-package graph that executes in the one job holding `id-token: write`. Not deferrable past 1.0, which has not been cut. Original entry follows. **goreleaser's ~500-package dependency graph executes in the one job that holds `id-token: write`, and nothing scans it.** `make vuln` covers the shipping module only. | [Release plan](plans/2026-08-26-release-pipeline.md), accepted risk 1, and the sharp end of [#12](#12). goreleaser is byte-pinned by its own committed `tools/goreleaser/go.sum` — Phase-1 review rejected the weaker `go run module@version` pinning the plan originally proposed, on the grounds that the signing job deserves better than checksum-database verification — but pinning is not scanning. That identity can sign artifacts as this repository, permanently, into a public transparency log; a compromised dependency in `gocloud.dev` or `sigs.k8s.io/kind` executes there. Dependabot watches the module weekly and deliberately does **not** group its bumps: a grouped bump of this graph is unreviewable, and cosign/sigstore/go-git are exactly what a group would bury. | **With [#12](#12)** — the same fix (`govulncheck` over the tool modules) covers both, and this is the module that makes it urgent rather than tidy. Not deferrable past 1.0. | @devarispbrown |
| <a id="72"></a>72 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `check` is still `fmt-check lint test typecheck-proto generate-check vuln docs judge-calibrate-check fuzz-short bench-diff` (`Makefile:151`), with `release-check` and `release-stamp` outside it in their own CI job; nobody has landed a broken release config on a green `make check`, and 1.0 has not been cut. Original entry follows. **`make release-check` and `make release-stamp` are not part of `make check`**, so a green local `make check` no longer implies a green CI for changes to `.goreleaser.yaml`, the release Makefile targets, or `cli/root.go`'s build stamp. | [Release plan](plans/2026-08-26-release-pipeline.md), accepted risk 2. Together they are a ~500-package compile plus a real build, which does not belong in a gate whose entire design is fail-fast-cheapest-first. They run as their own `release-config` job in `ci.yml`, following the precedent the `release-please-config` job already set, so both still run on every PR. CONTRIBUTING.md names the out-of-`check` targets in their own table rather than leaving contributors to discover them from a red build. | **Before 1.0**, or the first time someone lands a broken release config because `make check` was green. `make tools` already installs the binary into `./bin`, so folding it in is a one-line change once the compile is reliably cache-warm in CI. | @devarispbrown |
| <a id="73"></a>73 | **REPAID** (2026-08-27). The tap repository exists — `uknoAI/homebrew-tap`, public, with `Formula/kno.rb` pinning each v0.0.2 platform archive's SHA-256 and a README explaining the tap — the `brews:` block is uncommented, and `release.yml` wires `HOMEBREW_TAP_TOKEN` so goreleaser updates the formula on every release from the next one on. The one remaining human step is minting the fine-grained PAT (contents: write on the tap repo only) and adding it as the `HOMEBREW_TAP_TOKEN` repo secret; until that exists, the next release's publish step will fail on the tap push — loudly, after the binaries are built and signed, which is the cheapest place this failure can land. Original entry follows. **RE-DATED** (2026-08-27): v0.0.2 shipped without the tap because the tap repository still does not exist — and this entry's own constraint forbids inventing its name, since a typo discovered at publish would fail the release after the binaries are built and signed. The disposition is recorded rather than silent: the trigger moves to the third tagged release, with the tap repo's creation as the hard precondition, because a one-version tap remains a tap nobody has installed and a two-version gap is no better. Original entry follows. **The Homebrew tap is configured but disabled.** `CLAUDE.md` specifies a tap; `.goreleaser.yaml` carries a complete `brews:` block, commented out. | [Release plan](plans/2026-08-26-release-pipeline.md), accepted risk 3. No `homebrew-tap` repository is referenced anywhere in this codebase, and inventing the name would make the first tag fail at the publish step — after the binaries are built, signed and attested, which is the most expensive place to discover a typo. [#31](#31) already assumes a tap exists ("one `brew upgrade` and one `--resume` apart"), so this is load-bearing for that entry too. | **The PR that creates the tap repository** must uncomment the block and add the token it references, in the same change. Re-dated from "before the second tagged release" (lapsed at v0.0.2): now **before the third tagged release**. | @devarispbrown |
| <a id="74"></a>74 | **REPAID** (2026-08-26): `RELEASE_PLEASE_TOKEN` exists on the repository, so the tag push carries a non-Actions identity and release.yml fires on its own. `googleapis/release-please-action` is SHA-pinned in the same change rather than waiting for [#14](#14)'s trigger — a mutable tag on the action about to be handed a credential that can push to `main` and create tags is the one place that cannot wait, since per the release workflow that credential is what mints signed releases. Phase-3 review also found this entry's companion mitigation was worse than the problem: `"draft": true` in release-please-config.json created no git ref at all, so the token it prescribes would have changed the actor on a push that was not happening. The draft now lives in `.goreleaser.yaml`, where it hides the release until the artifacts are on it. **Now tested by the first real tag** (v0.0.2, 2026-08-27): the tag push fired the workflow on its own, and the release PR was opened by a person — both tells confirmed. Original entry follows. ~~**Untested until the first tag**: a fine-grained PAT can sit pending org approval and still appear in `gh secret list`. The tell is that the release PR is opened by a person rather than by github-actions[bot].~~ ~~**A tag pushed by release-please starts no release run.** GitHub does not create workflow runs from events triggered by the default `GITHUB_TOKEN`, and `googleapis/release-please-action` pushes the tag with it. Until a `RELEASE_PLEASE_TOKEN` secret exists, a maintainer must dispatch `release.yml` by hand against the tag.~~ | [Release plan](plans/2026-08-26-release-pipeline.md), Phase-1 finding 1 — caught in review, not in production, which is the only reason this entry is not a post-mortem. The failure mode is the dangerous kind: **no run is created at all**, so there is no red build and no notification, just a release page with no artifacts on it. Three mitigations landed together: `release-please.yml` already passes `token: ${{ secrets.RELEASE_PLEASE_TOKEN \|\| github.token }}` so the secret is the only missing piece; `release.yml` carries a `workflow_dispatch` trigger with the reason written above it; and release-please now creates the release as a **draft**, so a tag whose workflow never ran cannot be mistaken for a finished release. | **Before the first tag.** Create a PAT or GitHub App token with `contents: write` and add it as `RELEASE_PLEASE_TOKEN`; nothing else needs to change. Doing it after the first tag means doing the first release by hand. | @devarispbrown |
| <a id="75"></a>75 | **REPAID** (2026-08-29) — late: the trigger (`--routing-seed` shipping, #66) fired and lapsed across the 0.0.2–0.0.4 releases; the one-time stream break is absorbed into v0.1.0, named in the CHANGELOG and the `Seed` godoc. The fix inlined, as the entry prescribed: `core/value/shuffle.go` owns a Fisher-Yates over the raw PCG stream with an inlined bounded draw (`uniformBelow`), pinned by a golden-permutation test so a future change to the stream is a deliberate breaking promise, not an accident. The shuffle was replaced, not preserved: runs recorded by earlier releases re-derive only under the earlier binary, and `Route.Seed`'s godoc now states that boundary (the wording-scoped alternative would have made the promise weaker than the mechanism can now support). Original entry follows. ~~The Value stage's selection is reproducible from its seed only within one `kno` release.~~ `Route` records a seed on the Run and its godoc says a reader can re-derive exactly which Cases were chosen. `math/rand/v2`'s PCG output IS a specified algorithm and the `uint32n` path is documented as preserving its sequence, so GOARCH is not a risk — but `Rand.Shuffle`'s consumption pattern and `uint64n`'s rejection loop carry no compatibility promise, unlike `math/rand` v1. A future Go release may reshuffle identically-seeded runs. | Phase-3 review of V-4b, P3-10. The claim outlives the binary: the seed is persisted on the Run, so a reader re-deriving a selection years later is exactly the use the field exists for. Inlining an eight-line Fisher-Yates over `rng.Uint64()` with our own reduction removes the dependency entirely and is the fix; it was not taken in V-4b because the PR was already carrying three P1 statistical fixes and a hand-rolled shuffle is the kind of code that wants its own review. | **When `kno value` ships a user-visible seed** (V-5), which is the moment the claim becomes a promise to someone outside this repository. That PR either inlines the shuffle or scopes the wording to "re-derivable by the same kno release". | @devarispbrown |
| <a id="76"></a>76 | **REPAID** (2026-08-27). `scripts/fold-changelog.sh` runs in the release workflow after publish: it renames the hand-written `## [Unreleased]` heading to `## <version> — in detail`, commits with a DCO sign-off, and opens a PR that auto-merges when green — never a direct push to main, which CLAUDE.md forbids even for the pipeline. Idempotent for re-tags. Original entry follows. **The CHANGELOG has two sections per release and nothing keeps them in step.** release-please generates a commit-derived section at release time; CLAUDE.md separately requires a hand-written entry under `## [Unreleased]` on every PR. At 0.0.1 the result was a generated `## 0.0.1` sitting directly above `## [Unreleased]` containing the ~900 lines describing that same release — so a reader browsing the repo would conclude none of the work had shipped. The heading is renamed by hand at release time. | Found while cutting 0.0.1, from the release PR diff rather than from the file, because the collision only exists once release-please inserts its section. CLAUDE.md asks for both a derived changelog and a hand-maintained one and does not say how they compose; that is a genuine conflict between two of its own rules rather than a bug in either. | **Before 0.1.0.** Either the rename becomes part of the release workflow (it can run against the release PR branch, which is the only moment both sections exist), or `release-please-config.json` stops generating a changelog section and the hand-written file becomes the single source with release notes drawn from it. The second is closer to CLAUDE.md's intent — "proto comments are the single source" is the same principle applied one file over — but it changes what the GitHub release page shows, so it wants a decision rather than a patch. | @devarispbrown |
| <a id="77"></a>77 | **REPAID in V-4c** (2026-08-27). Value is the second `invoker` caller and its trigger fired: `TestValueWiresEveryInvokerHook` mirrors Baseline's, and the hooks now emit the money events with the measurement key — `RetryAttempted` and `SettlementOvershoot` carry `asset_id`/`arm`/`trial`, pinned end to end by `TestValueMoneyEventsCarryTheMeasurementKey`, which drives a real retry and a real overshoot through the fake adapter. Original entry follows. ~~**A stage that forgets to wire~~ an `invoker` event hook is silent about it.** `core/invoke.go` takes `OnOvershoot` and `OnRetry` as nil-able funcs; a nil one emits nothing, and the absence is indistinguishable from a run that had nothing to report. Those two events are the ones that explain where money went — a settlement overshoot is spend past its reservation, and a retry is the difference between obeying a provider and hanging. | Found reviewing the extraction that created the seam (7a2d94f). Not a defect in this release: Baseline wires both, so the nil branches are unreachable in shipping code, and nil-safety is deliberately kept because a panic on a money path is worse than a missing event. What is missing is a way to NOTICE. | **When the second `invoker` caller lands** (V-4c, the Value measurement loop). That PR adds the same wiring test this one added for Baseline (`TestBaselineWiresEveryInvokerHook`), or replaces both with a construction-time check if a third caller makes the per-stage test the wrong shape. | @devarispbrown |
| <a id="78"></a>78 | **CARRIED, trigger cell corrected** (2026-09-01, v0.2 ledger audit) — the 2026-08-28 split re-dated the live half and struck the old trigger through in the cell without replacing it, so the cell the release gate reads still named the dead trigger. Replaced. Unfired: no writable Destination adapter exists — the knowledge-injection plan has no PR, and `core.destinationFor` still routes nothing to `DESTINATION_KNOWLEDGE_BASE` (see [#133](#133)). Original entry follows. **SPLIT, both halves re-dated** (2026-08-28, select/export plan). The first draft of this plan re-dated this entry to "when a third Destination lands" — a trigger the same PR satisfies, i.e. a carryover wearing a disposition, the exact failure the ledger rules forbid. The amended disposition pays the cheaper half now and re-dates the rest to a trigger that cannot self-satisfy: the **pairing-scheme recording** half (which trial routed which Case) landed as additive proto fields on the durable Valuation (`n_control`, `fresh_control_arm`, `pairing_scheme`) — the schema half of the plan's A7. The **measurement-design** half (splitting the baseline's trials across the routed sample, changing the consent quote and the fresh control arm) is re-dated to **"when the first writable Destination adapter lands (v0.2 knowledge injection)"** — which nothing in this plan touches, so the trigger cannot be satisfied by accident. Original entry follows. ~~**A7 sample-splitting is deferred to its own plan.** The Value plan's Q11: with `trials > 1`, splitting the baseline's trials across the routed sample removes the 2x on the routed slice for free — but it changes the pairing scheme (which trial routed which Case must be recorded), the consent quote (measurement counts vary per trial), and the fresh control arm (control becomes trial-conditional). That is a measurement-design change needing its own Phase 0/1, not a V-4c footnote. | [Value plan](plans/2026-08-26-value-stage.md) §12.4 Q11, Amendment A. The cost of deferral is bounded (extra measurements on the routed sample); the cost of rushing is a changed measurement design with no review. | **When the first writable Destination adapter lands — the knowledge-injection PR ([plan](plans/2026-08-31-knowledge-injection.md)).** (Split and re-dated 2026-08-28 in the What column only; moved into this cell 2026-09-01. The release parenthetical the split carried is dropped for the reason [#142](#142) records: the obligation is the adapter, not a tag.) Superseded original: ~~When a second Destination lands, or a power argument for `trials > 1` appears.~~ | @devarispbrown |
| <a id="79"></a>79 | **RE-DATED** (2026-09-01, v0.2 ledger audit) — **the trigger's second leg had already fired before this entry was even written, and nothing reported it.** `costOf` prices cache writes in three of four adapters — `adapters/agent/anthropic/invoke.go:300-315`, `bedrock/invoke.go:302-313`, `vertex/invoke.go:266-277` — and has since e05810b (2026-08-22, #37), four days before the Value plan this entry cites; `openaicompat` carries no cache-write term because OpenAI does not bill one separately. So "`costOf` does not price cache writes today" was false on the day it was written, and re-dating it on 2026-08-31 restated a fired trigger instead of disposing it. The live debt is narrower and real: the **quote** is still iid — `core/value/` contains no reference to caching at all — so the pre-run number a user consents to does not model the first Case paying the shared prefix. Re-dated onto that, which is a condition somebody can actually check. Original entry follows. **Per-Case cost under prompt caching is order-dependent, and the quote models it as iid.** The Asset's shared prefix is cacheable across its routed sample (Q4's message-body resolution), so the first Case pays a cache write the rest do not — and `costOf` does not price cache writes today. The delta is order-invariant and tested (`TestValueOrderDoesNotChangeDeltas`); the cost is not. | [Value plan](plans/2026-08-26-value-stage.md) §12.4 Q4/Q12, Amendment A. Deterministic Asset order (sorted by ID) makes the cost path reproducible even while it is unmodeled. | **Before 1.0, or when the Value stage's quote models the shared-prefix cache write — whichever is first.** (Re-dated 2026-09-01. The previous `costOf` leg had already fired unnoticed; a trigger that has silently fired is worse than no trigger, because it occupies the slot where a live one would go.) | @devarispbrown |
| <a id="80"></a>80 | **REPAID** (2026-08-27). The first process now arms the gate from the BASELINE's recorded models — the reference was measured against them, so an agent answered by anything else blends two models into one delta, and `TestFirstProcessCatchesARepointedModel` refuses the very run the re-point happens in. Original entry follows. ~~**Value's mid-run model gate is armed on resume only.**~~ The gate checks every successful measurement's resolved model against the run's recorded set, but the FIRST process has nothing recorded to arm from — its close writes `CaseExecution.resolved_models` for the resume to check. An alias that re-points during a first process (a Value run's wall-clock is a multiple of Baseline's) goes unrefused until the next resume. | Phase-3 review of V-4c (2026-08-27). The gate itself landed — `gate.afterRecord` wired through the executor, models collected in the sink and written at close — because the machinery was cheap once the durable writer existed. The first-process gap is inherent to arming from a record. | **Before 1.0**: arm the first process from the baseline run's recorded models, or state why the baseline comparison suffices. | @devarispbrown |
| <a id="81"></a>81 | **REPAID** (2026-08-28). The release workflow now removes exactly the shape this entry describes — a PUBLISHED release with zero assets — before building. A release with assets is real work and is never deleted; a draft is goreleaser's own. Original entry follows. ~~**A failed release build leaves release-please's published empty release page up.**~~ release-please creates the GitHub Release when it tags; `draft: true` there cannot fix it, because a draft in release-please-config means NO tag ref is created at all ([#74](#74) measured that). The workflow's goreleaser runs `draft: true`, so its own output is draft-until-published — but the empty published release exists before the pipeline starts, and when the sign step failed on the first tag, the public v0.0.1 page sat published with zero assets. Mitigated by hand that time (delete the release, re-dispatch). | Discovered by the first tag's failed run (2026-08-27). The workflow's own releases are safe; release-please's aren't, and nothing can know whether a release is "empty pending pipeline" or "published by hand" without heuristics. | **When the next release is cut**: either a workflow step that deletes the empty release before building (with the check written down), or a release-please option that creates it as a draft without suppressing the tag. | @devarispbrown |
| <a id="82"></a>82 | **REPAID** (2026-08-30) — not by upgrading the action, but by removing the manual step the entry existed to track. `release-please.yml` now amends the release commit with its trailer and force-pushes with `RELEASE_PLEASE_TOKEN` (a default-token push creates no workflow runs, so the DCO check would never re-run). The amend is idempotent, matches dco.yml's own author-or-committer rule, sets the committer to the commit's author so the trailer cannot pass for the wrong reason, and uses `--force-with-lease` so a concurrent release-please update is refused rather than clobbered. An exemption in dco.yml was rejected: with `RELEASE_PLEASE_TOKEN` present the commit is authored by the token's OWNER, not a bot, so there is no bot login to match — and a branch-name exemption is a hole anyone can push through. The action SHA is unchanged, so the upgrade obligation below still stands and is now the entry's only open half. Original entry follows. ~~**The pinned release-please action ignores the `signoff` input**~~, so release PR commits ship without a DCO trailer and the DCO gate fails them until a maintainer amends with `--signoff` and force-pushes. The workflow's dead input is removed (it was misleading); the amend is the real mechanism and is documented here. | Measured in the action log for the 0.0.3 post-merge run: `##[warning] Unexpected input(s) 'signoff'`. Debt #65's fix assumed the input worked; it never did — every release PR since has been signed by hand. | **When the release-please action SHA is next upgraded**: pin a version whose inputs include `signoff` (or a comparable trailer mechanism), re-add the input, and drop the manual amend from the release runbook. | @devarispbrown |
| <a id="83"></a>83 | **REPAY — DUE, not deferred** (2026-09-01, v0.2 ledger audit). The trigger has now fired twice: at the v0.1.0 tag, and again with PR [#184](https://github.com/uknoAI/kno/pull/184) (the tuner bridge, opened 2026-09-01), which is the first Tuner PR and does **not** carry the reader. A third re-dating is not available and this entry says so itself. The other permitted disposition — an ADR abandoning parquet — was considered and rejected on four grounds: `DESIGN.md` names parquet among the launchable Ring-1 pools in four places (`:178`, `:223`, `:344`, `:398`); issue [#154](https://github.com/uknoAI/kno/issues/154) is already filed and labelled `good first issue`, with the dependency choice fenced off as the maintainer's; [the tuner-bridge plan](plans/2026-08-31-tuner-bridge.md) independently reached the same disposition after its own Phase-1 review; and nothing supersedes the need — `adapters/pool/hf` reads the datasets-server rows API, not parquet, so a local `.parquet` file still has no path into a Pool. So the disposition is repayment, and the ledger records it as work that is due rather than debt that is accepted. It is real engineering (a new dependency plus a package plus the `coretest` conformance calls), which a docs audit cannot do; it is scoped in #154. Original entry follows. **Re-dated** (2026-08-29, v0.1 ledger review): the v0.1.0 leg of the trigger fires with the tag; the deferral stands (Tuner is v0.2). The trigger is now **"the first Tuner PR"** — a condition that cannot self-satisfy. Original entry follows. **The parquet pool adapter is deferred from DESIGN's v0.1 bar.** DESIGN:397 lists parquet among the launchable Ring-1 pools; csv and markdown shipped without it (docs/plans/2026-08-29-ring1-adapters-batch.md). | A parquet reader is a real dependency, and nothing in the v0.1 feature set consumes parquet — tuning sets are the realistic source, and Tuner adapters are v0.2. The deferral is a DESIGN-scope disposition, recorded rather than silent. The cost is cheaper than it first looked: a pure-Go parquet reader is the same dependency class the repo already carries (`modernc.org/sqlite`), no cgo, so the single-runner release stays intact. | **Repayment is issue [#154](https://github.com/uknoAI/kno/issues/154) landing `adapters/pool/parquet` — no further deferral.** A Tuner PR that merges while #154 is open must state in its own body why the Pool half of its plan is unpaid; that is a lapse to record, not a carry. (The spent first-release leg is removed 2026-09-01: it fired at that tag and was disposed by the 2026-08-29 re-date, but the words stayed in this cell, so the release gate kept refusing a release long since cut. See the What column.) The landing PR adds the reader with the standard dependency justification. | @devarispbrown |

| <a id="84"></a>84 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: all five declared stages are `shipped` (`cli/status.go:87-91`, and `docs/status.json` agrees), so no stage has shipped `partial`, and 1.0 has not been cut. Original entry follows. **`shipped` in `cli/status.go` is still a human judgement.** The three cross-checks (exhaustiveness against the `Stage` enum, a registered command behind every shipped stage, agreement with README.md's Stages table) make the declaration hard to get *inconsistent*, not hard to get *wrong*: a maintainer who declares `validate` shipped the day a stub command lands passes all three. | [Plan](plans/2026-08-30-kno-status.md), section 4. Full derivation is impossible and the plan says why — the enum carries `STAGE_VALIDATE` before validate ships because proto leads implementation, `kno report` is a command and not a Stage, and passing tests can describe a half-built stage. A declaration that is cross-checked is more honest than a derivation that is subtly wrong. The mitigation for the remainder is social (review) plus the tri-state `partial`, and that is the honest ceiling of a mechanical approach. | Revisit when the first stage ships `partial`, or at 1.0 — whichever is first. | @devarispbrown |
| <a id="85"></a>85 | **CARRIED, and reviewed as this entry requires** (2026-09-01, v0.2 ledger audit) — the row asks to be reviewed at each minor release, so, explicitly: still no named consumer of the **command**. `uknoAI/kno-www` now consumes the generated artifact (`src/lib/kno-status.ts`, `src/components/StatusBlock.astro`, `src/pages/index.astro` and `src/content/docs/roadmap.md` all read `docs/status.json`), which is the file, not `kno status`, and does not fire this trigger. No cobra command is registered — `cli/root.go:127-139` lists every command and `status` is not among them. Original entry follows. **`kno status` is cut, not built.** Only the generated `docs/status.json` ships. The renderer and the stage declaration live in `cli/status.go` and are reachable, but no cobra command is registered. | [Plan](plans/2026-08-30-kno-status.md), section 2 and alternative D. A repo-wide grep found no CI check, workflow, script, plugin or doc that would call it, and "nearly free to add" prices the writing, not the keeping — pre-1.0 CLI surface becomes a post-1.0 covenant under CLAUDE.md's SemVer section. The asymmetry is decisive: adding a command later is cheap and additive, removing one post-1.0 is a major bump. The deferral may be wrong, because the evidence is an absence and absences are weak. | When a consumer is NAMED — a CI check, a Ring-2 plugin, or a user request in an issue. At that point it ships as a printer over `cli/status.go`'s existing struct and renderer. Reviewed at each minor release alongside this ledger. | @devarispbrown |
| <a id="86"></a>86 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `make status-check` still prevents the condition it describes and no PR has merged with a stale count, so the second leg cannot fire; 1.0 has not been cut. Still overlapping [#90](#90), and the two still collapse into one entry at repayment. Original entry follows. **`docs/status.json` appears in the diff of every ledger-touching PR**, because the debt counts move with the ledger. | [Plan](plans/2026-08-30-kno-status.md), accepted risk 2. Arguably correct — the count *did* change — and accepted over the alternative, which is a field nothing checks, which is a field that lies. The file is small and `make status` is one command. | **Before 1.0, or if a PR merges with a stale count, whichever is first.** The date leg is new (2026-08-31): `make status-check` now prevents the condition, so the original trigger could never fire. See also #90, which this overlaps — they collapse into one entry at repayment. | @devarispbrown |
| <a id="87"></a>87 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: there are still exactly two independent consumers, `make ledger-check` (via `.github/workflows/release.yml:124`) and `internal/cmd/status/main.go:108`. `scripts/ledger_check_test.py` also parses the live ledger, but only from inside `status-check`, so it is a third reader of the file and not a third caller with its own contract. This audit changed `scripts/ledger-check.py` — the release gate now reads a minor series, not only a patch-complete version — and added tests for that; the consumer count is unchanged. Original entry follows. **`scripts/ledger-check.py` now has two callers with different strictness.** The release gate tolerates a row it cannot parse (`skipped` is ignored); `make status-check` refuses to publish over one. One script, two contracts, and the module docstring's argument for narrowness now has an exception living next to it. | [Plan](plans/2026-08-30-kno-status.md), accepted risk 4. Accepted because the alternative is a second parser of the same 84-row hand-written table, which is strictly worse: two readers asking different questions would drift silently, because neither would fail when they disagreed. The strictness lives in the new consumer only, so the release path is untouched. | When a THIRD consumer of `docs/debt.md` appears. At that point the ledger stops being a Markdown table parsed by regex and becomes a data file with a rendered view — that is the real fix, and this defers it deliberately. | @devarispbrown |
| <a id="88"></a>88 | **REPAID** (2026-09-01, v0.2 ledger audit) — the consumer was built against the real file, which is exactly what this entry asked for. `uknoAI/kno-www` PR #13 (2026-08-31, "derive status and roadmap copy from docs/status.json") replaced all three hand-maintained copies: `src/lib/kno-status.ts`, `src/components/StatusBlock.astro`, `src/pages/index.astro` and `src/content/docs/roadmap.md` all read the artifact, and a code search of that repository for the literal `Status · v0` label now returns nothing. The shape fit without amendment — `schema_version` is still 1 — and the site pins the artifact per release (kno-www #14, #16, #17, #19). Tracking issue [#149](https://github.com/uknoAI/kno/issues/149) is still open and should be closed. Original entry follows. **The site contract is designed from the Kno side and the consumer has not been built against it.** `uknoAI/kno-www` still hand-maintains its status copy in three places: `src/content/docs/roadmap.md`, `src/content/home/home.yaml`, and a literal `Status · v0.1.1` label in `src/pages/index.astro`. | [Plan](plans/2026-08-30-kno-status.md), accepted risk 3 and section 6. "Astro can read this JSON" is not the same as "this JSON is the shape the roadmap page wants" — the three copies it replaces carry prose and ordering the artifact does not. The ordering constraint is real: the JSON has to ship first so the site change has something to build against. | Before the `kno-www` PR is written — build the roadmap section against the real file and amend the plan if the shape does not fit, rather than bending the JSON in review. Tracked as an issue on this repo. | @devarispbrown |
| <a id="89"></a>89 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `schema_version` is still 1 and has never been bumped — its only commit is cb50d5b, which introduced it — so no bump has reached `main` with or without the cross-repo issue. Original entry follows. **`schema_version` depends on a human filing an issue in another repo.** The bump protocol in CONTRIBUTING.md makes the cross-repo notification a deliverable of the bumping PR, but nothing mechanical enforces it — this repo cannot fail its own CI on the state of `kno-www`. | [Plan](plans/2026-08-30-kno-status.md), accepted risk 7. The mitigation is that the failure is loud on the other side — the site build refuses an unrecognised `schema_version` — and never an outage, because the site fetches at a release tag and keeps rendering the last shape it understands. | The first time a `schema_version` bump reaches `main` without the `kno-www` issue, the protocol moves from a PR convention to a check: a CI step asserting that a changed `schema_version` is accompanied by a linked cross-repo issue. | @devarispbrown |
| <a id="90"></a>90 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no conflict in `docs/status.json` has been counted in this release cycle, and 1.0 has not been cut. Still overlapping [#86](#86). Original entry follows. **The `docs/status.json` merge-conflict procedure is a documented convention, not a mechanism.** A contributor can still hand-resolve the file into nonsense. | [Plan](plans/2026-08-30-kno-status.md), accepted risk 8. `make status-check` inside `make check` catches it, so the wrong answer cannot survive review — but only after the effort is wasted. A `.gitattributes` merge driver was considered and rejected: repo-global machinery a contributor cannot see working, for a file whose correct resolution is one command. | **Before 1.0, or if conflicts in this file recur more than once in a release cycle, whichever is first.** The date leg is new (2026-08-31): the count was never counted by anything. Overlaps #86; the two collapse into one entry at repayment. | @devarispbrown |
| <a id="91"></a>91 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: 1.0 has not been cut, and nothing on CI times `make docs`, so the 60-second leg cannot report itself. Worth stating plainly at this audit: that leg is weaker than it reads — a stated number nobody measures — and this row is carried on its date leg, not on the threshold. Original entry follows. **`make status-check` sits in `make docs`, late in a chain CLAUDE.md wants ordered fail-fast-cheapest-first.** A status drift is discovered after lint and test have run. | [Plan](plans/2026-08-30-kno-status.md), accepted risk 6b. It is cheap — one Go program plus one `python3` invocation — and `make docs` is where the repo already keeps generated-artifact agreement checks, so a reviewer finds it where they expect it. Same concern [#72](#72) records for the release gates. | **Before 1.0, or when `make docs` exceeds 60 seconds on CI, whichever is first.** The threshold is new (2026-08-31): the original — 'past a point where contributors start skipping `make check`' — was unmeasurable by construction, so nothing could ever check it. Sixty seconds is a stated number that can be wrong and revised, which is the property the original lacked. | @devarispbrown |
| <a id="130"></a>130 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `go.mod` is still `github.com/knograph/kno`, no `github.com/uknoAI/kno` import exists anywhere in the tree, module resolution through the org-rename redirect has not broken, and the 1.0 decision has not been taken. Original entry follows. **The Go module path is `github.com/knograph/kno` and it stays, through the org rename to uknoAI (2026-08-29).** Every URL, repo ref, and org name in this repo now names `uknoAI` — install.sh (`REPO`, raw URL), `.goreleaser.yaml` (brews owner/homepage, cosign `--repo`/`--signer-workflow`/certificate-identity regexp), the release workflow's tap PAT comment, `README.md`/`SECURITY.md`/`CONTRIBUTING.md`/`CODE_OF_CONDUCT.md`/`DESIGN.md`/`CLAUDE.md`, the changelog's links, and the NOTICE/LICENSE copyright lines — but `go.mod`, `tools/go.mod`, every Go import, the `buf.gen.yaml` generated-code module value, and the goreleaser ldflags (`-X github.com/knograph/kno/cli.version` and siblings) still name the old org. Renaming the module path is deliberately NOT part of the rename PR: v0.1.0 is published under it, external consumers import it, and GitHub's org-rename redirect serves it (module resolution via the redirect keeps `go get`/`go install` working). A module path is a published identity, not a cosmetic string — changing it silently would strand every pinned consumer and break the v0.1.0 covenant. This entry records the decision so a future PR does not smuggle the rename in by accident. | The org-rename PR ("chore: rename org references to uknoAI"), which touched every other knograph reference in the repo. The module path is the one identifier that outlives the rename, and the redirect is what makes keeping it sound rather than merely convenient. | **If module resolution via the redirect ever breaks, or at the 1.0 decision** — whichever is first. The action in either case is rename-the-module-path (to `github.com/uknoAI/kno`, with a published mapping and `replace` guidance for pinned consumers) or record why not, in writing. | @devarispbrown |
| <a id="131"></a>131 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: nothing populates `Score.components`. `goal/exactmatch/exactmatch.go:47-54` sets `CaseId`, `Value`, `Passed` and `Rationale` and no components; `goal/registry.go:60-62` still allowlists exactly one Goal; and although `judge/` grew sixteen files with `judge calibrate` (#177), none of them implements `core.Goal`. `cli/evalinspect_json.go:16-18` says the same thing in code. Original entry follows. **`kno eval inspect` reports no score decomposition, because none is computable.** The command's product spec asked for `"overall_quality" accounts for 62% of total score`. `Score.components` exists (`proto/kno/v1/case.proto` field 4) and the full `Score` proto is persisted as `score_proto` (`store/sqlite.go`, `RecordOutcome`) — but **nothing in the tree writes a component**: `goal/exactmatch` sets `Value`, `Passed` and `Rationale` only, `resolveGoal` accepts exactly one name, and `judge/` contains one file (`doc.go`). A run has one Goal emitting one number, so there is no score to take a percentage of. `inspect` prints Case-share concentration instead, which measures the same "one giant score" anti-pattern from data that exists. | [The `kno eval inspect` plan](plans/2026-08-30-kno-eval-inspect.md), section "The line that isn't computable". Printing a score decomposition derived from nothing would be the exact failure the command exists to criticize. Two things must land for the original line to become honest: a composite Goal that populates `Score.components`, and a store reader that surfaces them per run. Both belong to the judge milestone (`judge calibrate`, DESIGN.md v0.2). | **When the first Goal that populates `Score.components` merges.** A merge event a reviewer can check, not a date. At that point `inspect` gains the decomposition line, or records why the number still is not honest. | @devarispbrown |
| <a id="132"></a>132 | **Partly repaid** (2026-09-01, v0.2 ledger audit) — the headline defect was fixed ahead of the trigger: #183 (5cf4ab2) made `renderTuningSet` emit an assistant turn or refuse outright, with `errUntrainable` for empty content and for chat JSON carrying no assistant message (`core/export.go:375-394`, `:421-446`), and `cli/export.go:46-47` no longer claims the artifact is trainable today. What this entry actually asked for is still undone: `tuningExample` is Kind-agnostic, so a knowledge Asset explicitly routed to `TUNING_SET` is wrapped as a bare assistant turn rather than rendered per Kind or refused, and "what a tuning_set entry means per Kind" has not been decided. Carried on the same trigger, which has not completed: PR #184, the first Tuner PR, is open and not merged. Original entry follows. **`kno export --destination tuning_set` writes a file no provider can train on.** `core.renderTuningSet` (core/export.go:360) emits one `user` message per selected Asset and nothing else. A fine-tuning example needs a target completion; a file of user-only turns trains against nothing, and every hosted FT API either rejects it or silently learns no mapping. The select/export plan named this artifact "OpenAI chat format JSONL — the file the Tuner adapters will parse", so the shape is advertised and the content is unusable. | Found by the v0.2 Tuner Phase-0 workstream while reading its own input format. Not a rendering slip: what the assistant turn SHOULD contain is a real design question. A behavior Asset may carry a demonstration with both sides; a knowledge Asset is not a chat example at all, and pairing it with a fabricated completion would be inventing training data. Fixing it means deciding what a tuning_set entry means per Kind, which is plan work, not a patch. | **The first Tuner adapter PR**, which is the moment the file acquires a real consumer and the question stops being hypothetical. That PR either renders a trainable example per Kind or refuses the Destination for Kinds it cannot render. Until then the docs must not claim the artifact is trainable. | @devarispbrown |
| <a id="133"></a>133 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `core.destinationFor` (`core/select.go:597-607`) still returns TUNING_SET for behavior Kind and CONTEXT for everything else, leaving `DESTINATION_KNOWLEDGE_BASE` reachable only from an explicit per-Asset declaration. The knowledge-injection PR has not been written. Original entry follows. **`DESTINATION_KNOWLEDGE_BASE` is unreachable by routing.** `core.destinationFor` (core/select.go:573) returns TUNING_SET for behavior Kind and CONTEXT for everything else; the knowledge_base Destination is assigned only when an Asset declares it explicitly on itself. So the Destination that v0.2 makes WRITABLE has, in practice, no input — a user who never sets the field on their Assets can select and export forever without producing one. | Found by the v0.2 knowledge-injection Phase-0 workstream, which needs this Destination to have contents before it can write them anywhere. Harmless in v0.1, where knowledge_base emits a manifest a human applies: an empty manifest is merely useless. It stops being harmless when Kno writes into a live KB, because the feature would appear to work and move nothing. | **The knowledge-injection PR**, which must either add a routing rule (knowledge Kind with a retrieval-shaped Goal?) or state plainly that the Destination is opt-in per Asset and say so in the CLI help. | @devarispbrown |
| <a id="134"></a>134 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired, and checked by hand at this audit precisely because nothing checks it automatically: all 27 recipe links in `docs/cookbook/README.md` resolve against `uknoAI/kno-examples/recipes/` today. (Seven recipes now exist there that this index does not name — `mine-your-transcripts`, `orchestration`, `power-and-sample-size`, `what-it-costs`, `why-not-diy`, `analyze-in-a-notebook`, `from-your-warehouse` — which is staleness in the other direction and not what this entry tracks.) 1.0 has not been cut. Original entry follows. **The cookbook index's outbound links are not link-checked from either side.** `docs/cookbook/README.md` now points at `uknoAI/kno-examples` over `https://`, and `make docs`' link checker `continue`s on every `http://`/`https://` target by construction — so this page can name a recipe that has been renamed or deleted and nothing here will fail. The twenty-five tombstone stubs carry the same exposure, and `scripts/cookbook-stub-check.sh` checks only their *shape*: that each is one line naming its own recipe, never that the URL resolves. | [The examples-repo plan](plans/2026-08-30-examples-repo.md) §1 and §8. Checking external links from this repository would put a network call in `make docs` and make a local `make check` fail on someone else's outage — which is how a gate becomes something people route around. The check belongs on the other side, where the recipe files are. | **When a recipe is first renamed or removed in `kno-examples`, or before 1.0 — whichever is first.** Repay with an external-link check in `kno-examples`' nightly that validates this pointer page and the stub set from the other side, filing a `docs-drift` issue here on a miss. | @devarispbrown |
| <a id="135"></a>135 | **REPAID** (2026-09-01, v0.2 ledger audit) — `uknoAI/kno-www` PR #15 (2026-08-31, "point recipe links at kno-examples instead of the tombstones") re-pointed them, and `kno-examples` #7 checked it off from the other side. Verified from here rather than taken on trust: a code search of that repository for `docs/cookbook` returns 0 hits, and for `kno-examples` returns 20. Original entry follows. **`uknoAI/kno-www` holds 22 branch-pinned links into `docs/cookbook/*.md` that now resolve through a tombstone.** They work — that is what the stubs are for — but a link to a one-line redirect is worse prose than a link to the page, and the site's own Playwright crawl skips external hrefs (`tests/e2e/links.spec.ts:31`), so nothing on either side reports the indirection. | [The examples-repo plan](plans/2026-08-30-examples-repo.md) §8 *(F1, F3)*. The plan's original sequencing assumed a possible build break in `kno-www` and put its PR first; reading the repository answered that — every content collection globs `src/content/**` and nothing sources cookbook prose — so the site cannot break and the coordinating PR is a quality step that may follow. It is deliberately not in this PR: two repositories merging in one window is more expensive than one, and the stubs make it unnecessary. | **Before the next release after this one.** The `kno-www` PR re-points all 22 references at `kno-examples`, and lapses observably: the links are enumerable with one `git grep` in that repository, so "did it happen" is a checkable question rather than a memory. | @devarispbrown |
| <a id="136"></a>136 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: the recipes were published 2026-08-31, so no `last-manual-verification` is anywhere near 180 days old, no staleness banner has rendered, and certainly not for two consecutive quarters. 1.0 has not been cut. Original entry follows. **Vendor recipes are verified by a human on a cadence nothing enforces.** Sixteen `flags-only` recipes in `kno-examples` have their command shapes checked nightly against the released binary and their vendor halves checked by nobody, on no schedule. The page renders a staleness banner once its `last-manual-verification` is 180 days old; a banner reports the rot, it does not prevent it. | [The examples-repo plan](plans/2026-08-30-examples-repo.md) §3. Nightly runs with real credentials were rejected on principle and on cost: unattended spend violates prime directive 4's spirit, it needs long-lived credentials for eight third-party SaaS products in a public repo's CI, and it would redden on every vendor's outage — training everyone to ignore the signal. `vendor-smoke` (workflow_dispatch, environment approval, three independent spend caps, date written back by machine) buys most of the truth at none of that risk. | **Before 1.0, or when any vendor recipe's staleness banner has rendered for two consecutive quarters — whichever is first.** Repay by running `vendor-smoke` across the set and recording the result, or by demoting the recipes that nobody will own. | @devarispbrown |
| <a id="137"></a>137 | **REPAID** (2026-08-31), **and reopened and repaid again the same day** — the first repayment fixed half the defect while its own note named the other half. It reads "repaid ahead of its trigger because `validate` shipped as a second resuming spend stage on the same `SettledSpend` path", and then changed only `core/value_loop.go`. `core/validate_loop.go`'s `RecordMeasurement` was still writing `budget.Spend{Calls, CostUSDMicros}`, so a resumed Validate run restored zero tokens exactly as a resumed Value run had. Found by reading the two sinks side by side while scoping the holdout canary, not by any gate: `TestValidateMeasuresBothArmsOverTheHoldout` compared cost and calls and skipped tokens, and the scripted agent declared `TokenCounts: true` while returning none, so the dimension was unobservable in the stage that had it wrong. Both are fixed; the assertion is verified failing with the one line reverted, reporting "result spend {Calls:40 Tokens:400} != settled spend {Calls:40 Tokens:0}". The lesson worth keeping is the shape: a defect named "the Value sink drops tokens" got repaid as literally as it was written, and the second instance survived because the entry was closed on the wording rather than on the invariant. Original repayment note follows. **REPAID** (2026-08-31). `core/value_loop.go`'s sink now computes `Tokens` from the `Response` exactly as Baseline's `settledSpend` does, so the measurements table and the outcomes table agree about what the same kind of call cost. The pin lives where the bug bit: `TestResumeDoesNotRePay` already asserted that dollars do not double across a resume, and now asserts the tokens came back at all — verified failing with the one line reverted, reporting "zero tokens over 3 calls costing 3000 micros". Repaid ahead of its trigger because `validate` shipped as a second resuming spend stage on the same `SettledSpend` path, which doubled the surface a token cap could be under-enforced on. Original entry follows. ~~**`Store.SettledSpend` loses a Value run's token count, so a resumed Value run's guard restores zero tokens.** `core/value_loop.go`'s `sinkFunc` records `budget.Spend{Calls, CostUSDMicros}` and drops `Tokens`; Baseline's `settledSpend` (`core/baseline_budget.go:452-463`) writes all three from the Response. Consequences: `kno report` can never show a Value run's tokens, and — the part that is a defect rather than a reporting gap — `Guard.Restore` seeds a resumed Value run with `Tokens: 0`, under-enforcing a token cap across a resume. | Found by writing `TestValueResultSpendEqualsSettledSpend` in [the stage-spend-reporting plan](plans/2026-08-31-stage-spend-reporting.md) (AC1), whose three-dimension equality assertion failed on `main`'s recording behavior rather than on anything that PR changed. Not fixed there: changing what a stage persists about money is a `core` budget change needing its own plan, its own resume test and its own review, not a hunk inside a rendering PR. That PR's own surfaces are unaffected — the in-process block reads `Guard.Spent()`, whose token count is correct, and `report`'s entries carry no token field. The test compares calls and cost exactly and logs loudly if the divergence changes shape, so the allowance is removed deliberately rather than forgotten. | **Before the next minor release, or the first time a token cap is set on a Value run that resumes — whichever is first.** Repay by recording tokens in the Value sink and widening AC1's assertion to all three dimensions. | @devarispbrown |
| <a id="138"></a>138 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `core/value_loop.go:1059-1064` still writes a `CaseExecution` with no `UsageEstimatedCaseCount` while `core/baseline_close.go:222-242` sets it; no adapter that cannot report token usage has been measured with `kno value`, and 1.0 has not been cut. Original entry follows. **`value` records no `usage_estimated_case_count`, so the estimated-usage qualifier is structurally dead on the expensive stage.** `core/value_loop.go`'s close writes a `CaseExecution` with attempted / scored / errored / resolved models and no usage-estimated count; only `core/baseline_close.go` writes one. A Value run priced from the engine's estimate rather than from reported usage says so nowhere, in either rendering. | [The stage-spend-reporting plan](plans/2026-08-31-stage-spend-reporting.md), accepted risks. That PR renders the field wherever it is recorded and deliberately does not start recording it: aggregating it for measurements needs a store query and a `core` change, and bundling it into a rendering PR is how a rendering PR becomes unreviewable. The renderer takes the number the day it exists — `spendLines` and `newSpendReport` already accept it. | **Before 1.0, or when the first provider adapter that cannot report token usage is measured with `kno value` — whichever is first.** | @devarispbrown |
| <a id="139"></a>139 | **RE-DATED — the trigger fired at this release and the work is not done** (2026-09-01, v0.2 ledger audit). Spend reporting shipped in #168 and released in v0.1.4; "within one minor release" is this one. Verified against the published file rather than assumed: `uknoAI/kno-examples/recipes/ci-gate.md` still teaches the pre-`guarded` idiom — it mentions neither `guarded` nor `spent_usd_micros`, and it still prints a `$0.00` spend figure. Recorded as the slip it is rather than carried silently. The repayment is a one-file edit in another repository, which this repo can neither land nor gate, so the new trigger names that file: "did it happen" stays a one-command question, and the release leg is a hard backstop the gate can read. Original entry follows. **The `kno-examples` CI-gate recipe still teaches the pre-`guarded` `--json` idiom, and the `--json` stability sentence it used to carry now points at nothing citable.** `docs/cookbook/ci-gate.md` became a one-line tombstone in #163, taking with it the repository's only statement of the `--json` stability promise; the surviving copy lives in `uknoAI/kno-examples` and cannot be edited from here. Until it is updated, the published example of a CI cost gate does not mention `guarded`, does not sum `spent_usd_micros`, and may still show `// 0` — the repair that reinstates the metered-zero ambiguity. | [The stage-spend-reporting plan](plans/2026-08-31-stage-spend-reporting.md), Phase-2 amendment and AC17. [ADR-0006](adr/0006-the-json-contract.md) is the in-repo mitigation and now holds the contract, the `select(.guarded)` idiom and the explicit warning against `// 0`. The recipe edit lands in a different repository with its own CI and cannot be made atomic with this one. | **The next `uknoAI/kno-examples` PR that touches `recipes/ci-gate.md`, or v0.3.0 — whichever is first.** (Re-dated 2026-09-01 after the original fired unpaid at this minor release; the What column records that lapse. The reason for the move, stated rather than implied: the edit lands in a repository this one can neither land nor gate, and the original's second leg — a CI-gate run reporting a spend figure from an unmetered stage — is an observation nobody is watching for.) | @devarispbrown |
| <a id="140"></a>140 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no real run has reached a `holdout_uses` count of 3 for any eval fingerprint, and 1.0 has not been cut. The disclosure is in place as described — `HoldoutUseIndex` is recorded (`core/validate_loop.go:579`) and rendered (`cli/validate_render.go:116`) — and no interval is widened for it. Original entry follows. **Repeated holdout use is counted, not corrected.** Validating N Portfolios against one holdout re-introduces exactly the multiplicity the holdout existed to remove, at rate N. `--allow-repeat-holdout` records the ordinal on `Validation.holdout_use_index`, every rendering discloses it, and the interval is not widened for it. | [The Validate plan](plans/2026-08-31-validate.md) §3 and §8. Refusing a second Portfolio outright is statistically the purest answer and was rejected: the user's next move is `rm kno.db` or `--split-seed`, which converts a counted second peek into an invisible one. Every mechanism in this repo prefers a labelled compromise to an unenforceable prohibition — `split.MinHoldout`'s own godoc makes the same judgement. The truth is stated in the report and in [what the numbers mean](what-the-numbers-mean.md) rather than enforced. | **When a `holdout_uses` count reaches 3 for any eval fingerprint in a real run, or before 1.0.** The first leg is observable from data validate now collects, so it cannot wait on itself. | @devarispbrown |
| <a id="141"></a>141 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no Validate run has reported a holdout interval entirely below the dev estimate's interval low, and 1.0 has not been cut. One precision fix for the record: `interaction_penalty_detected` is a field on the CLI's JSON structs (`cli/jsonreport.go:357-363`, `:734-737`), not on the persisted `Validation` proto, which has no such field. It is unconditionally false either way, which is what the entry is about. Original entry follows. **Pairwise interaction detection is not shipped**, against `DESIGN.md:88`. `Validation.interaction_penalty_detected` is `false` unconditionally and no suspect pairs are emitted. This is a scope reduction against DESIGN, flagged rather than silently resolved. | [The Validate plan](plans/2026-08-31-validate.md) §7. The tempting rule — flag an interaction when `holdout_gain < dev_estimated_gain` — fires at roughly 100% under the null, because dev inflation is the expected state of a correct pipeline; a detector with that positive rate is not a detector. The honest version is leave-one-out on the holdout: `k+1` arms for a `k`-Asset Portfolio, 520 calls against this stage's 80 for a 12-Asset set on a 40-Case holdout, a 6.5x increase for a secondary signal. What ships instead is both numbers side by side with their difference labelled `shrinkage` and one sentence saying shrinkage is expected. | **When a Validate run reports a holdout interval entirely below the dev estimate's interval low, or 1.0, whichever is first.** The first leg is a real signal that interactions are biting, observable from data this stage starts collecting. | @devarispbrown |
| <a id="142"></a>142 | **CARRIED, trigger restated** (2026-09-01, v0.2 ledger audit) — examined and unfired: no writable knowledge-base adapter exists, the knowledge-injection plan has no PR, and `core.destinationFor` still routes nothing to that Destination (see [#133](#133)), so a knowledge arm has nothing to measure with. The trigger's "(v0.2's other half)" parenthetical is dropped, and the reason is worth stating because this audit also fixed the gate that surfaced it: `scripts/ledger-check.py` matched a full version string only, so it read `0.2.0` and was blind to `v0.2` — and blind to `before 1.0`, which is how most of this table is written. With that fixed, the parenthetical read as a claim that this row is due at the 0.2.0 tag, which the entry does not mean; the obligation is the adapter landing, and that is a merge event a reviewer can check. Original entry follows. **The holdout gain is a context-injection upper bound**, inheriting the whole caveat from *Context injection is an upper bound*. The treatment arm puts the Portfolio in the prompt, so the number is a ceiling on what a retriever would deliver, not a deployment prediction. Knowledge- and tuning-Destination entries are refused rather than approximated. | [The Validate plan](plans/2026-08-31-validate.md) §1 and accepted risks. Measuring a knowledge Destination faithfully needs an agent whose index Kno can write, which is the other half of v0.2 and does not exist yet. Approximating it with context injection would produce a deployment-shaped number that is not a deployment prediction — the exact confusion `docs/what-the-numbers-mean.md` exists to prevent. Refusal is the honest state until the adapter lands. | **The first writable knowledge-base adapter — the knowledge-injection PR ([plan](plans/2026-08-31-knowledge-injection.md)).** At that point validate gains a knowledge arm and this row is repaid or restated against what that arm actually measures. (Restated 2026-09-01: the scheduling parenthetical naming the next minor milestone is dropped, because the release gate now reads a minor series and would have read that parenthetical as a deadline this entry never claimed. The reasoning is in the What column.) | @devarispbrown |
| <a id="143"></a>143 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: neither the knowledge nor the tuning arm exists, so a whole-Portfolio number is still not computable and `--context-only` (`cli/validate.go:134`, refusing mixed Portfolios at `core/validate.go:470-472`) is still the only way to get a partial one. Original entry follows. **`--context-only` produces a number about a subset.** A mixed-Destination Portfolio is refused; the flag validates the context-Destination entries alone. It is labelled a subset everywhere it appears, and a labelled subset number is still a number someone can quote out of context. | [The Validate plan](plans/2026-08-31-validate.md) accepted risks. The alternative to the flag is refusing mixed Portfolios with no escape hatch, which blocks a user who wants the honest partial answer about the half of their Portfolio that is validatable today. Labelling was chosen over refusal for the same reason as entry 140: an unenforceable prohibition routes people around the tool. | **When both other Destinations are validatable** — that is, when the knowledge and tuning arms exist and a whole-Portfolio number is computable, so the subset flag stops being the only way to get one. | @devarispbrown |
| <a id="144"></a>144 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: entry [#65](#65)'s uniform-effect scaling model has not been replaced, so the two figures are still not comparable. `shrinkage` is reported in line and in `--json` exactly as described (`cli/validate_render.go:97`, `cli/jsonreport.go:352`). Original entry follows. **The dev/holdout comparison is between two estimands, not two measurements of one.** `dev_estimated_gain` is a dev-population figure built from per-entry deltas scaled by `PortfolioEntry.n_routed_scale` under the uniform-effect model entry 65 names; `holdout_gain` is a direct, unscaled measurement of the whole set over every holdout Case. Their difference is reported as `shrinkage` and is not a bias estimate. | [The Validate plan](plans/2026-08-31-validate.md) §8. Stated in the report line and in [what the numbers mean](what-the-numbers-mean.md) rather than resolved, because resolving it means measuring the Portfolio on the dev slice unscaled — another full arm's spend, on the slice whose number is already known to be inflated. Named as the single most likely misreading of the stage, which is why it is printed in line rather than left in a doc. | **When entry 65's uniform-effect scaling model is replaced.** A real per-slice effect model makes the two figures comparable, and that is the PR where this row is settled rather than restated. | @devarispbrown |
| <a id="145"></a>145 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired on both legs: no `holdout_uses` count has reached 3, and no issue reports a validate quote declined on cost. The control arm is still measured fresh on every validation (`core/validate_loop.go:75,109`), with no cache or replay path anywhere in the stage. Original entry follows. **The control arm is re-measured for every validation, at 2x the naive call count.** The cached-control design — record the un-injected arm once per `(eval_fingerprint, agent_ref, resolved_model, trials)` and replay it as the paired baseline of later validations — would halve the marginal cost of every validation after the first (40 calls to 20 at `MinHoldout`, 480 to 240 at a 400-Case, 3-trial scale). It is deferred, not rejected. | [The Validate plan](plans/2026-08-31-validate.md) §1 (F4) and Alternatives considered. Three reasons in damage order: a replayed control breaks the within-run pairing that makes the interval honest and reintroduces exactly the provider-drift gap used to reject `kno baseline --holdout`, which would be incoherent; it pays only on the repeat-holdout path §3 already discourages, so it optimizes the path the design discourages; and its invalidation rule spans the agent ref, the resolved model and `trials`, where a stale-control bug does not crash but produces a wrong headline number, in the one place this product promises a right one. | **When a `holdout_uses` count reaches 3 for any eval fingerprint in a real run — the same signal that fires entry 140 — or the first issue reporting a validate quote declined on cost.** | @devarispbrown |
| <a id="146"></a>146 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no PR has added an indirection through which `openHoldout` could be reached, and 1.0 has not been cut. `TestOnlyValidateOpensTheHoldout` is still the bespoke AST walk (`core/holdout_boundary_test.go:39-68`, with the walk at `:262-287`). Two commits since validate landed touch holdout tests and neither touches this one: 226924a (#171) changes `TestSelectHoldoutCanary`, and 727caa9 (#179) scopes a different test's filesystem walk in the same file. Original entry follows. **`TestOnlyValidateOpensTheHoldout` is a bespoke AST test, not a toolchain assertion.** It parses `core`'s own non-test files and counts call expressions inside one package, where the import graph says nothing. It can in principle be evaded by an alias, a method value, or a wrapper the walk does not model, and it is the walk itself — not the toolchain — that a reviewer has to trust. | [The Validate plan](plans/2026-08-31-validate.md) §2 (F2), recorded in [ADR-0007](adr/0007-the-holdout-opener-is-unexported.md). It does not inherit `core/boundary_test.go`'s robustness and no longer claims to: that test shells out to `go list -deps` and asserts over a coarse, total relation computed by the toolchain. This one is the only mechanism available for an in-package boundary, so it is worth having; its own correctness is pinned by falsification cases rather than by precedent. | **Any PR that adds an indirection through which `openHoldout` could be reached, or 1.0.** The fourth check — no exported `Evals` outlet in `core` — fails on such a PR by construction, so the trigger announces itself. | @devarispbrown |
| <a id="147"></a>147 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `adapters/evals/split/split.go`'s last commits are 957ba8f and d568a82, both 2026-08-28 and both before validate landed (e13b558, 2026-08-31). Nothing has touched the hashing since. Original entry follows. **The holdout split is recomputed, not persisted.** Which Cases are in the holdout rests on `split.AssignSplit`'s determinism plus the eval fingerprint check, not on a stored membership list. A change to `splitResolution` or to the hash function would silently redefine every holdout, including ones already validated against. | [The Validate plan](plans/2026-08-31-validate.md) accepted risks. Persisting membership means storing a Case-ID list per eval fingerprint and reconciling it on every open, which is a second source of truth that can disagree with the first — and the failure it prevents is one nobody has yet had. The fingerprint check catches an edited Pool or eval file before any spend; it does not catch a changed hash function, because the fingerprint is over content, not over the splitter. | **Any PR touching `adapters/evals/split/split.go`'s hashing.** That PR either persists membership or states, in its own body, why every existing holdout may be redefined. | @devarispbrown |
| <a id="148"></a>148 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: `knov1.Report` is still constructed nowhere outside `gen/` — the only other literals are schema assertions in `internal/schema/schema_test.go` — and `serve` is v0.3. This trigger names v0.3 and the release gate now reads a minor series, so a 0.3.0 tag will refuse this row without a disposition. Original entry follows. **`knov1.Report` still has no producer.** The Validate stage adds a `Validation` message and a `validations` table; it does not fix the fact that `Report` is aliased at `core/types.go` and constructed nowhere outside `gen/`. `kno report` renders from the runs it names, not from a persisted `Report`. | [The Validate plan](plans/2026-08-31-validate.md) Problem §4 and Alternatives considered. Reusing `Report` as validate's persisted output was rejected: it carries portfolio, gaps, agent, goal, total cost and Case counts validate does not compute, so validate would either fabricate them or write a `Report` that is mostly absent — and `Report`'s `optional` fields exist precisely so that absence means *not run*, which a half-written `Report` corrupts. | **The v0.3 `serve` PR**, which needs a Report on the wire and therefore needs something to construct one. | @devarispbrown |
| <a id="149"></a>149 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no real-provider run has reported `n_dropped` above zero, and 1.0 has not been cut. The disclosure is in place (`core/validate_measure.go:118-119`, surfaced at `core/validate_loop.go:609-610`) and `RecordMeasurement` is still `INSERT OR IGNORE` (`store/measurement.go:245-256`). 14291c8 (#172) changed the resumed-token accounting on this path, not the drop accounting. Original entry follows. **A holdout Case whose provider call was charged and then cancelled is lost to that Portfolio permanently.** Validate's sink follows Value's rule — a terminal outcome is a done-marker, so a resume never re-pays — and the measurements table is `INSERT OR IGNORE`, so a re-attempt's answer would be discarded anyway. The consequence is specific to this stage: the one-shot rule refuses a fresh run, so the Cases in flight at a Ctrl-C are subtracted from the holdout the headline number is computed over, for good. Validate already departs from Value for the harmless half of this — a stop that settled NOTHING writes no row, so the resume measures the Case for the first time — and cannot for the half that cost money. | [The Validate plan](plans/2026-08-31-validate.md) §4 and criterion 7. Found by writing `TestInterruptedValidateStillConsumesTheHoldout`, whose mid-run variant measured 7 of 8 Cases on a clean resume. Not fixed here: re-attempting a charged Case means either paying twice for one answer or making `RecordMeasurement` replace rather than ignore, and changing that write's idempotency is a store-level money change needing its own plan and its own resume test — not a hunk inside the stage that discovered it. What ships instead is disclosure: `measured_case_count` and `n_dropped` sum to the holdout size, both are printed beside the gain and both are in `--json`, and a test asserts the sum so a Case can never vanish from the denominator silently. | **Before 1.0, or the first time a Validate run reports `n_dropped` above zero on a real provider — whichever is first.** Repay by making the interrupted-and-charged case re-measurable (a replacing write plus the spend accounting that goes with it), or by refusing to write a Validation whose measured count is short and requiring the operator to decide. | @devarispbrown |
| <a id="150"></a>150 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired, and this is the row to read first at every release because it is prime directive 4 in the abstract: `goal/registry.go:60-62` still allowlists exactly `exact-match`, which is arithmetic and calls no provider, and `Score` still runs outside the reservation bracket in both callers (`core/baseline_invoke.go:78`, `core/value_loop.go:834`, against the bracket at `core/invoke.go:238-269`). No judged Goal has been registered, so the trigger has not been approached. Original entry follows. **`core.Goal.Score` runs OUTSIDE the budget reservation.** `core/invoke.go` brackets the agent call — Authorize, Invoke, Settle — and `Score` runs after the reservation has settled, in both callers (`core/baseline_invoke.go`, `core/value_loop.go`). `Estimator` further requires `Estimate.Calls == 1` per `Invoke`. A Goal that calls a provider inside `Score` therefore spends money the guard never authorized and never settles: prime directive 4, verbatim. Contained, not fixed: `goal.Registry` is **default-deny** — a Goal whose name is absent from `goal/registry.go`'s self-contained allowlist cannot register, whatever it does or does not declare about itself. | [Plan](plans/2026-08-31-judge-calibrate.md), finding F1. A must-affirm marker method was rejected: it is still a self-report, so it catches the author who forgot and admits the one who writes `SelfContained() { return true }` on a Goal that calls a provider. `TestUnmarkedLLMBackedGoalIsRefused` and `TestSelfDeclaredGoalIsStillRefused` assert refusal in both shapes. Fixing the seam means a second reservation around `Score` or a `Goal`-side cost surface, both `core` changes with an event and a settlement story. | **Before any judged Goal is added to `goal.Registry`** — mechanically the same edit as adding a name to the allowlist, where this trigger is written in the comment on the line being edited. The trigger cannot self-satisfy: nothing registered today is judged. | @devarispbrown |
| <a id="151"></a>151 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no out-of-tree Goal has been requested, and [#150](#150)'s budget seam is unfixed, so the compile-time allowlist still has the reason to exist that this entry gives it. Original entry follows. **An out-of-tree Goal cannot register at all.** The allowlist in `goal/registry.go` is compile-time, so a Goal defined outside this module has no way to be admitted. | [Plan](plans/2026-08-31-judge-calibrate.md), finding F1, accepted cost. Goals are in-tree today — `goal/` holds one implementation — and the plugin boundary `CLAUDE.md` describes is Ring-2 adapters, not Goals. | **When an out-of-tree Goal is requested, or when entry 150's budget seam is fixed — whichever is first**, since fixing the seam is what removes this allowlist's reason to exist. Both halves move together or neither moves. | @devarispbrown |
| <a id="152"></a>152 | **CARRIED** (2026-09-10, this PR) — FIRED, inspected, and found not to engage this entry's concern: `goal/tokenf1.Goal` (`docs/plans/2026-09-10-graded-goal-token-f1.md`) is the first `SCORE_DOMAIN_UNIT_INTERVAL` Goal registered in `goal/registry.go`'s allowlist, and the trigger below reads that condition literally, so it fires on this PR — said plainly rather than edited around. `token-f1` is not a judge in the sense this entry is about: it has no prompt (`judge.PromptSHA` returns `judge.NoPromptSHA` for it, since it does not implement `judge.Prompted`), this PR authors no calibration Set or `--baseline` entry naming it, and `kno judge calibrate --all` only calibrates pairs a `--baseline` file lists (`cli/judge.go`'s `--all needs --baseline`), so it is not reachable from that command today. `judge/calibrate.go`'s existing `SCORE_DOMAIN_UNIT_INTERVAL` branch (`judge/verdict.go`'s `gradeAll`) already reports `Graded{WeightedKappa, Spearman, MAE}` with `VerdictNotApplicable` for any Goal on that domain, exactly as this entry intended — so registering `token-f1` required no change to `judge/`. Disposition: carried and re-dated, not repaid. The anchored-scale design work this entry names is for gating an LLM judge's fidelity against human labels — a different question from what a self-contained arithmetic Goal answers — and building it now, under this PR's pressure, to satisfy a literal trigger match would be inventing scope nobody asked for. The trigger is narrowed below accordingly, as a named judgment call a future reader can disagree with rather than a silently moved goalpost. Previous disposition follows. **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no Goal declaring `SCORE_DOMAIN_UNIT_INTERVAL` is registered. `exactmatch.Domain()` returns `SCORE_DOMAIN_BINARY`, and the unit-interval constant appears only inside `judge/calibrate.go`. Original entry follows. **Graded judges are reported and not gated.** A Goal declaring `SCORE_DOMAIN_UNIT_INTERVAL` gets weighted kappa, Spearman's rho and MAE, and `GATE: not applicable`. Kappa is undefined on continuous scores. | [Plan](plans/2026-08-31-judge-calibrate.md). Gating a graded judge needs an anchored scale the calibration record format does not carry, and inventing one here would be the invented threshold `kno judge calibrate` exists to avoid — the same defect `kno eval inspect` had a 25% threshold deleted for. | **When the first `SCORE_DOMAIN_UNIT_INTERVAL` Goal that has a prompt, is named in a `kno judge calibrate` `--baseline` file, or otherwise reaches that command, is registered** (narrowed 2026-09-10 — see the What cell: the original wording below fired literally on `goal/tokenf1`, a self-contained non-judge Goal, and was found on inspection not to engage this entry's actual concern). That PR adds the anchored scale to the record format and the gate that reads it. Original trigger, superseded: "When the first `SCORE_DOMAIN_UNIT_INTERVAL` Goal is registered." | @devarispbrown |
| <a id="153"></a>153 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired on both legs: the largest published calibration set is 60 records (`judge/testdata/calibration/starter/records.jsonl`; the straddle set holds 30), well under the 200-record leg, and no nightly disagreement has been reported. `stats/interval` still ships the percentile bootstrap only — `BCa` appears nowhere in the tree — and `TestBootstrapCoverageOnTheDecisionGrid` is at `judge/coverage_property_test.go:37`. Original entry follows. **The calibration gate rests on a percentile bootstrap, which is this repository's first.** `stats/interval` shipped adjusted-Wald, paired-t and sign intervals while `Interval.method`'s godoc already listed "bootstrap". Percentile intervals are known to under-cover for statistics whose small-sample distribution is skewed, and kappa near a hard decision boundary is that regime; under-coverage there flips a verdict rather than producing a visibly wrong number. Measured, not assumed: `TestBootstrapCoverageOnTheDecisionGrid` sweeps n ∈ {50,100,200} × kappa ∈ {0.55…0.65} and reads 0.923–0.970 against a nominal 0.95. BCa remains the named upgrade, not the ship. | [Plan](plans/2026-08-31-judge-calibrate.md), finding F2. The measurement is a synthetic-ground-truth simulation; the real record distribution is not simulated. | **When the nightly live calibration and the replayed kappa disagree by more than the paired CI, or when a calibration set exceeds 200 records — whichever is first.** BCa is re-costed then. | @devarispbrown |
| <a id="154"></a>154 | **CARRIED** (2026-09-01, v0.2 ledger audit) — unfired: no published kappa has had to be defended against a held-back private set, and 1.0 has not been cut. Original entry follows. **The published calibration set is contaminated for training purposes, and its records are synthetic.** Any judge model released after publication may have seen it, so it detects a prompt change making things worse and is not evidence that a judge generalizes. Its records are templated rather than drawn from production, because the set is public and traces are customer data. | [Plan](plans/2026-08-31-judge-calibrate.md), accepted risk 1. Stated in `docs/what-the-numbers-mean.md` rather than argued away. The mitigation available to a user is to add records from their own domain under the same invariants. | **Revisit if a held-back private set is ever needed to defend a published kappa — at 1.0 at the latest.** | @devarispbrown |
| <a id="165"></a>165 | **CUT from v0.2** (2026-09-01), and still OPEN — the hole is unrepaired rather than moot: the feature this entry blocks is not shipping, and PR #178 stays open as its record. [ADR-0008](adr/0008-measured-redundancy-is-cut-from-v0-2.md) is the decision. Condition 1 is unsound for this problem — `MinDetectableEffect` as the margin and `signBound` as the interval decay at different rates in `n`, so their ratio diverges (1.49x at n=6, 3.02x at n=40, 11.73x at n=640, 18.52x at n=1600, reproduced against the real functions) — and the fix is a redesign, not a patch. Three things measured since this row was last written. **Refusing the degenerate path outright** (one of the honest fix options named below) removes the small-sample failures at the trial count the acceptance test uses — `[0 0 1 1 1 3 4 0]` becomes `[0 0 1 1 0 0 0 0]` at 200 trials — but the same sweep at **4000** trials reads `[4 13 16 20 27 0 0 0]`, still rising as the shared slice shrinks, because at small `n` the OBSERVED spread has higher variance and a sample more often looks tighter than it is. **The acceptance test was underpowered to measure the property it exists to measure**, which is its own lesson and is recorded as one in the ADR. And **every fixture in the feature's suite that produces a measurement-decided REDUNDANT verdict passes the same delta vector on both sides**, so Condition 1's positive path is exercised almost entirely through the one input where a paired-difference interval carries no information about magnitude. Original entry follows. **Condition 1's equivalence claim is NOT monotonic in shared-Case count, contradicting acceptance criterion 4 of the redundancy-detection plan as literally stated — and the coordinator's proposed fix (refuse via `--redundancy-max-margin` whenever `MinDetectableEffect(n,...)` exceeds it) is ALREADY IMPLEMENTED (`core/redundancy.go`'s `cond1 := margin <= cfg.resolvedMaxMargin() && withinMargin(...)`), checked, and found insufficient.** `TestRedundancyMonotonicityUnderShrinkingOverlap` (200 seeded pairs) shows REDUNDANT counts `[0 0 1 1 1 3 4 0]` at shared-Case counts {40,30,20,15,10,6,5,4} with the ceiling at 0.90. **Checked against #182** (the degenerate-variance-guard fix): counts are byte-identical before and after, because this test's deltas are `{-1,0,+1}` — always exactly representable — so `allIdentical` and the old `variance <= 0` guard agree on every window and #182 changes nothing here. **Checked against tightening the existing ceiling** (a full sweep at ceilings 0.10/0.15/0.20/0.25/0.30/0.35/0.50/0.90, 200 trials each, all 8 vectors logged in `TestZZCeilingSweep` before removal): AC4 holds only at ceiling <= ~0.25-0.29, and ONLY because that admits solely `n=40` (the largest size swept) while refusing everything smaller as UNKNOWN — not a structural fix, a razor-thin window. **The root cause, now proven structural rather than probabilistic**: `MinDetectableEffect(n,...)` decays as `~1/sqrt(n)` while `signBound`'s width (`-log(1-level)/n`) decays as `~1/n` exactly, so their RATIO (margin/width) GROWS WITHOUT BOUND as `n` grows — measured `3.02x` at `n=40` up to `18.5x` at `n=1600`. No ceiling value, however tightened, closes this gap for all `n`; a tighter ceiling only shrinks the PROBABILITY that a given admitted window happens to show a coincidental tie, for whichever window sizes it happens to admit. The gap is not a bug in `MinDetectableEffect` or in `signBound` — both are correct in isolation for the purpose each was designed for — it is `signBound`'s degenerate path being reused, unmodified, as Condition 1's decision instrument, when its own confidence-grows-with-n formula was designed for a different question ("does repeated agreement license the sign-test bound") than the one it is now asked ("does this tied window license an equivalence CLAIM whose margin was derived from a DIFFERENT, more conservative formula"). | Found implementing the redundancy-detection plan's own acceptance criterion 4 as a property test. Re-verified twice at the coordinator's explicit request to measure rather than assume: once against #182 (ruled out), once against tightening `--redundancy-max-margin` (found insufficient, and now proven structural via the margin/signBound-width ratio, not just observed in one 200-trial sweep). Not fixed here: the real fix touches `signBound`'s degenerate path itself or how Condition 1 consumes it — e.g. refusing (UNKNOWN) whenever the diff interval takes the degenerate path at all, rather than accepting it whenever it happens to clear the derived margin — and that is a statistical design decision, not an implementation bug, so it is not picked unilaterally inside the PR that found the hole. [ADR-0008](adr/0008-measured-redundancy-is-cut-from-v0-2.md) records the decision, the three alternatives measured and rejected (tighten the ceiling; refuse the degenerate path; ship it as-is), and the direction a redesign should start from — behavioural identity as its own evidence kind, TOST kept for the genuinely noisy case with a power requirement. Every number in this row was independently reproduced on 2026-09-01, against the branch at `1180dd4`, before that record was written. | **Before this PR merges** — the redundancy-detection plan's own acceptance criterion 4 is not met until this is resolved, one way or the other, by whoever owns the statistical design call. Re-dated with this finding's precision (2026-08-31): the trigger is unchanged, but the evidence behind it is now a structural proof (the margin/width ratio diverges), not an empirical observation from one sweep. | @devarispbrown |
| <a id="155"></a>155 | **The bridge's training headroom multiplier (`pricing.TrainingHeadroomPct = 120`) and per-minute hosting rate are documented constants, not measured ones, and a job's recorded spend is an upper bound whenever the provider reports no actual cost.** The tuner-bridge plan's Step 2(a) calls for a headroom multiplier covering packed sequences, padding, and a provider-side validation split, none of which a local token count sees; the shipped constant is a judgement call, not a measurement, and Step 2(c)'s "estimate stands" rule means a provider that never reports `actual_cost_usd_micros` leaves every rendering saying `estimated` forever, with no mechanism to true it up. | [The tuner-bridge plan](plans/2026-08-31-tuner-bridge.md), Debt section (the same entry the plan pre-registers). Shipping a measured multiplier needs live spend against a real provider, which this PR does not do — `adapters/agent/pricing`'s train/serve tables ship empty specifically because no number here has been checked against a live call (see entry below on `#83` for the parallel reasoning applied to a dependency instead of a constant). | **When a second Tuner adapter lands, or when the pricing drift detector (`internal/cmd/pricingcheck`) gains a training-price or serve-price check — whichever is first.** | @devarispbrown |
| <a id="156"></a>156 | **FULLY REPAID (2026-09-01)** — [the bridge eval-seam PR](plans/2026-09-01-bridge-eval-seam.md) closed the remaining gap this row tracked: `kno bridge` now wires a real `core.Tuner` (`cli/bridge_measure.go`'s `newBridgeTuner`) and reaches `Deploy` for real once armed and confirmed (`confirmAndRun` calls `bridge.Run`), so the leak risk this row named is live rather than dormant, and the sweep/doctor-report machinery below now has a caller that can actually exercise it. See [`#158`](#158) for what closed it. **REPAID (mechanism) in the tuner-bridge orchestration-loop PR** (2026-09-01). The trigger named below — "when the bridge's job-submission orchestration loop lands" — has fired: `bridge.DeployGroup`/`SettleServeTick`/`TeardownGroup`/`SweepEndpoints` (`bridge/hosting.go`) and `bridge.Run` (`bridge/run.go`) implement the per-minute settle-forward tick loop, `--bridge-max-live-endpoints` serialization (`LiveEndpointLimiter`), unconditional teardown on every exit path with a loud `leaked` state on failure, and the resume-time endpoint sweep (own-row EndpointID first, `Tuner.ListEndpoints` fallback for the crash-during-Deploy window, settling at `--bridge-max-serve-minutes` when the provider no longer lists an endpoint). `store.LeakedEndpoints` and `kno doctor --db` report any row carrying a non-null `EndpointID` with a null `TornDownAt`, across every run. All of it is tested (`bridge/hosting_test.go`, `bridge/run_test.go`, `store/tuning_test.go`, `cli/wiring_test.go`). **Not fully repaid**: `kno bridge` itself cannot yet reach `Deploy` in this build — see entry [#158](#158) — so no real endpoint can be created by the shipped CLI yet, and the leak risk stays dormant until #158 closes. This row stays open until then, tracking only that gap. ~~A bridge run can leak a dedicated inference endpoint that keeps billing after Kno exits. `core.Tuner.Deploy`/`Teardown` and `store.TuningJobRecord`'s hosting columns (`EndpointID`, `DeployedAt`, `TornDownAt`, `ServeMinutes`, `ServeCostUSDMicros`) exist — the data model the tuner-bridge plan's Step 2(f)/(g) hosting lifecycle needs — but the enforcement built on top of it does not ship in this PR: no per-minute settle-forward tick loop, no `--bridge-max-live-endpoints` serialization, no resume-time endpoint sweep (Step 2(g)), and no `kno doctor` report of a row carrying a non-null `EndpointID` with a null `TornDownAt`. Until those land, a process that dies between `Deploy` and `Teardown` leaves a durable record of the leak but nothing that reads it or stops the meter.~~ | [The tuner-bridge plan](plans/2026-08-31-tuner-bridge.md), Debt section and accepted risks ("A leaked endpoint bills after Kno exits"). This PR's own report explains the scope cut: no CLI orchestration loop exists yet to hold a live endpoint across eval passes, so building the sweep and the doctor report ahead of the loop that would exercise them was judged premature — better to land the durable shape now and the enforcement with the first caller that actually deploys something. | **Fully closes when [#158](#158) closes** (an EvalRunner lands and `kno bridge` can reach `Deploy`); until then this row is repaid-but-open, tracking only the CLI-reachability gap. | @devarispbrown |
| <a id="157"></a>157 | **Entry [`#83`](#83)'s trigger ("the first Tuner PR, or v0.1.0, whichever is first") has fired, and its own text — re-dated once already on 2026-08-29 specifically so it could not self-satisfy — says a second re-date is not available.** This PR (the tuner-bridge plan) is that first Tuner PR: it ships `adapters/tuner/together`, a real `core.Tuner` implementation. It does **not** ship `adapters/pool/parquet`, the repayment #83 names. The ledger rules permit repay, re-date-with-a-written-reason, or promotion to won't-fix with an ADR — and #83's own text forecloses the second option for itself. This row does not resolve #83; it records that the trigger fired and the repayment did not land in the PR that fired it, so the disposition decision (repay in a fast-follow PR before this one merges, or promote #83 to won't-fix with an ADR explaining why a parquet reader is no longer worth the dependency) is left to a maintainer rather than decided unilaterally by the workstream that triggered it. | [The tuner-bridge plan](plans/2026-08-31-tuner-bridge.md) Debt section, which assumed the same PR series would ship both the adapter and the reader ("an independent workstream... blocking nothing"). Reading the tree at implementation time found no `adapters/pool/parquet` and no time budget in this pass to add a new pool adapter (dependency evaluation, license check, format parsing, its own test suite) alongside the bridge's engine, adapter, and CLI layers. Flagged here rather than silently left for `#83` to look repaid when it is not. | **Before this PR merges to `main`.** A trigger that already fired and named "no second re-date" is not a row this ledger can carry forward silently; the disposition must be made at merge time, not discovered at the next ledger review. | @devarispbrown |
| <a id="158"></a>158 | **REPAID (2026-09-01)** — by [the bridge eval-seam PR](plans/2026-09-01-bridge-eval-seam.md): `core.ScorePass` (`core/score.go`) is the exported budget-guarded, retrying, panic-safe invoke seam this entry names as needed (option "a small, additive, pre-1.0-permitted change: exporting the existing type" — realized as a narrow new function rather than exporting `invoker` itself, which stays unexported); `kno bridge` gained `--evals`, sealed at the CLI choke point; `bridge.EvalRunner.Measure` is implemented in production (`bridge.ScoreEvalRunner`, `bridge/score.go`) and wired into `cli/bridge.go`'s `confirmAndRun`; and `BRIDGE_GROUP_VERDICT_INTERFERENCE` is emitted via the newly-exported `stats/interval.NetEffect` (`core/select.go`'s `netInterval` now delegates to it), closing both triggers this entry named. Two residual gaps from this repayment are recorded as their own entries: [`#161`](#161) (the Together dedicated-endpoint inference URL this PR's `cli/bridge_measure.go` assumes is unconfirmed against a live endpoint) and the accepted risks at [`#159`](#159)/[`#160`](#160). Original entry follows, for the record. ~~`kno bridge` cannot run its own orchestration loop end to end: `bridge.Run` (`bridge/run.go`) requires an `EvalRunner` — the seam that invokes a group's deployed model over dev and control Cases and returns paired score deltas — and this PR ships the interface with no production implementation.** Everything up to and including deployment is real and tested: `SubmitGroup` (adopt-by-suffix included), the poll-to-terminal loop, `ReconcileTerminal`, `DeployGroup`, the settle-forward hosting tick, and `TeardownGroup` all run for real against a `core.Tuner`. `bridge.Run` refuses to start without an `EvalRunner` rather than deploying a paid endpoint with nothing to measure it, so `kno bridge --bridge` plans, prices, and confirms, then stops with an actionable error naming the gap — never a fake success. A real `EvalRunner` needs two things this PR's `kno bridge` flags do not provide: (1) an Evals source to resolve Case IDs into Case content (`kno bridge` today takes `--pool` but no `--evals`), and (2) the SAME budget-guarded, retrying, panic-safe invoke path `core.invoker` (`core/invoke.go`) already gives Value and Validate — unexported from `core` today, so bridge cannot reach it without either a core export (a small, additive, pre-1.0-permitted change: exporting the existing type rather than re-deriving its six previously-fixed defects) or a hand-rolled reimplementation this PR deliberately declined to risk on a P0 money-and-correctness path. Acceptance criteria 18-21 (the leave-one-group-out measurement itself, Δ_group/Δ_control with intervals, the BridgeGroupMeasured event's real numbers) do not hold in this build; criteria 22-23 (fixture-backed poll sequences, no network on the un-armed path) and the money-safety criteria around Submit/Deploy/Teardown/Sweep DO hold. Also unresolved in `bridge/run.go`'s `groupMeasuredEvent`: the `BRIDGE_GROUP_VERDICT_INTERFERENCE` verdict is never emitted — `HarmBound`'s one-sided interval cannot be read for a "confirmed harmful" claim without the same variance-weighted net-effect combination `core/select.go`'s unexported `netInterval` implements, and shipping a guessed sign rule for a regression claim was judged a statistical-validity risk CLAUDE.md's prime directive 5 exists to prevent.~~ | [The tuner-bridge orchestration-loop PR](plans/2026-08-31-tuner-bridge.md)'s own report: this is the honest stop the workstream made after implementing submission, polling, reconciliation, deployment, the hosting lifecycle, and the resume sweep for real, rather than fabricating a measurement step or reimplementing `core.invoker`'s retry/billing logic a second time. | **When a core export for the budget-guarded invoke path lands (whichever PR adds `--evals` to `kno bridge` and a production `EvalRunner`), or when `core/select.go`'s net-effect combination logic is exported/duplicated for the INTERFERENCE verdict — whichever is first, and independently for each half.** | @devarispbrown |
| <a id="159"></a>159 | **REPAID (2026-09-02)** — by [the OpenAI Tuner pricing-seam plan](plans/2026-09-02-openai-tuner.md), PR 1: `bridge.ScoreEvalRunner.AcceptFreeCalls` is now a field the caller sets (`cli/bridge_measure.go`'s `confirmAndRun`), computed as `evalPrice == nil` rather than hardcoded `true`. `evalPrice` is resolved once in `cli/bridge.go`'s `runBridgeCore` (`resolveEvalPrice`, beside `resolveTrainPrice`/`resolveServePrice`) from a new `fineTunedTable` in `adapters/agent/pricing` (`training.go`), keyed by `(scheme, base model)` and **shipping empty** — the same doctrine `trainTable`/`serveTable` already apply, so a base model with no row is refused under a cost cap rather than priced at zero or multiplied. Together's `evalPrice` stays `nil` (the table carries zero `"together"` rows), so `AcceptFreeCalls` stays `true` and every dollar figure a Together run prints is unchanged — asserted by `TestBridgeUnarmedTogetherPlanOmitsEvalPassCost` and `TestResolveEvalPriceTogetherStaysNil` (`cli/bridge_test.go`), and `TestEvalPassCeilingNilPriceIsZero` (`cli/bridge_internal_test.go`). The un-armed plan and the armed run's consent total both carry the eval pass as a third addend (`evalPassCeiling`, `cli/bridge.go`), computed with `pricing.EstimateWithPrice` directly rather than by constructing an Agent, so an un-credentialed `kno bridge` still prints a plan with zero network calls. **One gap this repayment opens, not closes**: with no escape-hatch flag for the eval-pass rate, a per-token scheme whose base model has no `fineTunedTable` row also reads `evalPrice == nil` and is treated as free — see [`#162`](#162). Original entry follows. ~~**Bridge asserts its scoring calls are free (`core.ScoreParams.AcceptFreeCalls`, wired by `bridge.ScoreEvalRunner`), and that assertion is only as true as the provider's billing model.** A Together dedicated endpoint is reserved capacity billed per minute per replica, so inference on it is zero-marginal today; if a provider bills tokens on top of reserved capacity, the hosting ticker (`bridge/hosting.go`) would under-count and `--max-cost-usd` would under-enforce. Mitigated by the assertion being explicit and per-adapter (`bridgeAgentFactory` in `cli/bridge_measure.go`) rather than global.~~ | [The bridge eval-seam plan](plans/2026-09-01-bridge-eval-seam.md), accepted risk 1 — the plan's own text, mirrored here per its instruction to record accepted risks in this ledger with a real trigger. | **When a second Tuner adapter lands, or the first time a provider's dedicated pricing carries a per-token line — whichever is first.** Fired and repaid 2026-09-02. | @devarispbrown |
| <a id="160"></a>160 | **The ablation group travels in `store.MeasurementKey.AssetID` (`bridge/measure.go`'s `loadGroupScores`/`recordScores`).** A documented repurposing of a vocabulary field (CLAUDE.md prime directive 2), with one prior instance (`core/validate_loop.go`'s `measureHoldoutArm`, which stuffs the Select run ID into the same field). A third instance means the key needs a real dimension instead of two unrelated repurposings sharing one string slot. | [The bridge eval-seam plan](plans/2026-09-01-bridge-eval-seam.md), accepted risk 2, and `docs/plans/2026-09-01-bridge-eval-seam.md`'s "Both blockers resolved" section, which names this convention rather than inventing a new store table. | **The third caller to repurpose `AssetID`, or before 1.0 — whichever is first.** | @devarispbrown |
| <a id="161"></a>161 | **REPAID (mechanism) 2026-09-02** — by [the OpenAI Tuner pricing-seam plan](plans/2026-09-02-openai-tuner.md), PR 1, for the SWITCH-SCHEME leg of this entry's trigger only: `newBridgeTuner` and `bridgeAgentFactory` (`cli/bridge_measure.go`) no longer `switch scheme` at all — both dispatch through a new scheme-keyed `bridgeAdapters` registry (`map[string]bridgeAdapter`, each entry bundling a `tunerFactory` and an `evalAgentFactoryBuilder`), so a second Tuner adapter is a map entry rather than a new branch in two places. There is no more switch for that leg's trigger to fire on. **NOT repaid, and still fully open**: the live-endpoint URL assumption this entry's original text names — that a Together dedicated endpoint answers OpenAI-compatible chat completions at `together.DefaultBaseURL + "/v1"` — is untouched by PR 1, which ships no adapter and made no live call (`KNO_LIVE_TESTS` was not set in this pass either). This row stays open, tracking only that gap, with its trigger re-dated to drop the now-satisfied disjunct. Original entry follows. ~~**`cli/bridge_measure.go`'s `bridgeAgentFactory` asserts, unconfirmed against a live endpoint, that a Together dedicated endpoint answers OpenAI-compatible chat completions at `together.DefaultBaseURL + "/v1"` with its served-model name in the `model` field.** This is the same class of provider fact the tuner-bridge plan tags `(verify)` throughout and is the one piece of the eval seam this PR could not test against real infrastructure: `KNO_LIVE_TESTS`/`KNO_ALLOW_TUNING_SPEND` gate live recording, and none was run in this pass. If the assumption is wrong, `bridge.Run`'s measurement calls fail loudly (a transport or 404 error surfaces through `core.ScorePass`'s retry path and the group is reported `unknown`) rather than silently — money is not at risk, only the measurement is — but the CLI wiring should not be trusted end to end until checked against a live deployment. Separately, `bridge/run.go`'s `deployAndMeasure` builds the invoked `AgentRef` directly from `core.Endpoint.Served`/`.Provider` rather than calling `core.Tuner.Model`, which `core.Tuner`'s own doc names as "precisely the seam Tier 4 will use" — inherited from `feat/tuner-bridge` (#184) unchanged, and now load-bearing for the first time because this PR is what makes `Deploy` reachable from the CLI at all.~~ | This PR's own report: constructing a real `together.Tuner` and a real `openaicompat.Agent` against a live dedicated endpoint was out of this pass's time budget, and `adapters/tuner/together/testdata/fixtures` fixtures cover Submit/Status, not Deploy's actual serving route. Re-dated 2026-09-02 by the pricing-seam PR: the switch-scheme disjunct is satisfied (there is no switch left), so only the live-verification leg is carried forward. | **Before the first live (`KNO_LIVE_TESTS=1`) bridge run.** | @devarispbrown |
| <a id="162"></a>162 | **REPAID (2026-09-02)** — by PR 2 of [the OpenAI Tuner plan](plans/2026-09-02-openai-tuner.md): the honest fix chosen is the SECOND of the two options this entry's original text left open — "refusing to construct a per-token scheme's Agent under a cost cap when `evalPrice` is nil" — not a `--price-eval-per-mtok` flag, because a user-typed rate for an inference dimension `fineTunedTable` deliberately ships unpriced is the same fabricated-number risk the table's own doctrine refuses, one layer up. `cli/bridge.go`'s new `acceptFreeCalls(evalPrice, servePrice)` replaces the bare `evalPrice == nil` rule with `evalPrice == nil && servePrice.PerMinuteUSDMicros > 0`: free is now asserted ONLY when a real, nonzero hosting charge already covers the eval pass (Together's case), never merely because no per-token rate happened to resolve. `adapters/tuner/openai` ships in this PR with `fineTunedTable` carrying zero `"openai"` rows (deliberately — see that package's doc), so for every OpenAI model `evalPrice` is `nil` and `adapters/agent/pricing.serveTable`'s new genuine-zero `"openai"` rows make `servePrice.PerMinuteUSDMicros` `0` — `acceptFreeCalls` returns `false`, `core.ScorePass.estimate()`'s existing unpriced-under-a-cap refusal (`core/score.go`) becomes REACHABLE instead of bypassed, and a capped `kno bridge --tuner openai:<model>` refuses with an actionable error naming the missing price rather than silently treating the eval pass as free. That refusal is the intended behaviour, not a gap: see `adapters/tuner/openai`'s package doc and this PR's report. Tested: `TestAcceptFreeCalls` (`cli/bridge_test.go`, table-driven over all four scheme/price combinations) and the pre-existing `TestScoreEvalRunnerMeasureRefusesAZeroEstimateWhenNotAcceptingFreeCalls` (`bridge/score_test.go`, proves `AcceptFreeCalls: false` + a cost cap + an unpriced Agent refuses) compose to cover the full path; `TestResolveEvalPriceOpenAIStaysNilInThisPR` pins the table staying empty. Original entry follows. ~~**`AcceptFreeCalls := evalPrice == nil` (docs/debt.md#159's repayment) has no escape-hatch flag on the "not free" side.** `--price-train-per-mtok` and `--price-serve-per-minute` both let a user state an unpriced rate explicitly; the eval-pass dimension has no equivalent. A per-token Tuner scheme (PR 2's `adapters/tuner/openai` and beyond) whose BASE model has no row in `adapters/agent/pricing`'s `fineTunedTable` reads `evalPrice == nil` exactly like a genuinely zero-marginal provider (Together) does, so `ScoreEvalRunner.AcceptFreeCalls` is `true` and `core.ScorePass` asserts every call free — reopening the exact free-calls hole [`#159`](#159) exists to close, for an UNTABLED model specifically. Not reachable in this build: PR 1 ships no per-token Tuner adapter, so `fineTunedTable` has no scheme whose absence this fires for yet. Named as an accepted risk in [the OpenAI Tuner pricing-seam plan](plans/2026-09-02-openai-tuner.md)'s review (the "no escape-hatch flag" finding), not fixed here: the honest fix is either a `--price-eval-per-mtok`-shaped flag (mirroring `--price-train-per-mtok`) or refusing to construct a per-token scheme's Agent under a cost cap when `evalPrice` is nil, and choosing between them is a statistical/UX call for whoever ships the first per-token adapter, not a decision to make unilaterally inside PR 1.~~ | [The OpenAI Tuner pricing-seam plan](plans/2026-09-02-openai-tuner.md), "Two more the review should weigh," item 3 — carried into this ledger per the plan's own instruction to mirror accepted risks here with a real trigger. | **Before `adapters/tuner/openai` (or any other per-token Tuner adapter) ships with a base model absent from `fineTunedTable`, or before PR 2 merges — whichever is first.** Fired and repaid 2026-09-02. | @devarispbrown |
| <a id="163"></a>163 | **`adapters/tuner/together/security.go` duplicates HTTP-transport-safety policy (redirect refusal, plain-HTTP refusal, private-address refusal, credential binding) that already has a shared home: `adapters/internal/endpointsec`, whose own package doc names three prior copies (the agent transport, LangSmith, Langfuse) and says plainly "New adapters must use this package instead of copying a fourth time."** `together/security.go` was written without that package in view — its own doc calls `adapters/agent/internal/transport`'s unreachability "a genuine plan-vs-code contradiction" and proposes, as the fix, a shared package one directory up from both `adapters/agent` and `adapters/tuner` — not realizing `adapters/internal/endpointsec` already IS that package, just under a different name and built for a different first caller (the Hugging Face dataset-server client). This PR's own `adapters/tuner/openai` does NOT repeat the mistake: its `client.go` imports `endpointsec.Check`/`WithResolvedAddressCheck` directly, so no fifth copy of the policy was written, and no new `adapters/internal/transport` package was invented either (the plan's own "lift it now" language, satisfied by using what already existed rather than building a duplicate of the fix). `together/security.go` itself is untouched by this PR — migrating a second, already-shipped, already-tested adapter's transport layer onto `endpointsec` mid-PR was judged out of scope and out of proportion to the risk (Together's copy is not wrong, only duplicated), and left for a dedicated, reviewed diff. | This PR's own report (`docs/plans/2026-09-02-openai-tuner.md`'s non-negotiable item on the transport question): "adapters/agent/internal/transport is Go-internal to adapters/agent/ and unreachable from adapters/tuner/... This is the second instance — CLAUDE.md's DRY rule fires on the third... Do not silently write a third copy." Investigating found the second copy was unnecessary to write at all — `endpointsec` already existed — which changes the finding from "accept a second copy with a trigger" to "migrate the one copy that predates the shared package." | **Before a third Tuner (or any adapter outside `adapters/agent/`) would otherwise write its own local HTTP-transport-safety layer, or when `adapters/tuner/together/security.go` is next touched for an unrelated reason — whichever is first.** | @devarispbrown |
